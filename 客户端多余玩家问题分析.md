# 客户端多余玩家问题分析

## 问题描述

**联机情况**: 2 人联机（1 主机 + 1 客户端）

**现象**: 客户端游戏中看到 3 个角色

**观察到的角色**:

1. 一个在出生点不动的角色
2. 一个跟随主机移动的角色
3. 客户端自己的角色

**异常**: 客户端 Debug 输出显示有 **2 个远程玩家**，但实际只应该有 1 个（主机）

## Debug 输出分析

### 🔍 最新日志分析 (2025-11-08 11:21:06)

#### 主机端 Debug 数据 ✅ 正常

```
Role: Server
NetworkStarted: True
LocalPlayer: Host:9050
RemoteCharacters: 0        ← 正确！客户端已断开，没有远程玩家
PlayerStatuses: 1
ConnectedPeers: 1
AllCharactersInScene: 10   ← 场景中有10个角色（包括NPC）
```

**主机的本地角色**:

```json
{
  "GameObjectName": "Character(Clone)",
  "InstanceId": -207920,
  "Active": true,
  "Position": "(-4.96, 0.02, -85.05)",
  "HasRemoteReplicaTag": false,  ← 正确！本地玩家没有RemoteReplicaTag
  "EnabledRenderers": 9,
  "TotalRenderers": 9,
  "ComponentCount": 23
}
```

**场景中的其他角色**: 9 个 NPC 角色（都没有 RemoteReplicaTag）

#### 客户端 Debug 数据 (2025-11-08 11:21:00) ⚠️ 发现关键问题！

```
Role: Client
NetworkStarted: True
LocalPlayer: Client:d9805d89
ClientRemoteCharacters: 2   ← 异常！应该只有 1 个（主机）
ClientPlayerStatuses: 2
ConnectedPeer: Connected
AllCharactersInScene: 5     ← 场景中有5个角色
```

**关键发现**:

-   `MyLocalPlayerId`: `Client:d9805d89`
-   `MyNetworkId`: `192.168.123.1:9050` ← 客户端的网络地址
-   `AllRemotePlayerIds`: `192.168.137.30:62825, Host:9050`

**问题**: 客户端的 `MyNetworkId` (192.168.123.1:9050) 与 `AllRemotePlayerIds` 中的任何一个都不匹配！

### 旧版日志分析 (2025-11-08 10:36:41)

#### 主机端 Debug 数据 ✅ 正常

```
Role: Server
NetworkStarted: True
LocalPlayer: Host:9050
RemoteCharacters: 1        ← 正确！只有 1 个远程玩家（客户端）
PlayerStatuses: 1
ConnectedPeers: 1
```

**主机的远程玩家**:

```json
{
    "Index": 1,
    "PeerEndPoint": "192.168.137.30:52729",
    "GameObjectName": "Character(Clone)(Clone)",
    "Health_Current": 51.0,
    "Health_Max": 51.0,
    "ComponentCount": 28
}
```

#### 客户端 Debug 数据 ❌ 异常

```
Role: Client
NetworkStarted: True
LocalPlayer: Client:c27add0f
ClientRemoteCharacters: 2   ← 异常！应该只有 1 个（主机）
ClientPlayerStatuses: 2
ConnectedPeer: Connected
```

### 🔥 最新日志：ClientRemoteCharacters 详细分析

#### 远程玩家 1 (主机) ✅ 正常

```json
{
  "Index": 2,
  "PlayerId": "Host:9050",
  "GameObjectName": "Character(Clone)(Clone)",
  "GameObjectInstanceId": -155830,
  "Position": "(-4.96, 0.02, -85.05)",
  "Health_Current": 106.0,
  "Health_Max": 106.0,
  "HasRemoteReplicaTag": true,
  "HasNetInterpolator": true,
  "NetInterp_Enabled": true,
  "HasAnimInterpolator": true,
  "AnimInterp_Enabled": true,
  "EnabledRenderers": 9,
  "TotalRenderers": 9,
  "IsLocalPlayerDuplicate": false,  ← 正确！不是本地玩家副本
  "IsSelfId_Check": false,
  "ComponentCount": 27
}
```

#### 远程玩家 2 (客户端自己) ❌ 异常！

```json
{
  "Index": 1,
  "PlayerId": "192.168.137.30:62825",  ← 这是客户端自己的网络地址！
  "GameObjectName": "Character(Clone)(Clone)",
  "GameObjectInstanceId": -154834,
  "Position": "(-4.96, 0.02, -85.05)",
  "Health_Current": 0.0,  ← 注意：血量为0！
  "Health_Max": 0.0,
  "HasRemoteReplicaTag": true,
  "HasNetInterpolator": true,
  "NetInterp_Enabled": true,
  "HasAnimInterpolator": true,
  "AnimInterp_Enabled": true,
  "EnabledRenderers": 1,  ← 只有1个渲染器启用（异常）
  "TotalRenderers": 1,
  "IsLocalPlayerDuplicate": false,  ← 检测失败！应该是true
  "IsSelfId_Check": false,  ← IsSelfId检查失败！
  "ComponentCount": 27
}
```

**关键问题**:

1. `PlayerId` = `192.168.137.30:62825` 是客户端的网络地址
2. `IsLocalPlayerDuplicate` = false（检测失败！）
3. `IsSelfId_Check` = false（IsSelfId 检查失败！）
4. `Health` = 0（异常状态）
5. `EnabledRenderers` = 1（应该是 9）

#### 本地玩家信息

```json
{
  "GameObjectName": "Character(Clone)",
  "InstanceId": -139348,
  "IsMain": true,
  "Active": true,
  "Position": "(-4.96, 0.02, -85.05)",
  "HasRemoteReplicaTag": false,  ← 正确！本地玩家没有RemoteReplicaTag
  "HasNetInterpolator": false,
  "HasAnimInterpolator": false,
  "InClientRemoteCharacters": false,
  "PlayerId": "null"
}
```

#### 场景中的所有角色 (AllCharactersInScene)

```json
{
  "Count": 5,
  "Data": [
    {
      "GameObjectName": "Character(Clone)",
      "InstanceId": -139348,
      "IsMain": true,  ← 本地玩家
      "InClientRemoteCharacters": false
    },
    {
      "GameObjectName": "Character(Clone)",
      "InstanceId": -152866,
      "IsMain": false,
      "InClientRemoteCharacters": false,  ← 不在远程列表中（可能是NPC）
      "PlayerId": "null"
    },
    {
      "GameObjectName": "Character(Clone)(Clone)",
      "InstanceId": -154834,
      "IsMain": false,
      "InClientRemoteCharacters": true,  ← 远程玩家2（自己的副本）
      "PlayerId": "Host:9050"  ← 注意：这里显示的是Host，但实际是192.168.137.30:62825
    },
    {
      "GameObjectName": "Character(Clone)(Clone)",
      "InstanceId": -155830,
      "IsMain": false,
      "InClientRemoteCharacters": true,  ← 远程玩家1（主机）
      "PlayerId": "Host:9050"
    },
    {
      "GameObjectName": "Character(Clone)",
      "InstanceId": -157740,
      "InClientRemoteCharacters": false,
      "PlayerId": "null"
    }
  ]
}
```

### ClientPlayerStatuses (最新)

```json
{
  "Count": 2,
  "Data": [
    {
      "PlayerId": "Host:9050",
      "PlayerName": "Host",
      "IsInGame": true,
      "SceneId": "Base",
      "Position": "(-4.96, 0.02, -85.05)",
      "Latency": 0
    },
    {
      "PlayerId": "192.168.137.30:62825",  ← 客户端自己
      "PlayerName": "Client",
      "IsInGame": true,
      "SceneId": "Base_SceneV2",
      "Position": "(-4.96, 0.02, -85.05)",
      "Latency": 12
    }
  ]
}
```

## 问题分析

### 1. 核心问题：客户端为自己创建了远程副本！

❌ **clientRemoteCharacters**: 2 个（错误！应该只有 1 个）

-   Host:9050 ✅ 正确（主机）
-   192.168.137.30:62825 ❌ **错误！这是客户端自己！**

❌ **clientPlayerStatuses**: 2 个（错误！应该只有 1 个）

✅ **主机端正常**: remoteCharacters 只有 1 个（客户端）

### 2. 🔍 问题根源深度分析

#### A. 客户端收到了自己的 REMOTE_CREATE 包

**代码位置**: `CreateRemoteCharacter.cs` -> `CreateRemoteCharacterForClient()`

```csharp
public static async UniTask CreateRemoteCharacterForClient(string playerId, ...)
{
    // ★ 不给自己创建"远程自己"
    if (NetService.Instance.IsSelfId(playerId)) return;  // ← 这个检查失效了！
    ...
}
```

**问题**: `IsSelfId()` 检查失败，导致客户端为自己创建了远程副本

#### B. PlayerId 不匹配的根本原因

**最新日志显示的 ID**:

-   `MyLocalPlayerId`: `Client:d9805d89` ← 客户端本地生成的 ID
-   `MyNetworkId`: `192.168.123.1:9050` ← 客户端认为的自己的网络地址
-   `AllRemotePlayerIds`: `192.168.137.30:62825, Host:9050` ← 所有远程玩家 ID
-   **远程副本的 PlayerId**: `192.168.137.30:62825` ← 主机看到的客户端地址

**关键发现**:

```
客户端认为自己是: 192.168.123.1:9050
主机认为客户端是: 192.168.137.30:62825
```

**这是一个网络地址不一致的问题！**

#### C. 为什么会有两个不同的 IP 地址？

可能的原因：

1. **多网卡问题**: 客户端有多个网络接口

    - `192.168.123.1` - 客户端本地看到的 IP（可能是虚拟网卡或另一个网络接口）
    - `192.168.137.30` - 主机看到的客户端 IP（实际连接的 IP）

2. **NAT/端口映射**: 客户端通过 NAT 连接，内外网地址不同

3. **Socket 绑定问题**: 客户端绑定到了错误的网络接口

#### D. IsSelfId() 检查失败的原因

```csharp
public bool IsSelfId(string id)
{
    var mine = localPlayerStatus?.EndPoint;  // = "Client:d9805d89"
    if (id == mine) return true;  // "192.168.137.30:62825" != "Client:d9805d89" → false

    // 客户端额外检查
    if (!IsServer && connectedPeer != null)
    {
        var myNetworkId = connectedPeer.EndPoint.ToString();  // = "192.168.123.1:9050"
        if (id == myNetworkId) return true;  // "192.168.137.30:62825" != "192.168.123.1:9050" → false
    }

    return false;
}
```

**三个 ID 都不匹配**:

1. `localPlayerStatus.EndPoint` = `Client:d9805d89`
2. `connectedPeer.EndPoint` = `192.168.123.1:9050`
3. 主机发送的 `playerId` = `192.168.137.30:62825`

**结果**: IsSelfId() 返回 false，客户端为自己创建了远程副本！

#### C. 主机广播逻辑问题

**代码位置**: `SceneNet.cs` -> `Server_HandleSceneReady()`

```csharp
// 2) 广播给同场景的其他人：创建 fromPeer
foreach (var kv in SceneM._srvPeerScene)
{
    var other = kv.Key;
    if (other == fromPeer) continue;  // ← 应该跳过发送者
    if (kv.Value == sceneId)
    {
        var w = new NetDataWriter();
        w.Put((byte)Op.REMOTE_CREATE);
        w.Put(playerId);  // ← 这里发送的是什么 ID？
        ...
        other.Send(w, DeliveryMethod.ReliableOrdered);
    }
}
```

**可能的问题**:

1. 主机可能把客户端的 REMOTE_CREATE 包也发回给了客户端自己
2. 或者客户端收到了自己的状态更新包

### 3. 三个角色的身份确认（基于最新日志）

根据最新日志数据：

| 角色   | 身份                         | PlayerId             | InstanceId | 位置     | 状态     | 渲染器   |
| ------ | ---------------------------- | -------------------- | ---------- | -------- | -------- | -------- |
| 角色 1 | **本地玩家** (Main)          | Client:d9805d89      | -139348    | 出生点   | 本地控制 | 应该隐藏 |
| 角色 2 | **远程玩家 - 主机** ✅       | Host:9050            | -155830    | 跟随主机 | 正常同步 | 9/9 启用 |
| 角色 3 | **远程玩家 - 自己的副本** ❌ | 192.168.137.30:62825 | -154834    | 同位置   | 异常     | 1/1 启用 |

**关键发现**:

-   客户端为**自己**创建了一个远程副本（InstanceId: -154834）
-   PlayerId `192.168.137.30:62825` 是主机看到的客户端网络地址
-   这个副本的血量为 0，只有 1 个渲染器启用（异常状态）
-   `IsLocalPlayerDuplicate` 检测失败（应该是 true，实际是 false）

### 4. 网络地址不一致问题

**客户端的三个不同 ID**:

1. **本地 PlayerId**: `Client:d9805d89` (localPlayerStatus.EndPoint)
2. **客户端认为的网络地址**: `192.168.123.1:9050` (connectedPeer.EndPoint)
3. **主机看到的客户端地址**: `192.168.137.30:62825` (peer.EndPoint)

**问题**: 这三个 ID 都不相同，导致 `IsSelfId()` 检查失败！

**可能的原因**:

-   多网卡环境（虚拟网卡、WiFi、有线网络）
-   客户端绑定到了错误的网络接口
-   NAT/端口映射导致的地址转换

## 解决方案

### 1. 修复 IsSelfId() 检查（推荐）

**问题**: PlayerId 不一致导致检查失效

**解决方案 A**: 统一使用网络地址作为 PlayerId

```csharp
// 在客户端连接时，使用主机分配的 ID
if (!IsServer && connectedPeer != null)
{
    localPlayerStatus.EndPoint = connectedPeer.EndPoint.ToString();  // 使用网络地址
}
```

**解决方案 B**: 改进 IsSelfId() 检查逻辑

```csharp
public bool IsSelfId(string id)
{
    var mine = localPlayerStatus?.EndPoint;
    if (string.IsNullOrEmpty(id) || string.IsNullOrEmpty(mine)) return false;

    // 直接比较
    if (id == mine) return true;

    // 如果是客户端，还要检查网络地址
    if (!IsServer && connectedPeer != null)
    {
        var myNetworkId = connectedPeer.EndPoint.ToString();
        if (id == myNetworkId) return true;
    }

    return false;
}
```

### 2. 在 REMOTE_CREATE 时额外检查（推荐）

**代码位置**: `CreateRemoteCharacter.cs`

```csharp
public static async UniTask CreateRemoteCharacterForClient(string playerId, ...)
{
    // 多重检查：防止为自己创建远程副本
    if (NetService.Instance.IsSelfId(playerId)) return;

    // 额外检查：如果 playerId 包含本机 IP
    if (!string.IsNullOrEmpty(playerId) && playerId.Contains(GetLocalIP()))
    {
        Debug.LogWarning($"[CreateRemote] 阻止为自己创建远程副本: {playerId}");
        return;
    }

    // 检查是否已经存在
    if (clientRemoteCharacters.ContainsKey(playerId) && clientRemoteCharacters[playerId] != null)
        return;

    ...
}
```

### 2. 隐藏本地玩家的模型

**可能的解决方案**:

```csharp
// 在客户端启动时，隐藏本地玩家的可视模型
if (!IsServer && CharacterMainControl.Main != null)
{
    var localPlayer = CharacterMainControl.Main.gameObject;
    var renderer = localPlayer.GetComponentsInChildren<Renderer>();
    foreach (var r in renderer)
    {
        r.enabled = false; // 隐藏渲染器
    }
}
```

### 3. 检查场景加载时的角色管理

**代码位置**: `SceneNet.cs` -> `TrySendSceneReadyOnce()`

**建议**: 确保场景切换时：

-   旧场景的远程角色被正确销毁
-   本地玩家不会被重复创建
-   远程玩家只在收到 `REMOTE_CREATE` 时创建

### 4. 添加调试标记

**建议**: 给每个角色添加可视化标记，方便识别：

```csharp
// 在创建远程角色时
instance.name = $"[REMOTE] {playerId}";

// 本地玩家
CharacterMainControl.Main.gameObject.name = "[LOCAL] Player";
```

## Debug 按钮增强 (2025-11-08)

### 已添加的新调试信息

#### 1. 本地玩家网络 ID 信息

-   `LocalPlayer.ConnectedPeerEndPoint` - 客户端连接的 Peer 端点（网络地址）
-   `LocalPlayer.ConnectedPeerId` - 客户端连接的 Peer ID

#### 2. 本地角色 GameObject 详细信息

-   `LocalCharacter` - 本地玩家的 GameObject 完整状态
    -   场景路径、实例 ID、激活状态
    -   是否有 RemoteReplicaTag（不应该有）
    -   渲染器状态（总数/启用数）
    -   所有组件列表

#### 3. 场景中所有 CharacterMainControl 对象

-   `AllCharactersInScene` - 场景中所有角色的列表
    -   每个角色的基本信息
    -   是否是 Main（本地玩家）
    -   是否在 remoteCharacters/clientRemoteCharacters 中
    -   对应的 PlayerId（如果是远程角色）

#### 4. 远程角色增强信息

-   `TotalRenderers` / `EnabledRenderers` - 渲染器状态
-   `ParentName` / `SiblingIndex` - 父对象和层级信息

#### 5. 客户端特有信息

-   `IsLocalPlayerDuplicate` - 是否是本地玩家的副本（关键！）
-   `IsSelfId_Check` - IsSelfId()检查结果
-   `CreateRemoteInfo` - 创建远程角色的相关信息
    -   `HasSelfDuplicate` - 是否存在自己的副本
    -   `MyNetworkId` - 自己的网络 ID（peer.EndPoint）
    -   `MyLocalPlayerId` - 自己的本地 PlayerId
    -   `AllRemotePlayerIds` - 所有远程玩家的 ID 列表

#### 6. LocalPlayerManager 信息

-   当前是否在游戏中
-   当前场景 ID
-   是否有 CharacterMain

### 使用方法

1. 在游戏中按 `=` 键打开联机面板
2. 点击 "调试信息" 按钮
3. 查看 Unity 日志输出的完整 JSON 数据

### 关键诊断点

通过新增的信息，可以快速诊断：

1. **客户端是否为自己创建了副本**

    - 检查 `ClientRemoteCharacters.Data[].IsLocalPlayerDuplicate`
    - 检查 `CreateRemoteInfo.HasSelfDuplicate`

2. **IsSelfId 检查是否正常工作**

    - 检查 `ClientRemoteCharacters.Data[].IsSelfId_Check`
    - 对比 `MyNetworkId` 和 `MyLocalPlayerId`

3. **场景中实际有多少角色**

    - 检查 `AllCharactersInScene.Count`
    - 查看每个角色的 `IsMain` 和 `InClientRemoteCharacters`

4. **本地玩家是否被错误标记**
    - 检查 `LocalCharacter.HasRemoteReplicaTag`（应该是 false）
    - 检查 `LocalCharacter.EnabledRenderers`（应该>0）

## 下一步调试

### 1. 运行增强的 Debug 按钮

在客户端和主机分别点击 Debug 按钮，对比输出的 JSON 数据，重点关注：

```json
{
    "ClientRemoteCharacters": {
        "Count": 2, // ← 应该是1，如果是2说明有问题
        "Data": [
            {
                "PlayerId": "Host:9050", // ← 正常的主机
                "IsLocalPlayerDuplicate": false,
                "IsSelfId_Check": false
            },
            {
                "PlayerId": "192.168.137.30:52729", // ← 可疑的副本
                "IsLocalPlayerDuplicate": true, // ← 如果是true，确认是自己的副本！
                "IsSelfId_Check": false // ← 如果是false，说明IsSelfId检查失败！
            }
        ]
    },
    "CreateRemoteInfo": {
        "HasSelfDuplicate": true, // ← 确认问题
        "MyNetworkId": "192.168.137.30:52729",
        "MyLocalPlayerId": "Client:c27add0f", // ← ID不匹配！
        "AllRemotePlayerIds": "Host:9050, 192.168.137.30:52729"
    }
}
```

### 2. 检查场景中的所有角色

```json
{
    "AllCharactersInScene": {
        "Count": 3, // ← 应该是2（1本地+1远程主机）
        "Data": [
            {
                "IsMain": true,
                "HasRemoteReplicaTag": false, // ← 本地玩家，正常
                "InClientRemoteCharacters": false
            },
            {
                "IsMain": false,
                "HasRemoteReplicaTag": true,
                "PlayerId": "Host:9050", // ← 远程主机，正常
                "InClientRemoteCharacters": true
            },
            {
                "IsMain": false,
                "HasRemoteReplicaTag": true,
                "PlayerId": "192.168.137.30:52729", // ← 多余的副本！
                "InClientRemoteCharacters": true
            }
        ]
    }
}
```

### 3. 监控角色创建

在 `CreateRemoteCharacterForClient` 中添加日志：

```csharp
Debug.Log($"[CREATE] 创建远程角色: {playerId} at {position}");
Debug.Log($"[CREATE] IsSelfId检查: {NetService.Instance.IsSelfId(playerId)}");
Debug.Log($"[CREATE] 当前场景中角色数: {FindObjectsOfType<CharacterMainControl>().Length}");
```

## 🎯 最终结论（基于最新日志）

### 问题根源确认

**客户端创建了 3 个角色**:

1. **本地玩家** (Client:d9805d89, InstanceId: -139348) - 本地控制
2. **远程玩家 - 主机** (Host:9050, InstanceId: -155830) - 正确 ✅
3. **远程玩家 - 自己的副本** (192.168.137.30:62825, InstanceId: -154834) - **错误！不应该存在** ❌

### 核心问题：网络地址不一致

**三个不同的 ID 导致 IsSelfId()检查失败**:

```
客户端本地ID:        Client:d9805d89
客户端认为的网络地址: 192.168.123.1:9050
主机看到的客户端地址: 192.168.137.30:62825  ← 主机用这个ID创建REMOTE_CREATE包
```

**IsSelfId() 检查流程**:

```csharp
IsSelfId("192.168.137.30:62825")
  → 检查 localPlayerStatus.EndPoint ("Client:d9805d89") → 不匹配
  → 检查 connectedPeer.EndPoint ("192.168.123.1:9050") → 不匹配
  → 返回 false → 创建远程副本！❌
```

### 为什么会有两个不同的 IP 地址？

**192.168.123.1** vs **192.168.137.30**

可能的原因：

1. **多网卡环境**: 客户端有多个网络接口（WiFi + 有线 + 虚拟网卡）
2. **Socket 绑定问题**: 客户端绑定到了错误的网络接口
3. **NAT/路由问题**: 网络地址转换导致的不一致

### 修复方案优先级

#### 🔥 高优先级：修复网络地址获取

**方案 1**: 客户端连接后，使用主机告知的地址作为自己的 ID

```csharp
// 在客户端连接成功后
if (!IsServer && connectedPeer != null)
{
    // 等待主机告知我们的真实网络地址
    // 通过握手包或专门的消息获取
    var myRealAddress = await GetMyAddressFromServer();
    localPlayerStatus.EndPoint = myRealAddress;
}
```

**方案 2**: 改进 IsSelfId()检查，支持多种 ID 格式

```csharp
public bool IsSelfId(string id)
{
    if (string.IsNullOrEmpty(id)) return false;

    // 检查本地ID
    if (id == localPlayerStatus?.EndPoint) return true;

    // 检查网络地址
    if (!IsServer && connectedPeer != null)
    {
        var myNetworkId = connectedPeer.EndPoint?.ToString();
        if (id == myNetworkId) return true;
    }

    // 🔥 新增：检查IP地址部分（忽略端口）
    if (id.Contains(":"))
    {
        var idIp = id.Split(':')[0];
        var myIp = connectedPeer?.EndPoint?.Address?.ToString();
        if (!string.IsNullOrEmpty(myIp) && idIp == myIp)
        {
            Debug.LogWarning($"[IsSelfId] IP匹配但端口不同: {id} vs {myIp}");
            // 可能需要进一步验证
        }
    }

    return false;
}
```

**方案 3**: 在 REMOTE_CREATE 时，主机发送客户端的本地 ID 而不是网络地址

```csharp
// 主机端：记录客户端的本地ID
Dictionary<NetPeer, string> peerToLocalId = new();

// 客户端连接时发送自己的本地ID
void Client_OnConnected()
{
    var w = new NetDataWriter();
    w.Put((byte)Op.HANDSHAKE);
    w.Put(localPlayerStatus.EndPoint);  // 发送本地ID
    connectedPeer.Send(w, DeliveryMethod.ReliableOrdered);
}

// 主机接收并记录
void Server_OnHandshake(NetPeer peer, string clientLocalId)
{
    peerToLocalId[peer] = clientLocalId;
}

// 广播REMOTE_CREATE时使用本地ID
void Server_BroadcastRemoteCreate(NetPeer fromPeer)
{
    var playerId = peerToLocalId[fromPeer];  // 使用客户端的本地ID
    // 广播给其他人...
}
```

#### ⚠️ 中优先级：添加防御性检查

在 `CreateRemoteCharacterForClient` 中添加额外的检查：

```csharp
public static async UniTask CreateRemoteCharacterForClient(string playerId, ...)
{
    // 多重检查
    if (NetService.Instance.IsSelfId(playerId)) return;

    // 🔥 新增：检查是否已经有本地玩家
    if (CharacterMainControl.Main != null)
    {
        var localPos = CharacterMainControl.Main.transform.position;
        var distance = Vector3.Distance(localPos, position);
        if (distance < 1f)  // 位置太接近
        {
            Debug.LogWarning($"[CreateRemote] 位置太接近本地玩家，可能是副本: {playerId}");
            // 可以选择跳过或进一步验证
        }
    }

    // 检查是否已存在
    if (clientRemoteCharacters.ContainsKey(playerId)) return;

    ...
}
```

#### 💡 低优先级：运行时清理

添加定期检查和清理错误的远程副本：

```csharp
// 在Update或定时器中
void CheckAndCleanupDuplicates()
{
    if (IsServer || clientRemoteCharacters == null) return;

    var toRemove = new List<string>();
    foreach (var kv in clientRemoteCharacters)
    {
        var playerId = kv.Key;
        var go = kv.Value;

        // 检查是否是自己的副本
        if (IsSelfId(playerId) || IsMyNetworkAddress(playerId))
        {
            Debug.LogWarning($"[CLEANUP] 发现自己的副本，删除: {playerId}");
            toRemove.Add(playerId);
            if (go != null) Destroy(go);
        }
    }

    foreach (var id in toRemove)
    {
        clientRemoteCharacters.Remove(id);
    }
}

bool IsMyNetworkAddress(string address)
{
    if (string.IsNullOrEmpty(address)) return false;

    // 检查是否包含本机IP
    var localIPs = GetLocalIPAddresses();
    foreach (var ip in localIPs)
    {
        if (address.StartsWith(ip)) return true;
    }

    return false;
}
```

### 验证方法

修复后，客户端应该：

-   `ClientRemoteCharacters: 1` (只有主机)
-   `ClientPlayerStatuses: 1` (只有主机)
-   `AllCharactersInScene: 2` (本地玩家 + 主机)
-   游戏中只看到 2 个角色（自己 + 主机）

### 下一步行动

1. **诊断网络环境**:

    - 检查客户端有哪些网络接口
    - 确认客户端绑定到了哪个接口
    - 验证主机看到的客户端地址

2. **实施修复**:

    - 优先实施方案 3（使用本地 ID）
    - 添加防御性检查
    - 测试多网卡环境

3. **验证修复**:
    - 在多网卡环境下测试
    - 确认不再创建副本
    - 检查网络同步是否正常
