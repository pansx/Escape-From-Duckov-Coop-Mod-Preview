# 进图血量减半问题分析

## 问题描述

设备1(客户端)在进图后,血量只有一半左右。

## 日志分析

### 主机端日志 (logs_host.txt)

所有的HP_REPORT都显示相同的模式:
```
[HP_REPORT] max=131.0, cur=65.0, force=True
```

**关键发现**:
- 最大血量: 131.0
- 当前血量: 65.0
- **当前血量正好是最大血量的一半** (65.0 ≈ 131.0 / 2)

### 客户端日志 (logs_client.txt)

主机接收到的血量报告:
```json
{
  "event": "Server_ReceiveHealthReport",
  "playerId": "49.75.39.85:33732",
  "maxHealth": 131.0,
  "currentHealth": 65.0,
  "hasRemoteCharacter": true,
  "time": 141.794861
}
```

**关键发现**:
- 主机收到的血量数据与客户端发送的一致
- 说明**问题不在网络传输,而在客户端读取血量时就已经是一半了**

## 问题定位

### 1. 血量上报流程

客户端血量上报的代码位于 `HealthM.cs`:

```csharp
public void Client_SendSelfHealth(Health h, bool force)
{
    // ...
    float max = 0f, cur = 0f;
    try { max = h.MaxHealth; } catch { }
    try { cur = h.CurrentHealth; } catch { }
    
    // 发送血量
    w.Put((byte)Op.PLAYER_HEALTH_REPORT);
    w.Put(max);
    w.Put(cur);
    connectedPeer.Send(w, DeliveryMethod.ReliableOrdered);
}
```

**问题**: 客户端在读取 `h.CurrentHealth` 时,值就已经是一半了。

### 2. 可能的原因

#### 原因1: Health组件初始化问题

Health组件可能在场景加载时被重置,导致血量变为默认值的一半。

需要检查:
- Health组件的 `autoInit` 设置
- `defaultMaxHealth` 字段的值
- `_currentHealth` 字段的初始化

#### 原因2: 场景切换时血量未保存

客户端在切换场景时,可能没有正确保存血量状态,导致进图后血量被重置。

需要检查:
- 场景切换前是否保存了血量
- 场景加载后是否恢复了血量

#### 原因3: 血量同步时机问题

客户端可能在Health组件完全初始化之前就上报了血量,导致读取到错误的值。

需要检查:
- `Client_ReportSelfHealth_IfReadyOnce()` 的调用时机
- Health组件的初始化顺序

## 下一步调查

### 1. 添加更详细的日志 ⭐

需要在 `HealthM.cs` 的 `Client_SendSelfHealth()` 方法中添加更详细的日志:

```csharp
public void Client_SendSelfHealth(Health h, bool force)
{
    // ... 现有代码 ...
    
    // 🔍 添加详细的Health状态日志
    var debugData = new Dictionary<string, object>
    {
        ["event"] = "Client_SendSelfHealth_Debug",
        ["maxHealth"] = max,
        ["currentHealth"] = cur,
        ["force"] = force,
        ["time"] = Time.time
    };
    
    // 反射读取内部字段
    try
    {
        var defaultMax = HealthTool.FI_defaultMax?.GetValue(h);
        var lastMax = HealthTool.FI_lastMax?.GetValue(h);
        var _current = HealthTool.FI__current?.GetValue(h);
        var autoInit = h.autoInit;
        
        debugData["defaultMaxHealth"] = defaultMax;
        debugData["lastMaxHealth"] = lastMax;
        debugData["_currentHealth"] = _current;
        debugData["autoInit"] = autoInit;
    }
    catch (Exception e)
    {
        debugData["reflectionError"] = e.Message;
    }
    
    Debug.Log($"[HP_REPORT_DEBUG] {Newtonsoft.Json.JsonConvert.SerializeObject(debugData, Newtonsoft.Json.Formatting.Indented)}");
    
    // ... 继续现有代码 ...
}
```

### 2. 检查Health组件状态

在客户端上报血量时,记录:
- ✅ `h.MaxHealth`
- ✅ `h.CurrentHealth`
- ✅ `h.autoInit`
- ✅ `defaultMaxHealth` (反射读取)
- ✅ `_currentHealth` (反射读取)
- ✅ `lastMaxHealth` (反射读取)

### 3. 检查场景切换流程

查看场景投票和切换的代码,确认:
- 血量是否在切换前被保存
- 血量是否在切换后被恢复
- 是否有代码在场景加载时重置了血量

### 4. 检查Health组件的初始化逻辑

游戏原始的Health组件可能有以下行为:
- 在 `autoInit=true` 时,自动初始化血量
- 初始化时可能将 `CurrentHealth` 设置为 `MaxHealth` 的一半
- 或者在某些情况下,`CurrentHealth` 没有被正确初始化

需要反编译游戏的Health类,查看其初始化逻辑。

## 临时解决方案

### 方案1: 延迟血量上报

在Health组件完全初始化后再上报血量:

```csharp
// 在 Client_ReportSelfHealth_IfReadyOnce() 中添加延迟
yield return new WaitForSeconds(1.0f); // 等待1秒
// 然后再上报血量
```

### 方案2: 强制恢复血量

主机在接收到血量报告后,检查是否为一半,如果是则恢复为满血:

```csharp
if (Mathf.Approximately(cur, max / 2f))
{
    Debug.LogWarning($"[HP_RECEIVE] 检测到血量减半,恢复为满血: {max}");
    cur = max;
}
```

### 方案3: 在场景加载后强制同步血量

在场景加载完成后,主机强制向所有客户端发送权威血量:

```csharp
// 在场景加载完成后
foreach (var kv in remoteCharacters)
{
    var peer = kv.Key;
    var go = kv.Value;
    var h = go.GetComponentInChildren<Health>(true);
    if (h)
    {
        Server_ForceAuthSelf(h); // 强制发送权威血量
    }
}
```

## 已添加的调试代码

### 位置: `EscapeFromDuckovCoopMod/Main/Health/HealthM.cs`

在 `Client_SendSelfHealth()` 方法中添加了详细的调试日志:

```csharp
// 🔍 详细调试：反射读取Health内部状态
try
{
    var debugData = new Dictionary<string, object>
    {
        ["event"] = "Client_SendSelfHealth_Debug",
        ["maxHealth"] = max,
        ["currentHealth"] = cur,
        ["force"] = force,
        ["time"] = Time.time
    };
    
    try
    {
        var defaultMax = HealthTool.FI_defaultMax?.GetValue(h);
        var lastMax = HealthTool.FI_lastMax?.GetValue(h);
        var _current = HealthTool.FI__current?.GetValue(h);
        
        debugData["defaultMaxHealth"] = defaultMax;
        debugData["lastMaxHealth"] = lastMax;
        debugData["_currentHealth"] = _current;
        debugData["autoInit"] = h.autoInit;
        debugData["gameObjectName"] = h.gameObject?.name ?? "null";
        debugData["gameObjectActive"] = h.gameObject?.activeSelf ?? false;
    }
    catch (Exception e)
    {
        debugData["reflectionError"] = e.Message;
    }
    
    Debug.Log($"[HP_REPORT_DEBUG] {Newtonsoft.Json.JsonConvert.SerializeObject(debugData, Newtonsoft.Json.Formatting.Indented)}");
}
catch
{
    // 静默失败，避免影响正常流程
}
```

### 编译结果

✅ **编译成功**

```
dotnet build EscapeFromDuckovCoopMod.sln --configuration Release
还原完成(0.3)
EscapeFromDuckovCoopMod 成功，出现 19 警告 (1.1 秒)
→ EscapeFromDuckovCoopMod\bin\Release\netstandard2.1\EscapeFromDuckovCoopMod.dll
```

### 测试步骤

1. **启动游戏并复现问题**
   - 主机启动游戏并进入场景
   - 客户端连接到主机
   - 客户端进图

2. **获取日志**
   ```bash
   .\get-logs.bat
   ```

3. **查找调试日志**
   在日志中搜索 `[HP_REPORT_DEBUG]`，查看:
   - `defaultMaxHealth` - Health组件的默认最大血量
   - `lastMaxHealth` - 上次的最大血量
   - `_currentHealth` - 内部的当前血量字段
   - `autoInit` - 是否自动初始化
   - `gameObjectName` - GameObject名称
   - `gameObjectActive` - GameObject是否激活

4. **分析结果**
   - 如果 `_currentHealth` 也是一半，说明问题在Health组件内部
   - 如果 `defaultMaxHealth` 是0或错误值，说明初始化有问题
   - 如果 `autoInit` 是true，可能是自动初始化导致的问题

## 相关文件

- `EscapeFromDuckovCoopMod/Main/Health/HealthM.cs` - 血量管理（已添加调试日志）
- `EscapeFromDuckovCoopMod/Main/Health/HealthTool.cs` - 血量工具
- `EscapeFromDuckovCoopMod/Main/NetService.cs` - 网络服务
- `logs_host.txt` - 主机端日志
- `logs_client.txt` - 客户端日志
- `get-logs.bat` - 日志获取脚本
