# 投票成功强制传送功能说明

## 功能概述

在投票成功后，主机会通过 JSON 消息广播强制场景切换指令，确保所有客户端（包括投票状态未同步的）都能收到并执行传送。

## 实现原理

### 1. 投票成功检测

当所有玩家都准备完毕时，主机会检测到投票通过：

```csharp
// SceneVoteMessage.cs - Host_HandleReadyToggle()
bool allReady = _hostVoteState.playerList.items.All(p => p.ready);
if (allReady)
{
    Host_StartSceneLoad();
}
```

### 2. 广播强制场景切换

投票成功后，主机会：

1. **发送 JSON 消息**（新增）：通过 `Op.JSON` 广播 `forceSceneLoad` 消息
2. **发送二进制消息**（原有）：通过 `Op.SCENE_BEGIN_LOAD` 发送场景加载指令

```csharp
// SceneVoteMessage.cs - Host_StartSceneLoad()
Host_BroadcastForceSceneLoad(
    targetSceneId,
    curtainGuid,
    locationName,
    notifyEvac,
    saveToFile,
    useLocation
);
```

### 3. JSON 消息结构

```json
{
    "type": "forceSceneLoad",
    "targetSceneId": "场景ID",
    "curtainGuid": "过场GUID",
    "locationName": "位置名称",
    "notifyEvac": false,
    "saveToFile": true,
    "useLocation": false,
    "timestamp": "2025-11-09 16:46:00.123"
}
```

### 4. 客户端处理流程

客户端收到 `forceSceneLoad` 消息后会：

1. **停止投票 UI**：立即关闭投票界面
2. **清除投票状态**：重置所有投票相关的变量
3. **更新场景目标**：设置要传送的目标场景
4. **执行场景切换**：调用 `TryPerformSceneLoad_Local()` 开始传送

```csharp
// SceneVoteMessage.cs - Client_HandleForceSceneLoad()
if (sceneNet.sceneVoteActive)
{
    sceneNet.sceneVoteActive = false;
    sceneNet.sceneReady.Clear();
    sceneNet.localReady = false;
}

// 执行场景切换
TryPerformSceneLoad_Local(
    targetSceneId,
    curtainGuid,
    notifyEvac,
    saveToFile,
    useLocation,
    locationName
);
```

## 消息路由

JSON 消息通过 `JsonMessageRouter` 路由到对应的处理器：

```csharp
// JsonMessageRouter.cs
case "forceSceneLoad":
    SceneVoteMessage.Client_HandleForceSceneLoad(json);
    break;
```

## 优势

### 1. 双重保障

- **JSON 消息**：可靠传输，包含完整的场景信息
- **二进制消息**：原有机制，保持兼容性

### 2. 强制同步

- 无论客户端投票状态如何，都会收到并执行传送
- 解决投票状态未同步导致的传送失败问题

### 3. 清晰的日志

```
[SceneVote] 主机广播强制场景切换 JSON:
{
  "type": "forceSceneLoad",
  "targetSceneId": "Hideout",
  ...
}

[SceneVote] 客户端收到强制场景切换 JSON:
...

[SceneVote] 🚀 强制场景切换: Hideout
[SceneVote] 停止投票 UI，准备传送
[SceneVote] ✅ 已触发场景加载: Hideout
```

## 修改的文件

### 1. `EscapeFromDuckovCoopMod/Net/SceneVoteMessage.cs`

- 新增 `ForceSceneLoadData` 数据结构
- 新增 `Host_BroadcastForceSceneLoad()` 方法（主机广播）
- 新增 `Client_HandleForceSceneLoad()` 方法（客户端处理）
- 修改 `Host_StartSceneLoad()` 方法（添加 JSON 广播）

### 2. `EscapeFromDuckovCoopMod/Net/JsonMessageRouter.cs`

- 添加 `forceSceneLoad` 消息类型的路由

## 测试方法

1. **启动主机和客户端**
2. **主机发起投票**
3. **所有玩家准备**
4. **观察日志**：
   - 主机应该输出 "主机广播强制场景切换 JSON"
   - 客户端应该输出 "客户端收到强制场景切换 JSON"
   - 客户端应该输出 "停止投票 UI，准备传送"
5. **验证传送**：所有玩家应该成功传送到目标场景

## 故障排查

### 客户端未收到消息

**检查**：
- 网络连接是否正常
- `Op.JSON` 消息是否被正确处理
- `JsonMessageRouter` 是否正确路由

### 客户端收到消息但未传送

**检查**：
- `SceneNet.Instance` 是否存在
- `TryPerformSceneLoad_Local` 方法是否被正确调用
- 日志中是否有错误信息

### 投票 UI 未关闭

**检查**：
- `sceneNet.sceneVoteActive` 是否被设置为 `false`
- UI 是否正确监听 `sceneVoteActive` 状态

## 版本信息

- **添加日期**：2025-11-09
- **版本**：EscapeFromDuckovCoopMod-20251109-1646
- **作者**：Kiro AI Assistant

---

*最后更新: 2025-11-09 16:46*
