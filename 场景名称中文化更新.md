# 场景名称中文化更新

## 更新时间
2025-11-09

## 更新内容

### 1. 新增场景名称映射工具类

创建了 `EscapeFromDuckovCoopMod/Utils/SceneNameMapper.cs`，提供场景ID到中文名称的映射功能。

#### 支持的场景列表

| 场景ID | 中文名称 |
|--------|---------|
| Base | 基地 |
| Custom | 海关 |
| Custom_01 | 海关1 |
| Custom_02 | 海关2 |
| Custom_03 | 海关3 |
| Custom_04 | 海关4 |
| Custom_05 | 海关5 |
| Factory | 工厂 |
| Factory_01 | 工厂1 |
| Factory_02 | 工厂2 |
| Factory_03 | 工厂3 |
| Factory_04 | 工厂4 |
| Village | 村庄 |
| Village_01 | 村庄1 |
| Any | 任意地图 |

#### 主要方法

```csharp
// 获取场景的中文名称
string chineseName = SceneNameMapper.GetChineseName("Factory");
// 返回: "工厂"

// 获取场景的显示名称（智能判断是否需要替换）
string displayName = SceneNameMapper.GetDisplayName("Custom_01");
// 返回: "海关1"
```

### 2. 更新UI显示

#### MModUI.cs（现代化UI）
- 投票面板中的场景名称现在显示为中文
- 位置：`UpdateVotePanel()` 方法

#### ModUI.cs（传统UI）
- 投票窗口中的场景名称现在显示为中文
- 位置：`OnGUI()` 方法中的投票面板部分

#### WaitingSynchronizationUI.cs（场景加载同步界面）⭐
- **加载场景时显示的地图名称现在显示为中文**
- 位置：`GetMapDisplayName()` 和 `ExtractSceneId()` 方法
- 支持从Unity场景名称（如 "Base_Scenev2", "Level_Factory_Main"）提取场景ID并转换为中文

### 3. 数据来源

场景名称映射来自游戏官方本地化文件：
```
C:\SteamLibrary\steamapps\common\Escape from Duckov\Duckov_Data\StreamingAssets\Localization\ChineseSimplified.csv
```

从CSV文件中提取的场景翻译：
```csv
Scene_Base,基地
Scene_Custom,海关
Scene_Factory,工厂
Scene_Village,村庄
...
```

## 技术实现

### 智能名称选择逻辑

`SceneNameMapper.GetDisplayName()` 方法实现了智能选择逻辑：

1. 首先尝试从游戏的 `SceneInfoCollection` 获取场景信息
2. 如果获取到的 `DisplayName` 是英文或与场景ID相同，则使用我们的中文映射
3. 如果游戏已经提供了中文名称，则直接使用游戏的名称
4. 如果都没有，返回场景ID作为后备

### 英文名称判断

通过检查字符串是否只包含ASCII字符来判断是否为英文名称：
```csharp
private static bool IsEnglishName(string name)
{
    foreach (char c in name)
    {
        if (c > 127) return false; // 包含非ASCII字符（如中文）
    }
    return true;
}
```

## 使用示例

### 在UI代码中使用

```csharp
// 旧代码
var sceneName = SceneInfoCollection.GetSceneInfo(sceneId).DisplayName;

// 新代码
var sceneName = Utils.SceneNameMapper.GetDisplayName(sceneId);
```

### 获取所有场景信息

```csharp
// 获取所有场景ID
var sceneIds = SceneNameMapper.GetAllSceneIds();

// 获取所有中文名称
var chineseNames = SceneNameMapper.GetAllChineseNames();
```

## 测试验证

### 编译结果
✅ 编译成功，无错误

### 部署结果
✅ 已通过API上传部署到游戏目录
- 版本：EscapeFromDuckovCoopMod-20251109-1612

### 预期效果

启动游戏后，应该看到：

#### 场景投票界面
- ❌ 旧版：显示 "Factory"、"Custom_01" 等英文ID
- ✅ 新版：显示 "工厂"、"海关1" 等中文名称

#### 场景加载界面（重点）⭐
- ❌ 旧版：显示 "正在加载场景...地图: Base_Scenev2"
- ✅ 新版：显示 "正在加载场景...地图: 基地"

## 后续扩展

如果游戏添加了新的场景，只需在 `SceneNameMapper.cs` 的 `SceneNames` 字典中添加新的映射即可：

```csharp
private static readonly Dictionary<string, string> SceneNames = new()
{
    // ... 现有映射 ...
    
    // 添加新场景
    { "NewScene", "新场景" },
    { "NewScene_01", "新场景1" },
};
```

## 相关文件

- `EscapeFromDuckovCoopMod/Utils/SceneNameMapper.cs` - 场景名称映射工具类（新增）
- `EscapeFromDuckovCoopMod/Main/UI/MModUI.cs` - 现代化UI（已更新）
- `EscapeFromDuckovCoopMod/Main/UI/ModUI.cs` - 传统UI（已更新）
- `EscapeFromDuckovCoopMod/Main/UI/WaitingSynchronizationUI.cs` - 场景加载同步界面（已更新）⭐
- `Localization/zh-CN.json` - Mod的本地化文件（未修改，使用代码映射）

## 注意事项

1. **不修改游戏原始文件**：我们通过代码映射实现中文化，不修改游戏的本地化文件
2. **向后兼容**：如果场景ID不在映射表中，会返回原始ID，不会导致错误
3. **优先使用游戏数据**：如果游戏已经提供了中文名称，优先使用游戏的翻译
4. **易于维护**：所有映射集中在一个文件中，便于更新和维护
