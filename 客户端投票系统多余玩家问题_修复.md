# 客户端投票系统多余玩家问题 - 修复方案

## 问题分析

### 现象
- 2人联机（1主机 + 1客户端）
- 投票系统显示 **3个参与者**
- 客户端看到多余的玩家

### 根本原因

在 `SceneVoteMessage.cs` 的 `Client_HandleVoteState` 方法中，客户端构建参与者列表时：

```csharp
// 添加自己
var myId = service.localPlayerStatus?.EndPoint ?? "";
if (!string.IsNullOrEmpty(myId) && myId != hostId)
{
    sceneNet.sceneParticipantIds.Add(myId);
    sceneNet.sceneReady[myId] = false;
}

// 添加其他客户端
if (service.clientPlayerStatuses != null)
{
    foreach (var kv in service.clientPlayerStatuses)
    {
        var pid = kv.Key;
        if (!string.IsNullOrEmpty(pid) && pid != myId && pid != hostId)
        {
            sceneNet.sceneParticipantIds.Add(pid);
            sceneNet.sceneReady[pid] = false;
        }
    }
}
```

**问题**：
1. `clientPlayerStatuses` 包含了客户端自己的状态（主机广播的）
2. 由于网络地址不一致：
   - `myId` = `localPlayerStatus.EndPoint` = 客户端本地ID
   - `clientPlayerStatuses` 中的 key = 主机看到的客户端网络地址
3. 当 `myId != pid` 时，客户端会把自己也添加到参与者列表中

### 示例

```
myId = "Client:d9805d89"  (本地ID)
clientPlayerStatuses = {
    "Host:9050": {...},
    "192.168.137.30:62825": {...}  ← 这是客户端自己！
}

检查: "192.168.137.30:62825" != "Client:d9805d89" → true
结果: 把自己添加到参与者列表 ❌
```

## 修复方案

### 方案1：使用 IsSelfId() 检查（推荐）⭐

修改 `SceneVoteMessage.cs` 中的 `Client_HandleVoteState` 方法：

```csharp
// 添加其他客户端
if (service.clientPlayerStatuses != null)
{
    foreach (var kv in service.clientPlayerStatuses)
    {
        var pid = kv.Key;
        
        // 🔧 使用 IsSelfId() 检查，而不是简单的字符串比较
        if (!string.IsNullOrEmpty(pid) && 
            pid != hostId && 
            !service.IsSelfId(pid))  // ← 关键修改
        {
            sceneNet.sceneParticipantIds.Add(pid);
            sceneNet.sceneReady[pid] = false;
        }
    }
}
```

**优点**：
- 利用现有的 `IsSelfId()` 方法，该方法已经处理了多种ID格式
- 简单、直接、可靠
- 与 SetId 功能配合良好

### 方案2：不从 clientPlayerStatuses 构建列表

客户端不应该自己构建参与者列表，而应该完全依赖主机广播的 `readyStates`：

```csharp
// 从主机广播的 readyStates 构建参与者列表
sceneNet.sceneParticipantIds.Clear();
sceneNet.sceneReady.Clear();

if (!string.IsNullOrEmpty(data.readyStates))
{
    foreach (var pair in data.readyStates.Split(','))
    {
        var parts = pair.Split(':');
        if (parts.Length == 2)
        {
            var pid = parts[0];
            var isReady = parts[1] == "true";
            
            // 添加到参与者列表
            if (!sceneNet.sceneParticipantIds.Contains(pid))
            {
                sceneNet.sceneParticipantIds.Add(pid);
            }
            sceneNet.sceneReady[pid] = isReady;
            
            // 检查是否是自己
            if (service.IsSelfId(pid))
            {
                sceneNet.localReady = isReady;
            }
        }
    }
}
```

**优点**：
- 完全依赖主机的权威数据
- 避免客户端自己构建列表时的错误
- 更简洁

**缺点**：
- 如果主机的 `readyStates` 有问题，客户端也会有问题

## 推荐实施

### 立即修复（方案1）

修改 `EscapeFromDuckovCoopMod/Net/SceneVoteMessage.cs`：

```csharp
// 添加其他客户端
if (service.clientPlayerStatuses != null)
{
    foreach (var kv in service.clientPlayerStatuses)
    {
        var pid = kv.Key;
        if (!string.IsNullOrEmpty(pid) && 
            pid != hostId && 
            !service.IsSelfId(pid))  // ← 使用 IsSelfId() 检查
        {
            sceneNet.sceneParticipantIds.Add(pid);
            sceneNet.sceneReady[pid] = false;
        }
    }
}
```

### 验证

修复后，客户端应该：
- 投票参与者数量 = 2（主机 + 客户端）
- 不会把自己添加两次
- 投票系统正常工作

## 相关问题

这个问题与"客户端多余玩家问题"是同一个根源：
- 网络地址不一致
- 客户端无法正确识别自己

SetId 功能已经解决了远程角色创建的问题，但投票系统还需要相应的修复。

## 测试方法

1. 编译项目
2. 2人联机测试
3. 主机发起投票
4. 检查客户端投票面板显示的参与者数量
5. 应该只显示2个玩家（主机 + 客户端）

