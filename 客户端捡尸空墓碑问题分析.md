# 客户端捡尸空墓碑问题分析

## 🐛 问题描述

**现象：**

1. 客户端玩家死亡后，主机能看到墓碑并正常获取道具
2. 客户端因死亡无法验证是否可获取
3. 客户端撤退后再回到死亡地点捡尸时，发现墓碑是空的

---

## 🔍 根本原因分析

### 核心问题：战利品箱索引在场景切换时丢失

#### **问题 1：客户端索引未持久化**

```csharp
// LootManager.cs
public readonly Dictionary<int, Inventory> _cliLootByUid = new();
```

**问题：**

-   `_cliLootByUid` 是普通字典，**不会跨场景保存**
-   客户端撤退（场景切换）时，这个字典会被清空
-   再次进入场景时，之前的 `lootUid → Inventory` 映射关系丢失

**流程：**

```
客户端死亡
    ↓
生成墓碑，注册 _cliLootByUid[lootUid] = inv
    ↓
客户端撤退（场景切换）
    ↓
❌ _cliLootByUid 被清空（字典是场景内对象）
    ↓
客户端重新进入场景
    ↓
墓碑物理对象还在（Unity场景持久化）
    ↓
但 _cliLootByUid[lootUid] 已经不存在
    ↓
客户端打开墓碑时无法通过 lootUid 找到 Inventory
    ↓
❌ 显示为空墓碑
```

---

#### **问题 2：主机索引持续有效**

```csharp
// LootManager.cs
public readonly Dictionary<int, Inventory> _srvLootByUid = new();
```

**为什么主机正常：**

-   主机的 `_srvLootByUid` 在整个游戏会话期间持续存在
-   主机不会"撤退"，场景对象一直在内存中
-   即使客户端离开又回来，主机的索引仍然有效

**对比：**

```
主机侧：
_srvLootByUid[123] = inv  ✅ 一直有效
    ↓
客户端撤退、重进
    ↓
_srvLootByUid[123] = inv  ✅ 仍然有效
    ↓
主机可以正常响应客户端的请求

客户端侧：
_cliLootByUid[123] = inv  ✅ 初次有效
    ↓
场景切换（撤退）
    ↓
_cliLootByUid 清空  ❌ 索引丢失
    ↓
重新进入场景
    ↓
_cliLootByUid[123] 不存在  ❌ 无法找到墓碑
```

---

#### **问题 3：战利品箱物理对象与索引分离**

```csharp
// DeadLootBox.cs - SpawnDeadLootboxAt()
// 🆔 注册稳定ID索引
if (lootUid >= 0)
    LootManager.Instance._cliLootByUid[lootUid] = inv;
```

**关键点：**

1. **物理对象：** Unity 的 `InteractableLootbox` GameObject

    - 可能被场景系统持久化（取决于游戏设计）
    - 或者在场景重新加载时由原版游戏重新生成

2. **索引映射：** `_cliLootByUid` 字典

    - 存储在 `LootManager` 组件中
    - **不会自动跨场景保存**

3. **分离问题：**
    - 物理墓碑还在场景中
    - 但客户端的 `lootUid → Inventory` 映射已丢失
    - 打开墓碑时无法通过 `lootUid` 找到对应的 `Inventory`

---

## 📊 详细流程对比

### 正常流程（首次死亡，未撤退）

```
T0: 客户端死亡
    ↓
T1: 上报 PLAYER_DEAD_TREE 给主机
    ↓
T2: 主机生成墓碑，lootUid=123
    _srvLootByUid[123] = inv_host
    ↓
T3: 主机广播 DEAD_LOOT_SPAWN(lootUid=123)
    ↓
T4: 客户端接收，生成墓碑
    _cliLootByUid[123] = inv_client
    ↓
T5: 客户端请求内容 LOOT_REQ_OPEN(lootUid=123)
    ↓
T6: 主机查找 _srvLootByUid[123] ✅ 找到
    返回 LOOT_STATE
    ↓
T7: 客户端应用快照，墓碑显示正常 ✅
```

---

### 异常流程（撤退后重新捡尸）

```
T0: 客户端死亡，生成墓碑
    _cliLootByUid[123] = inv_client ✅
    ↓
T1: 客户端撤退（场景切换）
    ↓
T2: ❌ LootManager 组件被销毁或重置
    _cliLootByUid 清空
    ↓
T3: 客户端重新进入场景
    ↓
T4: 场景加载，墓碑物理对象存在
    但 _cliLootByUid[123] 不存在 ❌
    ↓
T5: 客户端打开墓碑
    ↓
T6: 尝试通过 lootUid=123 查找
    _cliLootByUid[123] → null ❌
    ↓
T7: 回落到 posKey/iid 查找
    可能也失败（位置浮点误差）❌
    ↓
T8: 无法找到 Inventory
    ↓
T9: 显示为空墓碑 ❌
```

---

## 🔍 代码证据

### 证据 1：索引查找逻辑

```csharp
// LootManager.cs - ResolveLootInv()
public Inventory ResolveLootInv(int scene, int posKey, int iid, int lootUid)
{
    Inventory inv = null;

    // 🔑 先用稳定ID（优先级最高）
    if (lootUid >= 0)
    {
        if (IsServer)
        {
            if (_srvLootByUid != null &&
                _srvLootByUid.TryGetValue(lootUid, out inv) && inv)
                return inv;  // ✅ 主机：一直有效
        }
        else
        {
            if (_cliLootByUid != null &&
                _cliLootByUid.TryGetValue(lootUid, out inv) && inv)
                return inv;  // ❌ 客户端：场景切换后失效
        }
    }

    // 回落到 scene/posKey/iid 三元定位
    if (TryResolveLootById(scene, posKey, iid, out inv) && inv)
        return inv;

    return null;  // ❌ 找不到，返回空
}
```

**问题：**

-   客户端撤退后，`_cliLootByUid[lootUid]` 不存在
-   回落到 `posKey/iid` 查找也可能失败
-   最终返回 `null`，导致空墓碑

---

### 证据 2：客户端打开战利品箱的请求

```csharp
// LootNet.cs - Client_RequestLootState()
public void Client_RequestLootState(Inventory inv)
{
    if (!networkStarted || IsServer || connectedPeer == null) return;

    writer.Reset();
    writer.Put((byte)Op.LOOT_REQ_OPEN);

    // 🔑 PutLootId 会尝试从 _cliLootByUid 反查 lootUid
    LootManager.Instance.PutLootId(writer, inv);

    // ... 发送请求
}
```

```csharp
// LootManager.cs - PutLootId()
public void PutLootId(NetDataWriter w, Inventory inv)
{
    // ... 其他标识 ...

    // 🔑 稳定 ID（仅死亡箱子会命中）
    var lootUid = -1;
    if (!IsServer)
    {
        // 客户端：从 _cliLootByUid 反查
        foreach (var kv in _cliLootByUid)
            if (kv.Value == inv)
            {
                lootUid = kv.Key;
                break;
            }
    }

    w.Put(lootUid);  // ❌ 场景切换后找不到，写入 -1
}
```

**问题：**

-   场景切换后，`_cliLootByUid` 为空
-   无法反查到 `lootUid`
-   发送给主机的请求中 `lootUid = -1`
-   主机也无法通过 `lootUid` 快速定位

---

### 证据 3：主机处理请求

```csharp
// LootManager.cs - Server_HandleLootOpenRequest()
public void Server_HandleLootOpenRequest(NetPeer peer, NetPacketReader r)
{
    // ... 读取标识 ...
    var lootUid = -1;
    if (r.AvailableBytes >= 4) lootUid = r.GetInt();

    // 🔑 先用稳定ID命中
    Inventory inv = null;
    if (lootUid >= 0)  // ❌ 客户端发来的是 -1，跳过
        _srvLootByUid.TryGetValue(lootUid, out inv);

    // ❌ 回落到"激进解析"：三元标识 + 附近3米扫描
    if (inv == null &&
        !Server_TryResolveLootAggressive(scene, posKey, iid, posHint, out inv))
    {
        // ❌ 找不到，发送拒绝
        COOPManager.LootNet.Server_SendLootDeny(peer, "no_inv");
        return;
    }

    // ...
}
```

**问题：**

-   客户端发来的 `lootUid = -1`（因为索引丢失）
-   主机无法通过 `lootUid` 快速定位
-   回落到位置查找，可能因为浮点误差失败
-   最终发送 `LOOT_DENY`，客户端看到空墓碑

---

## 🎯 为什么主机正常

### 主机的优势

1. **索引持久化：**

    ```csharp
    // 主机不会"撤退"，_srvLootByUid 一直有效
    _srvLootByUid[123] = inv;  // ✅ 永久有效
    ```

2. **场景不切换：**

    - 主机玩家死亡后，场景对象仍在内存中
    - 不会触发场景卸载/重新加载
    - 所有索引和引用保持有效

3. **直接访问：**
    - 主机可以直接访问本地的 `Inventory` 对象
    - 不需要通过网络请求
    - 不依赖 `lootUid` 索引

---

## 💡 解决方案

### 方案 1：持久化客户端索引（推荐）

**思路：** 在场景切换时保存 `_cliLootByUid`，重新进入时恢复

```csharp
// LootManager.cs
public class LootManager : MonoBehaviour
{
    // 跨场景持久化的索引
    private static Dictionary<int, (int scene, Vector3 pos, Quaternion rot)>
        _persistentLootUidMap = new();

    public readonly Dictionary<int, Inventory> _cliLootByUid = new();

    // 场景切换前保存
    private void OnDestroy()
    {
        if (IsServer) return;

        // 保存所有战利品箱的位置信息
        foreach (var kv in _cliLootByUid)
        {
            var lootUid = kv.Key;
            var inv = kv.Value;

            if (LootManager.Instance.TryGetLootboxWorldPos(inv, out var pos))
            {
                var scene = SceneManager.GetActiveScene().buildIndex;
                _persistentLootUidMap[lootUid] = (scene, pos, Quaternion.identity);
            }
        }
    }

    // 场景加载后恢复
    private void Start()
    {
        if (IsServer) return;

        StartCoroutine(RestoreLootIndexes());
    }

    private IEnumerator RestoreLootIndexes()
    {
        yield return new WaitForSeconds(1f); // 等待场景完全加载

        var currentScene = SceneManager.GetActiveScene().buildIndex;
        var boxes = FindObjectsOfType<InteractableLootbox>();

        foreach (var kv in _persistentLootUidMap)
        {
            var lootUid = kv.Key;
            var (scene, pos, rot) = kv.Value;

            if (scene != currentScene) continue;

            // 在附近查找墓碑
            foreach (var box in boxes)
            {
                if (Vector3.Distance(box.transform.position, pos) < 1f)
                {
                    var inv = box.Inventory;
                    if (inv != null)
                    {
                        _cliLootByUid[lootUid] = inv;
                        Debug.Log($"[Loot] 恢复索引: lootUid={lootUid}");
                    }
                    break;
                }
            }
        }
    }
}
```

---

### 方案 2：主机重新广播战利品箱信息

**思路：** 客户端重新进入场景时，主机重新发送所有战利品箱的生成消息

```csharp
// SceneNet.cs
public void Client_OnSceneReady()
{
    // 客户端场景加载完成，请求主机同步战利品箱
    writer.Reset();
    writer.Put((byte)Op.LOOT_RESYNC_REQUEST);
    connectedPeer.Send(writer, DeliveryMethod.ReliableOrdered);
}

// HostHandle.cs
public void Server_OnLootResyncRequest(NetPeer peer)
{
    var peerScene = SceneM._srvPeerScene.TryGetValue(peer, out var s) ? s : null;
    if (string.IsNullOrEmpty(peerScene)) return;

    // 重新广播该场景中的所有战利品箱
    foreach (var kv in LootManager.Instance._srvLootByUid)
    {
        var lootUid = kv.Key;
        var inv = kv.Value;

        // 找到对应的 InteractableLootbox
        var boxes = FindObjectsOfType<InteractableLootbox>();
        foreach (var box in boxes)
        {
            if (box.Inventory == inv)
            {
                // 重新发送 DEAD_LOOT_SPAWN
                writer.Reset();
                writer.Put((byte)Op.DEAD_LOOT_SPAWN);
                writer.Put(SceneManager.GetActiveScene().buildIndex);
                writer.Put(0); // aiId（玩家墓碑）
                writer.Put(lootUid);
                writer.PutV3cm(box.transform.position);
                writer.PutQuaternion(box.transform.rotation);

                peer.Send(writer, DeliveryMethod.ReliableOrdered);
                break;
            }
        }
    }
}
```

---

### 方案 3：增强位置查找的容错性

**思路：** 即使 `lootUid` 丢失，也能通过位置准确找到墓碑

```csharp
// LootManager.cs
public bool Server_TryResolveLootAggressive(
    int scene, int posKey, int iid, Vector3 posHint, out Inventory inv)
{
    inv = null;

    // 1) 原有的两条路径
    if (TryResolveLootById(scene, posKey, iid, out inv)) return true;

    // 2) 增强：扩大搜索半径，从 3m 增加到 5m
    if (TryResolveLootByHint(posHint, out inv, radius: 5f)) return true;

    // 3) 新增：按类型过滤（只查找墓碑类型的战利品箱）
    var best = 25f; // 5m^2
    InteractableLootbox bestBox = null;

    foreach (var b in FindObjectsOfType<InteractableLootbox>())
    {
        if (!b || !b.gameObject.activeInHierarchy) continue;
        if (scene >= 0 && b.gameObject.scene.buildIndex != scene) continue;

        // 🔑 只查找墓碑类型（通过名称或组件判断）
        if (!b.name.Contains("Tomb") && !b.name.Contains("Dead")) continue;

        var d2 = (b.transform.position - posHint).sqrMagnitude;
        if (d2 < best)
        {
            best = d2;
            bestBox = b;
        }
    }

    if (!bestBox) return false;

    inv = bestBox.Inventory;
    if (!inv) return false;

    // 保险：重新注册索引
    var dict = InteractableLootbox.Inventories;
    if (dict != null)
    {
        var key = ComputeLootKey(bestBox.transform);
        dict[key] = inv;
    }

    return true;
}
```

---

### 方案 4：在墓碑上添加持久化标记

**思路：** 给墓碑 GameObject 添加一个组件，存储 `lootUid`

```csharp
// 新增组件
public class LootUidTag : MonoBehaviour
{
    public int lootUid = -1;
}

// DeadLootBox.cs - SpawnDeadLootboxAt()
public void SpawnDeadLootboxAt(int aiId, int lootUid, Vector3 pos, Quaternion rot)
{
    // ... 现有代码 ...

    var go = Instantiate(prefab, pos, rot);
    var box = go ? go.GetComponent<InteractableLootbox>() : null;

    // 🔑 添加持久化标记
    var tag = box.gameObject.AddComponent<LootUidTag>();
    tag.lootUid = lootUid;

    // ... 现有代码 ...
}

// LootManager.cs - 恢复索引
private IEnumerator RestoreLootIndexes()
{
    yield return new WaitForSeconds(1f);

    var boxes = FindObjectsOfType<InteractableLootbox>();
    foreach (var box in boxes)
    {
        var tag = box.GetComponent<LootUidTag>();
        if (tag != null && tag.lootUid >= 0)
        {
            var inv = box.Inventory;
            if (inv != null)
            {
                _cliLootByUid[tag.lootUid] = inv;
                Debug.Log($"[Loot] 从标记恢复索引: lootUid={tag.lootUid}");
            }
        }
    }
}
```

---

## 📌 推荐方案

**综合方案：方案 1 + 方案 4**

1. **持久化索引映射：** 使用静态字典保存 `lootUid → 位置` 的映射
2. **GameObject 标记：** 在墓碑上添加 `LootUidTag` 组件
3. **场景重新加载时恢复：** 通过标记重建 `_cliLootByUid` 索引

**优点：**

-   不需要修改网络协议
-   不增加网络流量
-   容错性强（两种恢复机制）
-   对现有代码改动最小

---

## 🔍 验证方法

### 测试步骤

1. **客户端死亡：**

    ```
    - 客户端玩家死亡
    - 检查 _cliLootByUid[lootUid] 是否注册
    - 主机能否正常获取道具
    ```

2. **撤退：**

    ```
    - 客户端撤退（场景切换）
    - 检查 _persistentLootUidMap 是否保存了位置
    - 检查墓碑上的 LootUidTag 组件
    ```

3. **重新进入：**

    ```
    - 客户端重新进入场景
    - 检查 RestoreLootIndexes() 是否执行
    - 检查 _cliLootByUid[lootUid] 是否恢复
    ```

4. **捡尸：**
    ```
    - 客户端打开墓碑
    - 检查 ResolveLootInv() 是否找到 Inventory
    - 检查是否能正常获取道具
    ```

### 调试日志

```csharp
// 死亡时
Debug.Log($"[Loot] 客户端死亡，注册索引: lootUid={lootUid}");

// 撤退时
Debug.Log($"[Loot] 场景切换，保存索引: count={_persistentLootUidMap.Count}");

// 重新进入时
Debug.Log($"[Loot] 恢复索引: lootUid={lootUid}, found={_cliLootByUid.ContainsKey(lootUid)}");

// 打开墓碑时
Debug.Log($"[Loot] 查找墓碑: lootUid={lootUid}, inv={inv != null}");
```

---

## 📊 总结

### 问题根源

| 问题         | 原因                           | 影响                      |
| ------------ | ------------------------------ | ------------------------- |
| 索引丢失     | `_cliLootByUid` 不跨场景持久化 | 无法通过 lootUid 找到墓碑 |
| 位置查找失败 | 浮点误差 + 搜索半径不足        | 回落机制也失效            |
| 主机正常     | 主机不切换场景，索引一直有效   | 只有客户端受影响          |

### 解决思路

1. **持久化索引：** 使用静态字典或 DontDestroyOnLoad
2. **GameObject 标记：** 在墓碑上存储 lootUid
3. **场景加载时恢复：** 重建索引映射
4. **增强容错：** 扩大搜索半径，按类型过滤

### 实施优先级

1. **高优先级：** 方案 4（GameObject 标记）- 最简单，改动最小
2. **中优先级：** 方案 1（持久化索引）- 更健壮
3. **低优先级：** 方案 2（主机重新广播）- 增加网络开销
4. **辅助：** 方案 3（增强查找）- 作为兜底机制

通过实施这些方案，可以彻底解决客户端捡尸时墓碑为空的问题。
