# 进图血量减半问题 - 调试日志已添加

## 问题描述

设备1(客户端)在进图后,血量只有一半左右(max=131.0, cur=65.0)。

## 日志分析结果

### 关键发现

1. **客户端发送的血量本身就是一半**
   - 主机端日志: `[HP_REPORT] max=131.0, cur=65.0, force=True`
   - 客户端日志: `[HP_RECEIVE] {"maxHealth":131.0,"currentHealth":65.0,...}`

2. **问题不在网络传输**
   - 主机收到的数据与客户端发送的完全一致
   - 说明问题在客户端读取血量时就已经是一半了

3. **血量比例非常精确**
   - 65.0 正好是 131.0 的一半
   - 这很可能是Health组件初始化时的默认行为

## 已添加的调试代码

### 位置
`EscapeFromDuckovCoopMod/Main/Health/HealthM.cs` 的 `Client_SendSelfHealth()` 方法

### 功能
反射读取Health组件的内部状态,包括:
- `defaultMaxHealth` - 默认最大血量
- `lastMaxHealth` - 上次最大血量
- `_currentHealth` - 内部当前血量字段
- `autoInit` - 是否自动初始化
- `gameObjectName` - GameObject名称
- `gameObjectActive` - GameObject激活状态

### 日志标签
`[HP_REPORT_DEBUG]`

### 日志格式
```json
{
  "event": "Client_SendSelfHealth_Debug",
  "maxHealth": 131.0,
  "currentHealth": 65.0,
  "force": true,
  "time": 141.5,
  "defaultMaxHealth": 131,
  "lastMaxHealth": 131.0,
  "_currentHealth": 65.0,
  "autoInit": false,
  "gameObjectName": "Character(Clone)",
  "gameObjectActive": true
}
```

## 编译结果

✅ **编译成功**

```bash
dotnet build EscapeFromDuckovCoopMod.sln --configuration Release
还原完成(0.3)
EscapeFromDuckovCoopMod 成功，出现 19 警告 (1.1 秒)
→ EscapeFromDuckovCoopMod\bin\Release\netstandard2.1\EscapeFromDuckovCoopMod.dll
```

警告都是无关紧要的(nullable注释、未使用字段等),不影响功能。

## 下一步测试

### 1. 复现问题
- 主机启动游戏并进入场景
- 客户端连接到主机
- 客户端进图(此时应该会出现血量减半)

### 2. 获取日志
```bash
.\get-logs.bat
```

### 3. 分析调试日志

在日志中搜索 `[HP_REPORT_DEBUG]`,重点关注:

#### 场景A: _currentHealth也是一半
```json
{
  "maxHealth": 131.0,
  "currentHealth": 65.0,
  "_currentHealth": 65.0  // ← 内部字段也是一半
}
```
**结论**: 问题在Health组件内部,不是读取错误

#### 场景B: defaultMaxHealth是0或错误值
```json
{
  "maxHealth": 131.0,
  "currentHealth": 65.0,
  "defaultMaxHealth": 0  // ← 默认值错误
}
```
**结论**: Health组件初始化有问题

#### 场景C: autoInit是true
```json
{
  "maxHealth": 131.0,
  "currentHealth": 65.0,
  "autoInit": true  // ← 自动初始化开启
}
```
**结论**: 可能是自动初始化导致血量被重置

### 4. 根据结果采取行动

#### 如果是场景A或B
需要在场景加载后强制恢复血量:
```csharp
// 在场景加载完成后
if (cur < max * 0.9f) // 如果血量明显偏低
{
    Debug.LogWarning($"[HP_FIX] 检测到血量异常,恢复为满血: {max}");
    cur = max;
    HealthM.Instance.ForceSetHealth(h, max, max, true);
}
```

#### 如果是场景C
需要在创建远程角色时禁用autoInit:
```csharp
// 在创建远程角色后
var h = go.GetComponentInChildren<Health>(true);
if (h)
{
    h.autoInit = false; // 禁用自动初始化
}
```

## 可能的根本原因

### 原因1: Health组件的默认行为
游戏原始的Health组件可能在某些情况下会将CurrentHealth设置为MaxHealth的一半,例如:
- 在场景加载时
- 在GameObject被激活时
- 在autoInit触发时

### 原因2: 场景切换时血量未保存
客户端在切换场景时,可能没有正确保存血量状态,导致进图后血量被重置为默认值(一半)。

### 原因3: 血量同步时机问题
客户端可能在Health组件完全初始化之前就上报了血量,导致读取到错误的值。

## 临时解决方案

如果调试日志确认了问题,可以采用以下临时方案:

### 方案1: 主机端修复
在主机接收到血量报告时,检查是否为一半,如果是则恢复为满血:
```csharp
if (Mathf.Approximately(cur, max / 2f) && max > 0f)
{
    Debug.LogWarning($"[HP_FIX] 检测到血量减半,恢复为满血: {max}");
    cur = max;
}
```

### 方案2: 客户端延迟上报
在Health组件完全初始化后再上报血量:
```csharp
// 延迟0.5秒上报
yield return new WaitForSeconds(0.5f);
Client_ReportSelfHealth_IfReadyOnce();
```

### 方案3: 强制同步
在场景加载完成后,主机强制向所有客户端发送权威血量:
```csharp
// 在场景加载完成后
foreach (var kv in remoteCharacters)
{
    var h = kv.Value.GetComponentInChildren<Health>(true);
    if (h) Server_ForceAuthSelf(h);
}
```

## 相关文档

- `进图血量减半问题分析.md` - 详细的问题分析
- `HP血量调试日志_已添加.md` - 之前添加的血量调试日志
- `客户端HP回报机制分析.md` - 血量同步机制分析
- `get-logs.bat` - 日志获取脚本
