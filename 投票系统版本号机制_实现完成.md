# 投票系统版本号机制 - 实现完成

## 问题描述

客户端在取消投票后依然显示投票界面，原因是网络包的先发后至导致已经关闭的投票被后续到达的旧包覆盖。

## 解决方案

实现了基于版本号（voteId）的投票过期机制：

### 1. 数据结构变更

**SceneVoteMessage.VoteStateData**
- 新增 `voteId` 字段：每次投票的唯一标识符
- 主机每次发起投票时自增分配

**SceneNet**
- 新增 `expiredVoteId` 字段：客户端维护的过期投票ID
- 用于过滤已取消的投票消息

### 2. 工作流程

#### 主机端

1. **发起投票**
   - 分配新的 `voteId = _nextVoteId++`
   - 创建投票状态并广播

2. **取消投票**
   - 设置 `active = false`
   - 广播取消状态（包含 voteId）
   - 客户端收到后会更新 `expiredVoteId`

#### 客户端

1. **接收投票消息**
   - 检查 `voteId <= expiredVoteId`
   - 如果过期，直接忽略
   - 如果有效，正常处理

2. **接收取消消息**
   - 更新 `expiredVoteId = data.voteId`
   - 清除投票状态
   - 后续收到的旧投票包会被自动过滤

### 3. 关键代码

#### 主机发起投票
```csharp
// 分配新的投票ID
var currentVoteId = _nextVoteId++;

_hostVoteState = new VoteStateData
{
    voteId = currentVoteId,
    active = true,
    // ... 其他字段
};
```

#### 客户端处理投票
```csharp
// 检查投票ID是否过期
if (data.voteId <= sceneNet.expiredVoteId)
{
    LoggerHelper.Log($"[SceneVote] 忽略过期投票: voteId={data.voteId}");
    return;
}

// 如果投票已取消，更新过期ID
if (!data.active)
{
    sceneNet.expiredVoteId = data.voteId;
    // 清除投票状态
}
```

## 优势

1. **防止时序问题**：后到的旧投票包会被自动过滤
2. **单次广播**：主机只需发送一次取消消息
3. **简单高效**：使用整数ID，无需复杂的时间戳比较
4. **向后兼容**：旧版本客户端会忽略 voteId 字段

## 测试建议

1. **正常流程**
   - 发起投票 → 取消投票 → 确认UI关闭

2. **网络延迟**
   - 模拟网络延迟，确保旧包被过滤

3. **多次投票**
   - 连续发起多次投票，确认 voteId 正确递增

4. **暴力发包**
   - 模拟主机快速发送多个投票包，确认客户端正确处理

## 问题修复

### 第一次实现（0544版本）
- ✅ 添加了 voteId 和 expiredVoteId 机制
- ❌ 但是 `SceneNet.CancelVote()` 没有调用 JSON 系统

### 第二次修复（0547版本）
- ✅ 修改 `SceneNet.CancelVote()` 调用 `SceneVoteMessage.Host_CancelVote()`
- ✅ 主机取消投票时会发送 JSON 格式的 `active=false` 消息
- ✅ 客户端收到后更新 `expiredVoteId`，过滤后续的旧投票包

### 根本原因

主机在取消投票时只发送了旧的二进制消息（`Op.SCENE_CANCEL`），但没有发送 JSON 格式的取消消息。由于投票系统每秒广播一次，取消后的旧投票包仍在网络中传输，导致客户端收到后重新显示投票界面。

## 部署信息

- **编译状态**：✅ 成功（19个警告，均为非关键警告）
- **部署状态**：✅ 已上传到游戏目录
- **版本号**：EscapeFromDuckovCoopMod-20251110-0547

---

*实现日期：2025-11-10*
*修复日期：2025-11-10*
