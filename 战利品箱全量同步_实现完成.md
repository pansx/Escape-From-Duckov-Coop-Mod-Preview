# 战利品箱全量同步 - 实现完成

## 实现概述

在SCENE_GATE_RELEASE后使用JSON消息（type="lootFullSync"）一次性同步所有战利品箱。

## 实现文件

### 1. 新增文件

**EscapeFromDuckovCoopMod/Net/LootFullSyncMessage.cs**
- 战利品箱全量同步消息的实现
- 包含数据结构、收集、发送和应用逻辑

### 2. 修改文件

**EscapeFromDuckovCoopMod/Net/JsonMessageRouter.cs**
- 添加对"lootFullSync"消息类型的路由处理

**EscapeFromDuckovCoopMod/Main/SceneService/SceneNet.cs**
- 在`Server_SendGateRelease`方法中添加战利品箱同步调用

**EscapeFromDuckovCoopMod/Main/Loader/Mod.cs**
- 在迟到放行逻辑中添加战利品箱同步调用

## 数据结构

### LootBoxData
```csharp
[System.Serializable]
public class LootBoxData
{
    public string type = "lootFullSync";
    public LootBoxInfo[] lootBoxes;
    public string timestamp;
}
```

### LootBoxInfo
```csharp
[System.Serializable]
public class LootBoxInfo
{
    public int lootUid;              // 战利品箱唯一ID
    public int aiId;                 // 关联的AI ID
    public Vector3Serializable position;
    public Vector3Serializable rotation;
    public int capacity;             // 容量
    public LootItemInfo[] items;     // 物品列表
}
```

### LootItemInfo
```csharp
[System.Serializable]
public class LootItemInfo
{
    public int position;             // 在容器中的位置
    public int typeId;               // 物品类型ID
    public int stack;                // 堆叠数量
    public float durability;         // 耐久度
    public float durabilityLoss;     // 耐久度损失
    public bool inspected;           // 是否已检查
}
```

## 工作流程

### 主机端

1. **场景加载完成**
   - 主机场景加载完成，所有战利品箱已生成

2. **客户端举手**
   - 客户端发送SCENE_GATE_READY

3. **主机放行**
   - 主机调用`Server_SendGateRelease(peer, sid)`
   - 发送SCENE_GATE_RELEASE消息

4. **立即同步战利品箱**
   - 调用`LootFullSyncMessage.Host_SendLootFullSync(peer)`
   - 收集场景中所有InteractableLootbox
   - 序列化为JSON
   - 发送给客户端

### 客户端

1. **接收放行**
   - 收到SCENE_GATE_RELEASE消息
   - 设置`_cliSceneGateReleased = true`

2. **接收战利品箱同步**
   - 收到lootFullSync JSON消息
   - `JsonMessageRouter`路由到`LootFullSyncMessage.Client_OnLootFullSync`

3. **应用战利品箱数据**
   - 查找或创建战利品箱
   - 设置容量
   - 清空现有物品
   - 添加同步的物品
   - 注册到LootManager

## 关键方法

### 主机端

#### Host_SendLootFullSync
```csharp
public static void Host_SendLootFullSync(NetPeer peer)
```
- 收集所有战利品箱数据
- 序列化为JSON
- 发送给指定客户端

#### CollectAllLootBoxes
```csharp
private static LootBoxInfo[] CollectAllLootBoxes()
```
- 查找场景中所有InteractableLootbox
- 获取lootUid和aiId
- 收集物品数据
- 返回战利品箱信息数组

#### CollectLootBoxItems
```csharp
private static LootItemInfo[] CollectLootBoxItems(Inventory inventory)
```
- 获取容器内容
- 遍历所有物品
- 提取物品属性（typeId, stack, durability等）
- 返回物品信息数组

### 客户端

#### Client_OnLootFullSync
```csharp
public static void Client_OnLootFullSync(string json)
```
- 解析JSON数据
- 调用ApplyLootBoxes应用数据

#### ApplyLootBoxes
```csharp
private static void ApplyLootBoxes(LootBoxInfo[] lootBoxes)
```
- 遍历所有战利品箱
- 调用ApplySingleLootBox应用每个箱子

#### ApplySingleLootBox
```csharp
private static void ApplySingleLootBox(LootBoxInfo boxInfo)
```
- 查找或创建战利品箱
- 设置容量
- 清空现有物品
- 使用ItemTool.BuildItemFromSnapshot创建物品
- 添加到容器
- 注册到LootManager

## 物品序列化

使用现有的ItemSnapshot结构：

```csharp
var snapshot = new LootNet.ItemSnapshot
{
    typeId = itemInfo.typeId,
    stack = itemInfo.stack,
    durability = itemInfo.durability,
    durabilityLoss = itemInfo.durabilityLoss,
    inspected = itemInfo.inspected,
    slots = null,      // 附件暂不支持
    inventory = null   // 容器内容暂不支持
};

var item = ItemTool.BuildItemFromSnapshot(snapshot);
```

## 触发时机

### 正常放行
```csharp
// SceneNet.cs - Server_SendGateRelease
private void Server_SendGateRelease(NetPeer peer, string sid)
{
    // 发送SCENE_GATE_RELEASE
    peer.SendSmart(w, Op.SCENE_GATE_RELEASE);
    
    // 🔧 立即发送战利品箱全量同步
    LootFullSyncMessage.Host_SendLootFullSync(peer);
}
```

### 迟到放行
```csharp
// Mod.cs - SCENE_GATE_READY处理
if (SceneNet.Instance._srvSceneGateOpen)
{
    // 发送SCENE_GATE_RELEASE
    peer.SendSmart(w, Op.SCENE_GATE_RELEASE);
    
    // 🔧 立即发送战利品箱全量同步
    LootFullSyncMessage.Host_SendLootFullSync(peer);
}
```

## 日志输出

### 主机端
```
[LootFullSync] 找到 X 个战利品箱
[LootFullSync] 收集战利品箱: lootUid=X, aiId=X, pos=(x,y,z), items=X
[LootFullSync] 战利品箱包含 X 个物品
[LootFullSync] 发送战利品箱全量同步: X 个箱子 → peer.EndPoint
```

### 客户端
```
[LootFullSync] 收到战利品箱全量同步: X 个箱子, 时间=timestamp
[LootFullSync] 找到现有战利品箱: lootUid=X, pos=(x,y,z)
或
[LootFullSync] 创建AI掉落箱: lootUid=X, aiId=X
[LootFullSync] 应用战利品箱: lootUid=X, items=X
[LootFullSync] 应用完成: 成功=X, 失败=X
```

## 限制和待完善

### 当前限制

1. **物品附件不支持**
   - slots字段设为null
   - 不同步武器配件、装备附件等

2. **容器内物品不支持**
   - inventory字段设为null
   - 不同步背包、箱子等容器类物品的内容

3. **物品位置**
   - 使用TryAddToInventory自动添加
   - 不保证物品在容器中的精确位置

### 待完善功能

1. **完整物品序列化**
   - 支持附件树（slots）
   - 支持容器内容（inventory）
   - 递归序列化复杂物品结构

2. **精确位置同步**
   - 使用inventory.AddAt(item, position)
   - 保持物品在容器中的原始位置

3. **增量同步**
   - 只同步变化的战利品箱
   - 减少网络传输量

4. **分包处理**
   - 大量战利品箱时分批发送
   - 避免单个JSON过大

## 编译状态

✅ 编译成功，无错误
⚠️ 19个警告（nullable引用、未使用字段等，不影响功能）

## 测试建议

1. **基础测试**
   - 2人联机，主机先进图
   - 客户端后进图，检查是否能看到战利品箱
   - 打开战利品箱，检查物品是否正确

2. **AI掉落测试**
   - 主机击杀AI，生成掉落箱
   - 客户端进图，检查是否能看到掉落箱
   - 打开掉落箱，检查物品是否正确

3. **迟到测试**
   - 主机和客户端A已在游戏中
   - 客户端B后加入
   - 检查客户端B是否能看到所有战利品箱

4. **大量战利品箱测试**
   - 场景中有多个战利品箱
   - 检查同步性能和正确性

## 下一步

1. 测试基础功能
2. 根据测试结果优化
3. 实现完整的物品序列化（附件、容器内容）
4. 实现精确位置同步
5. 实现增量同步和分包处理
