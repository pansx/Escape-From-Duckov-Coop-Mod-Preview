# Escape From Duckov 联机通信流程详解

## 一、整体架构

这个游戏采用 **混合 P2P 架构**，支持两种传输模式：

```
┌─────────────────────────────────────────────────────────┐
│              游戏联机通信架构                              │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────┐         ┌──────────────┐            │
│  │  直连模式     │         │  Steam P2P   │            │
│  │  (Direct)    │         │   模式       │            │
│  └──────┬───────┘         └──────┬───────┘            │
│         │                        │                     │
│         ├────────────┬───────────┤                     │
│         │            │           │                     │
│    ┌────▼────┐  ┌───▼────┐ ┌───▼─────┐               │
│    │ UDP直连  │  │ Steam  │ │ 虚拟IP  │               │
│    │         │  │ P2P    │ │ 映射    │               │
│    └────┬────┘  └───┬────┘ └───┬─────┘               │
│         │           │          │                      │
│         └───────────┴──────────┘                      │
│                     │                                 │
│              ┌──────▼──────┐                          │
│              │  LiteNetLib │                          │
│              │  网络层      │                          │
│              └──────┬──────┘                          │
│                     │                                 │
│              ┌──────▼──────┐                          │
│              │  RPC 系统   │                          │
│              └──────┬──────┘                          │
│                     │                                 │
│              ┌──────▼──────┐                          │
│              │  游戏逻辑   │                          │
│              └─────────────┘                          │
└─────────────────────────────────────────────────────────┘
```


## 二、两种传输模式对比

### 1. 直连模式 (Direct Mode)

```
玩家A (客户端)                    玩家B (主机)
     │                                │
     │  1. UDP 广播发现               │
     │  ──────────────────────────>  │
     │                                │
     │  2. 返回主机信息               │
     │  <──────────────────────────  │
     │                                │
     │  3. 直接 UDP 连接              │
     │  ══════════════════════════>  │
     │                                │
     │  4. 数据传输                   │
     │  <═════════════════════════>  │
```

**特点**：
- 需要在同一局域网或公网 IP
- 端口：默认 9050
- 无需 Steam 支持
- 延迟最低

### 2. Steam P2P 模式

```
玩家A (客户端)                Steam 服务器              玩家B (主机)
     │                            │                         │
     │  1. 创建 Lobby              │                         │
     │                            │  <──────────────────── │
     │                            │                         │
     │  2. 搜索 Lobby              │                         │
     │  ──────────────────────>  │                         │
     │                            │                         │
     │  3. 加入 Lobby              │                         │
     │  ──────────────────────>  │  ──────────────────>   │
     │                            │                         │
     │  4. P2P 握手                │                         │
     │  <═══════════════════════════════════════════════>  │
     │     (Steam 自动 NAT 穿透)                            │
     │                            │                         │
     │  5. 虚拟 IP 映射            │                         │
     │     10.255.x.x ←→ SteamID  │                         │
     │                            │                         │
     │  6. 数据传输                │                         │
     │  <═══════════════════════════════════════════════>  │
     │     (可能通过 Steam 中继)                            │
```

**特点**：
- 自动 NAT 穿透
- 支持全球联机
- 虚拟 IP：10.255.x.x
- 可能使用中继服务器


## 三、详细连接流程

### 主机端启动流程

```
┌─────────────────────────────────────────────────────────┐
│                    主机启动流程                          │
└─────────────────────────────────────────────────────────┘

1. 玩家点击"开始主机"
   │
   ├─> NetService.StartNetwork(isServer: true)
   │
   ├─> 创建 NetManager (LiteNetLib)
   │   ├─> 启用 4 个通道
   │   │   ├─ 通道 0: Critical (投票、伤害)
   │   │   ├─ 通道 1: Important (血量、装备)
   │   │   ├─ 通道 2: Normal (NPC、物品)
   │   │   └─ 通道 3: Frequent (位置、动画)
   │   │
   │   └─> 监听端口 9050
   │
   ├─> 判断传输模式
   │   │
   │   ├─ 如果是 Direct 模式
   │   │   └─> UseNativeSockets = true (使用 UDP)
   │   │
   │   └─ 如果是 Steam P2P 模式
   │       ├─> UseNativeSockets = false
   │       ├─> 初始化 SteamP2PManager
   │       ├─> 初始化 SteamEndPointMapper
   │       └─> 创建 Steam Lobby
   │           ├─> 设置 Lobby 元数据
   │           │   ├─ mod_id
   │           │   ├─ name
   │           │   ├─ version
   │           │   └─ password (SHA256)
   │           │
   │           └─> 等待 Lobby 创建成功
   │
   ├─> 初始化本地玩家
   │   └─> LocalPlayerManager.InitializeLocalPlayer()
   │
   └─> 注册事件监听
       └─> ItemAgent_Gun.OnMainCharacterShootEvent
```


### 客户端连接流程

```
┌─────────────────────────────────────────────────────────┐
│                    客户端连接流程                         │
└─────────────────────────────────────────────────────────┘

【直连模式】

1. 玩家点击"搜索主机"
   │
   ├─> CoopTool.SendBroadcastDiscovery()
   │   └─> 发送 UDP 广播 "DISCOVER_REQUEST"
   │
   ├─> 主机收到广播
   │   └─> 回复 "DISCOVER_RESPONSE"
   │
   ├─> 客户端收到响应
   │   └─> 添加到主机列表
   │
2. 玩家选择主机并点击"连接"
   │
   ├─> NetService.ConnectToHost(ip, port)
   │   │
   │   ├─> 启动客户端网络
   │   │   └─> StartNetwork(isServer: false)
   │   │
   │   ├─> 创建连接请求
   │   │   └─> writer.Put("gameKey")
   │   │
   │   └─> netManager.Connect(ip, port, writer)
   │
   ├─> 主机验证连接请求
   │   ├─> 检查 "gameKey"
   │   └─> request.Accept() 或 request.Reject()
   │
   ├─> 连接成功
   │   ├─> OnPeerConnected 回调
   │   ├─> 创建 PlayerStatus
   │   └─> 发送客户端状态
   │
   └─> 开始数据同步


【Steam P2P 模式】

1. 玩家点击"搜索 Lobby"
   │
   ├─> SteamLobbyManager.RequestLobbyList()
   │   ├─> 添加过滤器 (mod_id)
   │   ├─> 设置距离过滤 (全球)
   │   └─> 请求 Lobby 列表
   │
   ├─> Steam 返回 Lobby 列表
   │   └─> 显示在 UI 上
   │
2. 玩家选择 Lobby 并点击"加入"
   │
   ├─> SteamLobbyManager.JoinLobby(lobbyID)
   │   │
   │   ├─> 如果有密码
   │   │   ├─> 弹出密码输入框
   │   │   └─> 验证 SHA256 哈希
   │   │
   │   └─> 发送加入请求
   │
   ├─> 加入成功回调
   │   ├─> OnLobbyEnter
   │   ├─> 获取主机 SteamID
   │   └─> 触发连接
   │
   ├─> SteamLobbyHelper.TriggerMultiplayerConnect(hostSteamID)
   │   │
   │   ├─> 注册虚拟端点
   │   │   └─> SteamEndPointMapper.RegisterSteamID(hostSteamID)
   │   │       └─> 生成虚拟 IP: 10.255.x.x
   │   │
   │   ├─> 发送 P2P 握手包
   │   │   └─> SteamNetworking.SendP2PPacket(HANDSHAKE)
   │   │
   │   ├─> 等待 P2P 会话建立
   │   │   └─> WaitForP2PSessionEstablished(timeout: 10s)
   │   │
   │   └─> 使用虚拟 IP 连接
   │       └─> NetService.ConnectToHost("10.255.x.x", 9050)
   │
   ├─> 连接成功
   │   └─> 开始数据同步
   │
   └─> Steam P2P 数据传输
       ├─> 应用层使用虚拟 IP
       ├─> SteamP2PManager 拦截数据
       ├─> 转换 IP ↔ SteamID
       └─> 通过 Steam P2P API 传输
```


## 四、数据传输流程

### 数据发送流程

```
┌─────────────────────────────────────────────────────────┐
│                    数据发送流程                          │
└─────────────────────────────────────────────────────────┘

游戏逻辑层
   │
   ├─> 触发事件 (例如：玩家移动)
   │
   ├─> 创建 NetDataWriter
   │   ├─> writer.Put((byte)Op.PLAYER_MOVE)  // 操作码
   │   ├─> writer.PutV3cm(position)          // 压缩位置 (12字节)
   │   ├─> writer.PutDir(forward)            // 压缩方向 (4字节)
   │   └─> writer.Put(speed)                 // 速度
   │
   ├─> 选择目标和通道
   │   ├─> RPCTarget (Server/AllClients/TargetClient)
   │   ├─> DeliveryMethod (Reliable/Unreliable)
   │   └─> Channel (0-3)
   │
   ├─> 调用 RPC 或直接发送
   │   └─> netManager.Send(peer, writer, deliveryMethod, channel)
   │
   ├─> 判断传输模式
   │   │
   │   ├─【Direct 模式】
   │   │   └─> 直接通过 UDP Socket 发送
   │   │
   │   └─【Steam P2P 模式】
   │       ├─> SteamP2PManager.SendPacket()
   │       ├─> 转换虚拟 IP → SteamID
   │       │   └─> SteamEndPointMapper.TryGetSteamID()
   │       ├─> 选择 P2P 发送类型
   │       │   ├─ Unreliable → EP2PSend.Unreliable
   │       │   ├─ Reliable → EP2PSend.Reliable
   │       │   └─ ReliableOrdered → EP2PSend.ReliableWithBuffering
   │       └─> SteamNetworking.SendP2PPacket(steamID, data, sendType, channel)
   │
   └─> 数据发送完成
       └─> 更新统计信息 (PacketsSent, BytesSent)
```

### 数据接收流程

```
┌─────────────────────────────────────────────────────────┐
│                    数据接收流程                          │
└─────────────────────────────────────────────────────────┘

【Direct 模式】

UDP Socket
   │
   ├─> 接收数据包
   │
   ├─> LiteNetLib 处理
   │   ├─> 解析包头
   │   ├─> 验证序列号
   │   └─> 重组分片包
   │
   ├─> 触发 OnNetworkReceive 回调
   │   └─> ModBehaviourF.OnNetworkReceive(peer, reader)
   │
   └─> 游戏逻辑处理


【Steam P2P 模式】

Steam P2P API (每帧轮询)
   │
   ├─> SteamP2PManager.Update()
   │   │
   │   ├─> 遍历所有通道 (0-3)
   │   │   │
   │   │   ├─> SteamNetworking.IsP2PPacketAvailable(channel)
   │   │   │
   │   │   ├─> SteamNetworking.ReadP2PPacket(buffer, channel)
   │   │   │   └─> 获取 SteamID 和数据
   │   │   │
   │   │   ├─> 过滤 HANDSHAKE 包
   │   │   │
   │   │   ├─> 转换 SteamID → 虚拟 IP
   │   │   │   └─> SteamEndPointMapper.TryGetEndPoint()
   │   │   │
   │   │   └─> 入队到 _receivedPackets
   │   │
   │   └─> 批处理限制 (最多 512 包/帧)
   │
   ├─> NetService.PollEvents()
   │   └─> netManager.PollEvents()
   │
   ├─> LiteNetLib 从队列取包
   │   └─> TryGetReceivedPacket()
   │
   ├─> 触发 OnNetworkReceive 回调
   │   └─> ModBehaviourF.OnNetworkReceive(peer, reader)
   │
   └─> 游戏逻辑处理
       ├─> 读取操作码
       ├─> 根据操作码分发
       └─> 更新游戏状态
```


## 五、RPC 系统流程

### RPC 调用流程

```
┌─────────────────────────────────────────────────────────┐
│                    RPC 调用流程                          │
└─────────────────────────────────────────────────────────┘

客户端                                              主机
   │                                                 │
   │  1. 调用 RPC                                    │
   │  ─────────────────────────────────────────>    │
   │  HybridRPCManager.CallRPC(                     │
   │    "VoteCast",                                 │
   │    RPCTarget.Server,                           │
   │    (writer) => { writer.Put(true); }           │
   │  )                                             │
   │                                                 │
   │  2. 查找 RPC ID                                 │
   │  ─> _rpcNameToId["VoteCast"] = 5               │
   │                                                 │
   │  3. 打包数据                                    │
   │  ─> writer.Put((ushort)5)  // RPC ID           │
   │  ─> writer.Put(true)        // 参数             │
   │                                                 │
   │  4. 发送数据包                                  │
   │  ══════════════════════════════════════════>   │
   │                                                 │
   │                                                 │  5. 接收数据包
   │                                                 │  <─ OnNetworkReceive
   │                                                 │
   │                                                 │  6. 读取 RPC ID
   │                                                 │  ─> rpcId = reader.GetUShort()
   │                                                 │
   │                                                 │  7. 查找处理器
   │                                                 │  ─> _rpcIdToHandler[5]
   │                                                 │
   │                                                 │  8. 调用处理器
   │                                                 │  ─> HandleVoteCast(peer, reader)
   │                                                 │
   │                                                 │  9. 读取参数
   │                                                 │  ─> bool ready = reader.GetBool()
   │                                                 │
   │                                                 │  10. 执行逻辑
   │                                                 │  ─> 更新投票状态
   │                                                 │
   │  11. 广播结果 (可选)                            │
   │  <══════════════════════════════════════════   │
   │  CallRPC("VoteReadySet", AllClients, ...)      │
   │                                                 │
```

### RPC 注册流程

```
游戏启动
   │
   ├─> 初始化 HybridRPCManager
   │
   ├─> 注册 RPC 方法
   │   │
   │   ├─> RegisterRPC("VoteStart", HandleVoteStart)
   │   │   ├─> 生成 RPC ID (自增)
   │   │   ├─> _rpcNameToId["VoteStart"] = 1
   │   │   └─> _rpcIdToHandler[1] = HandleVoteStart
   │   │
   │   ├─> RegisterRPC("VoteCast", HandleVoteCast)
   │   │   └─> _rpcNameToId["VoteCast"] = 2
   │   │
   │   └─> ... (注册所有 RPC)
   │
   └─> RPC 系统就绪
```


## 六、关键数据同步

### 玩家位置同步

```
┌─────────────────────────────────────────────────────────┐
│                  玩家位置同步 (每 15ms)                   │
└─────────────────────────────────────────────────────────┘

客户端                                              主机
   │                                                 │
   │  每 15ms (66.7 FPS)                             │
   │  ├─> 读取本地玩家状态                           │
   │  │   ├─ position                                │
   │  │   ├─ rotation                                │
   │  │   ├─ velocity                                │
   │  │   └─ animation state                         │
   │  │                                              │
   │  ├─> 压缩数据                                   │
   │  │   ├─ PutV3cm(position)    // 12字节          │
   │  │   ├─ PutDir(forward)      // 4字节           │
   │  │   └─ Put(speed)            // 4字节           │
   │  │                                              │
   │  ├─> 发送到主机                                 │
   │  │   └─ Channel 3 (Frequent)                    │
   │  │   └─ DeliveryMethod.Unreliable               │
   │  │                                              │
   │  ══════════════════════════════════════════>   │
   │                                                 │
   │                                                 │  ├─> 接收位置数据
   │                                                 │  │
   │                                                 │  ├─> 解压数据
   │                                                 │  │   ├─ GetV3cm() → position
   │                                                 │  │   ├─ GetDir() → forward
   │                                                 │  │   └─ GetFloat() → speed
   │                                                 │  │
   │                                                 │  ├─> 更新远程角色
   │                                                 │  │   └─ remoteCharacters[peer].transform
   │                                                 │  │
   │                                                 │  └─> 广播给其他客户端
   │                                                 │      └─ CallRPC("PlayerMove", AllClientsExceptSender)
   │                                                 │
   │  <══════════════════════════════════════════   │
   │  接收其他玩家位置                               │
   │                                                 │
   │  ├─> 更新远程角色                               │
   │  │   └─ clientRemoteCharacters[playerId]        │
   │  │                                              │
   │  └─> 插值平滑移动                               │
   │      └─ Vector3.Lerp(current, target, t)        │
```

### 伤害同步

```
┌─────────────────────────────────────────────────────────┐
│                      伤害同步流程                         │
└─────────────────────────────────────────────────────────┘

客户端 A (攻击者)                主机                  客户端 B (受害者)
      │                          │                          │
      │  1. 射击事件              │                          │
      │  ─> OnShoot()            │                          │
      │                          │                          │
      │  2. 发送射击数据          │                          │
      │  ══════════════════>     │                          │
      │  PutDamagePayload(di)    │                          │
      │  - 伤害值                 │                          │
      │  - 命中点 (压缩)          │                          │
      │  - 命中法线 (压缩)        │                          │
      │  - 武器 ID                │                          │
      │  Channel 0 (Critical)    │                          │
      │  Reliable                │                          │
      │                          │                          │
      │                          │  3. 验证射击              │
      │                          │  ─> 检查距离              │
      │                          │  ─> 检查弹药              │
      │                          │  ─> 计算伤害              │
      │                          │                          │
      │                          │  4. 应用伤害              │
      │                          │  ─> Health.TakeDamage()  │
      │                          │                          │
      │                          │  5. 广播伤害事件          │
      │                          │  ═════════════════════>  │
      │  <══════════════════════ │  ═════════════════════>  │
      │  CallRPC("DamageEvent",  │                          │
      │    AllClients)           │                          │
      │                          │                          │
      │  6. 播放伤害特效          │                          │  6. 播放伤害特效
      │  ─> 血液粒子              │                          │  ─> 血液粒子
      │  ─> 伤害数字              │                          │  ─> 伤害数字
      │  ─> 音效                  │                          │  ─> 音效
```


## 七、Steam P2P 虚拟 IP 映射

### 虚拟 IP 映射机制

```
┌─────────────────────────────────────────────────────────┐
│              Steam P2P 虚拟 IP 映射机制                   │
└─────────────────────────────────────────────────────────┘

真实 Steam 网络                    虚拟网络层
     │                                │
     │  SteamID: 76561198012345678    │  虚拟 IP: 10.255.0.1
     │  ─────────────────────────────>│
     │                                │
     │  SteamID: 76561198087654321    │  虚拟 IP: 10.255.0.2
     │  ─────────────────────────────>│
     │                                │
     │  SteamID: 76561198011111111    │  虚拟 IP: 10.255.0.3
     │  ─────────────────────────────>│


【映射表结构】

SteamEndPointMapper
   │
   ├─> _steamIdToEndPoint (Dictionary)
   │   ├─ 76561198012345678 → 10.255.0.1:9050
   │   ├─ 76561198087654321 → 10.255.0.2:9050
   │   └─ 76561198011111111 → 10.255.0.3:9050
   │
   ├─> _endPointToSteamId (Dictionary)
   │   ├─ 10.255.0.1:9050 → 76561198012345678
   │   ├─ 10.255.0.2:9050 → 76561198087654321
   │   └─ 10.255.0.3:9050 → 76561198011111111
   │
   └─> _p2pSessionEstablished (HashSet)
       └─ 已建立 P2P 会话的 SteamID


【虚拟 IP 生成算法】

GenerateVirtualEndPoint()
   │
   ├─> 基础地址: 10.255.0.0/16
   │
   ├─> 生成随机 IP
   │   ├─ byte3 = Random(0, 255)
   │   ├─ byte4 = Random(1, 254)  // 避免 0 和 255
   │   └─ IP = 10.255.{byte3}.{byte4}
   │
   ├─> 检查冲突
   │   └─> 如果已存在，重新生成
   │
   └─> 返回 IPEndPoint(IP, 9050)


【数据发送时的转换】

应用层
   │
   ├─> Send(10.255.0.2:9050, data)
   │
   ├─> SteamP2PManager 拦截
   │
   ├─> 查询映射表
   │   └─> TryGetSteamID(10.255.0.2:9050)
   │       └─> 返回 76561198087654321
   │
   ├─> 转换为 Steam P2P 调用
   │   └─> SteamNetworking.SendP2PPacket(
   │         steamID: 76561198087654321,
   │         data: data,
   │         sendType: Reliable,
   │         channel: 0
   │       )
   │
   └─> Steam 处理实际传输


【数据接收时的转换】

Steam P2P API
   │
   ├─> ReadP2PPacket()
   │   └─> 返回 (steamID: 76561198087654321, data)
   │
   ├─> 查询映射表
   │   └─> TryGetEndPoint(76561198087654321)
   │       └─> 返回 10.255.0.2:9050
   │
   ├─> 转换为虚拟端点
   │   └─> 创建 NetPacket(10.255.0.2:9050, data)
   │
   └─> 传递给 LiteNetLib
       └─> 应用层收到 (10.255.0.2:9050, data)
```


## 八、数据压缩技术

### 位置压缩 (Vector3 → 12 字节)

```
原始数据 (24 字节)
   │
   ├─> float x = 123.456789  (4 字节)
   ├─> float y = 78.912345   (4 字节)
   └─> float z = -45.678901  (4 字节)

压缩算法 (PutV3cm)
   │
   ├─> 乘以 100 (转换为厘米)
   │   ├─> x_cm = 12345.6789 → 12346 (四舍五入)
   │   ├─> y_cm = 7891.2345 → 7891
   │   └─> z_cm = -4567.8901 → -4568
   │
   ├─> 转换为整数
   │   ├─> int x_int = 12346  (4 字节)
   │   ├─> int y_int = 7891   (4 字节)
   │   └─> int z_int = -4568  (4 字节)
   │
   └─> 总大小: 12 字节 (节省 50%)

解压算法 (GetV3cm)
   │
   ├─> 读取整数
   │   ├─> int x_int = 12346
   │   ├─> int y_int = 7891
   │   └─> int z_int = -4568
   │
   ├─> 除以 100 (转换回米)
   │   ├─> float x = 123.46
   │   ├─> float y = 78.91
   │   └─> float z = -45.68
   │
   └─> 精度损失: ±0.5 厘米
```

### 方向压缩 (Vector3 → 4 字节)

```
原始数据 (12 字节)
   │
   ├─> float x = 0.707
   ├─> float y = 0.0
   └─> float z = 0.707

压缩算法 (PutDir)
   │
   ├─> 转换为球坐标
   │   ├─> yaw = atan2(z, x) = 45°
   │   └─> pitch = asin(y) = 0°
   │
   ├─> 归一化到 [0, 1]
   │   ├─> yaw_norm = 45° / 360° = 0.125
   │   └─> pitch_norm = (0° + 90°) / 180° = 0.5
   │
   ├─> 量化为 ushort
   │   ├─> yaw_u16 = 0.125 × 65535 = 8192  (2 字节)
   │   └─> pitch_u16 = 0.5 × 65535 = 32768 (2 字节)
   │
   └─> 总大小: 4 字节 (节省 67%)

解压算法 (GetDir)
   │
   ├─> 读取 ushort
   │   ├─> yaw_u16 = 8192
   │   └─> pitch_u16 = 32768
   │
   ├─> 反归一化
   │   ├─> yaw = 8192 / 65535 × 360° = 45°
   │   └─> pitch = 32768 / 65535 × 180° - 90° = 0°
   │
   ├─> 转换为笛卡尔坐标
   │   ├─> x = cos(pitch) × cos(yaw) = 0.707
   │   ├─> y = sin(pitch) = 0.0
   │   └─> z = cos(pitch) × sin(yaw) = 0.707
   │
   └─> 精度损失: ±0.0055°
```

### 伤害数据压缩

```
原始伤害数据 (约 80 字节)
   │
   ├─> float damage = 50.0           (4 字节)
   ├─> float armorPenetration = 0.3  (4 字节)
   ├─> float critMultiplier = 2.0    (4 字节)
   ├─> float critChance = 0.15       (4 字节)
   ├─> bool isCrit = true            (1 字节)
   ├─> Vector3 hitPoint              (24 字节)
   ├─> Vector3 hitNormal             (12 字节)
   ├─> int weaponId = 1001           (4 字节)
   ├─> float bleedChance = 0.2       (4 字节)
   ├─> bool isExplosion = false      (1 字节)
   └─> float explosionRadius = 0.0   (4 字节)

压缩后 (约 36 字节)
   │
   ├─> float damage                  (4 字节)
   ├─> float armorPenetration        (4 字节)
   ├─> float critMultiplier          (4 字节)
   ├─> float critChance              (4 字节)
   ├─> byte flags                    (1 字节)  ← 压缩 4 个 bool
   │   └─> PackFlags(isCrit, isExplosion, ...)
   ├─> Vector3 hitPoint (压缩)       (12 字节) ← PutV3cm
   ├─> Vector3 hitNormal (压缩)      (4 字节)  ← PutDir
   ├─> int weaponId                  (4 字节)
   ├─> float bleedChance             (4 字节)
   └─> float explosionRadius         (4 字节)

节省: 44 字节 → 36 字节 (约 18%)
```


## 九、通道系统

### 4 通道架构

```
┌─────────────────────────────────────────────────────────┐
│                    4 通道系统设计                         │
└─────────────────────────────────────────────────────────┘

通道 0: Critical (关键数据)
   ├─> 传输方式: ReliableOrdered
   ├─> 优先级: 最高
   ├─> 用途:
   │   ├─ 投票系统 (VoteStart, VoteCast)
   │   ├─ 伤害事件 (DamageEvent)
   │   ├─ 交互事件 (ItemPickup, DoorOpen)
   │   └─ 场景切换 (SceneLoad)
   └─> 特点: 必须可靠送达，顺序严格

通道 1: Important (重要数据)
   ├─> 传输方式: ReliableUnordered
   ├─> 优先级: 高
   ├─> 用途:
   │   ├─ 血量同步 (HealthUpdate)
   │   ├─ 装备变更 (EquipmentChange)
   │   ├─ 状态效果 (BuffApply, DebuffApply)
   │   └─ 任务进度 (QuestUpdate)
   └─> 特点: 必须可靠，但不要求顺序

通道 2: Normal (普通数据)
   ├─> 传输方式: ReliableUnordered
   ├─> 优先级: 中
   ├─> 用途:
   │   ├─ NPC 生成 (NPCSpawn)
   │   ├─ 物品生成 (ItemSpawn)
   │   ├─ 环境变化 (WeatherChange)
   │   └─ 聊天消息 (ChatMessage)
   └─> 特点: 可靠但不紧急

通道 3: Frequent (高频数据)
   ├─> 传输方式: Unreliable
   ├─> 优先级: 低
   ├─> 用途:
   │   ├─ 玩家位置 (PlayerMove) - 每 15ms
   │   ├─ 玩家旋转 (PlayerRotate)
   │   ├─ 动画状态 (AnimationState)
   │   └─ 音效触发 (SoundPlay)
   └─> 特点: 高频率，允许丢包


【通道选择决策树】

数据类型
   │
   ├─> 是否必须送达？
   │   │
   │   ├─ 是 → 是否需要严格顺序？
   │   │   │
   │   │   ├─ 是 → 通道 0 (Critical)
   │   │   │   └─ 例: 投票、伤害
   │   │   │
   │   │   └─ 否 → 是否紧急？
   │   │       │
   │   │       ├─ 是 → 通道 1 (Important)
   │   │       │   └─ 例: 血量、装备
   │   │       │
   │   │       └─ 否 → 通道 2 (Normal)
   │   │           └─ 例: NPC、物品
   │   │
   │   └─ 否 → 通道 3 (Frequent)
   │       └─ 例: 位置、动画


【带宽分配】

总带宽: 1 Mbps (假设)
   │
   ├─> 通道 0: 20% (200 Kbps)  ← 保证关键数据
   ├─> 通道 1: 30% (300 Kbps)  ← 重要但不紧急
   ├─> 通道 2: 20% (200 Kbps)  ← 普通数据
   └─> 通道 3: 30% (300 Kbps)  ← 高频但可丢弃
```


## 十、性能优化

### 同步频率优化

```
┌─────────────────────────────────────────────────────────┐
│                  同步频率优化策略                         │
└─────────────────────────────────────────────────────────┘

数据类型              频率        带宽占用      优化方法
────────────────────────────────────────────────────────
玩家位置              66.7 Hz     ~1 KB/s      ├─ 压缩位置 (12字节)
                     (15ms)                   ├─ 压缩方向 (4字节)
                                              ├─ Unreliable 传输
                                              └─ 客户端插值

玩家动画              30 Hz       ~0.5 KB/s    ├─ 只发送状态变化
                     (33ms)                   ├─ 使用枚举 (1字节)
                                              └─ Unreliable 传输

血量同步              事件驱动     ~0.1 KB/s    ├─ 只在变化时发送
                                              ├─ Reliable 传输
                                              └─ 通道 1

伤害事件              事件驱动     ~0.2 KB/s    ├─ 压缩伤害数据
                                              ├─ Reliable 传输
                                              └─ 通道 0

NPC 位置              10 Hz       ~2 KB/s      ├─ 压缩位置
                     (100ms)                  ├─ 只同步可见 NPC
                                              └─ Unreliable 传输

物品拾取              事件驱动     ~0.05 KB/s   ├─ 只发送物品 ID
                                              ├─ Reliable 传输
                                              └─ 通道 0


【自适应同步频率】

网络质量检测
   │
   ├─> 测量延迟
   │   ├─ < 50ms  → 优秀
   │   ├─ 50-100ms → 良好
   │   ├─ 100-200ms → 一般
   │   └─ > 200ms → 差
   │
   ├─> 测量丢包率
   │   ├─ < 1%   → 优秀
   │   ├─ 1-5%   → 良好
   │   ├─ 5-10%  → 一般
   │   └─ > 10%  → 差
   │
   └─> 调整同步频率
       │
       ├─ 优秀网络
       │   ├─ 位置: 66.7 Hz
       │   ├─ 动画: 30 Hz
       │   └─ NPC: 10 Hz
       │
       ├─ 良好网络
       │   ├─ 位置: 50 Hz
       │   ├─ 动画: 20 Hz
       │   └─ NPC: 5 Hz
       │
       ├─ 一般网络
       │   ├─ 位置: 30 Hz
       │   ├─ 动画: 15 Hz
       │   └─ NPC: 3 Hz
       │
       └─ 差网络
           ├─ 位置: 20 Hz
           ├─ 动画: 10 Hz
           └─ NPC: 2 Hz
```

### 客户端预测与插值

```
┌─────────────────────────────────────────────────────────┐
│                客户端预测与插值系统                       │
└─────────────────────────────────────────────────────────┘

【位置插值】

时间轴:  t0      t1      t2      t3      t4
         │       │       │       │       │
服务器:  ●───────●───────●───────●───────●
         │       │       │       │       │
客户端:  ●───○───●───○───●───○───●───○───●
             ↑       ↑       ↑       ↑
           插值点   插值点   插值点   插值点

插值算法:
   │
   ├─> 接收服务器位置 (t1)
   │   └─> targetPosition = serverPosition
   │
   ├─> 计算插值时间
   │   └─> interpolationTime = 100ms (延迟缓冲)
   │
   ├─> 每帧更新
   │   └─> currentPosition = Lerp(
   │         lastPosition,
   │         targetPosition,
   │         deltaTime / interpolationTime
   │       )
   │
   └─> 平滑移动


【客户端预测】

本地玩家输入
   │
   ├─> 立即应用到本地
   │   └─> localPosition += velocity × deltaTime
   │
   ├─> 发送到服务器
   │   └─> Send(inputData, sequenceNumber)
   │
   ├─> 服务器验证
   │   ├─> 计算服务器端位置
   │   └─> 返回权威位置
   │
   ├─> 客户端接收
   │   └─> serverPosition
   │
   └─> 误差校正
       │
       ├─> 计算误差
       │   └─> error = serverPosition - localPosition
       │
       ├─> 如果误差 < 阈值 (0.1m)
       │   └─> 忽略 (避免抖动)
       │
       └─> 如果误差 >= 阈值
           └─> 平滑校正
               └─> localPosition = Lerp(
                     localPosition,
                     serverPosition,
                     correctionSpeed × deltaTime
                   )


【延迟补偿 (Lag Compensation)】

客户端射击
   │
   ├─> 记录射击时间戳
   │   └─> shootTime = Time.time
   │
   ├─> 发送到服务器
   │   └─> Send(shootData, shootTime, clientLatency)
   │
   ├─> 服务器回溯
   │   │
   │   ├─> 计算回溯时间
   │   │   └─> rewindTime = shootTime - clientLatency
   │   │
   │   ├─> 回溯玩家位置
   │   │   └─> 从历史缓冲区获取 rewindTime 时的位置
   │   │
   │   ├─> 执行命中检测
   │   │   └─> Raycast(shootPosition, shootDirection)
   │   │
   │   └─> 应用伤害
   │       └─> target.TakeDamage(damage)
   │
   └─> 广播结果
       └─> CallRPC("DamageEvent", AllClients)
```


## 十一、错误处理与容错

### 连接失败处理

```
┌─────────────────────────────────────────────────────────┐
│                  连接失败处理流程                         │
└─────────────────────────────────────────────────────────┘

客户端尝试连接
   │
   ├─> 发送连接请求
   │
   ├─> 等待响应 (超时: 10s)
   │   │
   │   ├─【成功】
   │   │   └─> OnPeerConnected
   │   │       └─> 开始数据同步
   │   │
   │   ├─【超时】
   │   │   ├─> 显示错误: "连接超时"
   │   │   ├─> 记录日志
   │   │   └─> 提供重试选项
   │   │       ├─ 重试 (最多 3 次)
   │   │       └─ 取消
   │   │
   │   ├─【拒绝】
   │   │   ├─> 原因: 版本不匹配
   │   │   │   └─> 显示: "请更新到最新版本"
   │   │   │
   │   │   ├─> 原因: 密码错误
   │   │   │   └─> 显示: "密码错误，请重试"
   │   │   │
   │   │   ├─> 原因: 房间已满
   │   │   │   └─> 显示: "房间已满"
   │   │   │
   │   │   └─> 原因: 主机拒绝
   │   │       └─> 显示: "连接被拒绝"
   │   │
   │   └─【网络错误】
   │       ├─> SocketError.NetworkUnreachable
   │       │   └─> 显示: "网络不可达，请检查网络连接"
   │       │
   │       ├─> SocketError.ConnectionRefused
   │       │   └─> 显示: "连接被拒绝，主机可能未启动"
   │       │
   │       └─> SocketError.HostUnreachable
   │           └─> 显示: "无法连接到主机"


【Steam P2P 特殊处理】

P2P 会话建立失败
   │
   ├─> 原因: NAT 穿透失败
   │   ├─> 尝试使用 Steam 中继
   │   │   └─> SteamNetworking.AllowP2PPacketRelay(true)
   │   │
   │   └─> 如果仍失败
   │       └─> 显示: "无法建立 P2P 连接，请尝试直连模式"
   │
   ├─> 原因: Steam 未初始化
   │   └─> 显示: "Steam 未运行，请启动 Steam"
   │
   └─> 原因: 对方不在线
       └─> 显示: "对方不在线或未加入 Lobby"
```

### 断线重连

```
┌─────────────────────────────────────────────────────────┐
│                    断线重连机制                          │
└─────────────────────────────────────────────────────────┘

检测到断线
   │
   ├─> OnPeerDisconnected 回调
   │   └─> 原因: DisconnectInfo.Reason
   │       ├─ Timeout (超时)
   │       ├─ ConnectionFailed (连接失败)
   │       ├─ RemoteConnectionClose (远程关闭)
   │       └─ DisconnectPeerCalled (主动断开)
   │
   ├─> 保存游戏状态
   │   ├─> 玩家位置
   │   ├─> 背包物品
   │   ├─> 任务进度
   │   └─> 场景 ID
   │
   ├─> 显示断线提示
   │   └─> "连接已断开，是否重新连接？"
   │       ├─ 重新连接
   │       └─ 返回主菜单
   │
   ├─【选择重新连接】
   │   │
   │   ├─> 尝试重连 (最多 3 次)
   │   │   │
   │   │   ├─> 第 1 次: 立即重连
   │   │   ├─> 第 2 次: 等待 3 秒
   │   │   └─> 第 3 次: 等待 5 秒
   │   │
   │   ├─【重连成功】
   │   │   ├─> 恢复游戏状态
   │   │   ├─> 同步服务器数据
   │   │   └─> 继续游戏
   │   │
   │   └─【重连失败】
   │       ├─> 显示: "重连失败"
   │       └─> 返回主菜单
   │
   └─【选择返回主菜单】
       ├─> 清理网络资源
       ├─> 销毁远程角色
       └─> 加载主菜单场景


【主机迁移 (Host Migration)】

主机断线
   │
   ├─> 检测到主机断开
   │   └─> connectedPeer == null
   │
   ├─> 选择新主机
   │   ├─> 策略: 连接时间最长的客户端
   │   └─> 或: 延迟最低的客户端
   │
   ├─> 通知所有客户端
   │   └─> CallRPC("HostMigration", AllClients, newHostId)
   │
   ├─> 新主机初始化
   │   ├─> 切换到服务器模式
   │   │   └─> StartNetwork(isServer: true, keepSteamLobby: true)
   │   │
   │   ├─> 接管游戏状态
   │   │   ├─ NPC 控制权
   │   │   ├─ 物品生成
   │   │   └─ 任务进度
   │   │
   │   └─> 通知完成
   │       └─> CallRPC("HostMigrationComplete", AllClients)
   │
   └─> 客户端重新连接
       └─> ConnectToHost(newHostIP, port)
```


## 十二、安全机制

### 数据验证

```
┌─────────────────────────────────────────────────────────┐
│                    数据验证机制                          │
└─────────────────────────────────────────────────────────┘

服务器端验证
   │
   ├─> 位置验证
   │   ├─> 检查移动速度
   │   │   └─> if (distance > maxSpeed × deltaTime)
   │   │       └─> 拒绝并回滚
   │   │
   │   ├─> 检查位置合理性
   │   │   └─> if (position.magnitude > 10000)
   │   │       └─> 拒绝并回滚
   │   │
   │   └─> 检查穿墙
   │       └─> if (Physics.Linecast(lastPos, newPos))
   │           └─> 拒绝并回滚
   │
   ├─> 伤害验证
   │   ├─> 检查武器有效性
   │   │   └─> if (!HasWeapon(weaponId))
   │   │       └─> 拒绝
   │   │
   │   ├─> 检查射击距离
   │   │   └─> if (distance > weaponRange)
   │   │       └─> 拒绝
   │   │
   │   ├─> 检查弹药
   │   │   └─> if (ammo <= 0)
   │   │       └─> 拒绝
   │   │
   │   └─> 检查射击频率
   │       └─> if (timeSinceLastShot < fireRate)
   │           └─> 拒绝
   │
   ├─> 物品验证
   │   ├─> 检查拾取距离
   │   │   └─> if (distance > pickupRange)
   │   │       └─> 拒绝
   │   │
   │   ├─> 检查物品存在
   │   │   └─> if (!ItemExists(itemId))
   │   │       └─> 拒绝
   │   │
   │   └─> 检查背包空间
   │       └─> if (inventory.IsFull())
   │           └─> 拒绝
   │
   └─> 投票验证
       ├─> 检查投票权限
       │   └─> if (!IsInVote(playerId))
       │       └─> 拒绝
       │
       └─> 检查重复投票
           └─> if (HasVoted(playerId))
               └─> 拒绝


【防作弊机制】

1. 速度检测
   ├─> 记录玩家历史位置
   ├─> 计算移动速度
   └─> 超速 → 警告 → 踢出

2. 射击检测
   ├─> 记录射击时间戳
   ├─> 检查射击间隔
   └─> 异常 → 警告 → 踢出

3. 物品检测
   ├─> 记录物品获取
   ├─> 检查物品来源
   └─> 非法 → 删除 → 警告

4. 数据完整性
   ├─> 计算数据校验和
   ├─> 验证数据签名
   └─> 篡改 → 断开连接
```

### 密码保护

```
┌─────────────────────────────────────────────────────────┐
│                  Lobby 密码保护机制                       │
└─────────────────────────────────────────────────────────┘

主机创建 Lobby
   │
   ├─> 输入密码 (可选)
   │   └─> password = "myPassword123"
   │
   ├─> 计算 SHA256 哈希
   │   └─> passwordHash = SHA256(password)
   │       └─> "5e884898da28047151d0e56f8dc6292773603d0d6aabbdd62a11ef721d1542d8"
   │
   ├─> 设置 Lobby 元数据
   │   ├─> "password_protected" = "true"
   │   └─> "password" = passwordHash
   │
   └─> 创建 Lobby


客户端加入 Lobby
   │
   ├─> 检查是否有密码
   │   └─> if (lobbyData["password_protected"] == "true")
   │
   ├─> 弹出密码输入框
   │   └─> 用户输入: "myPassword123"
   │
   ├─> 计算 SHA256 哈希
   │   └─> inputHash = SHA256("myPassword123")
   │
   ├─> 比对哈希
   │   └─> if (inputHash == lobbyData["password"])
   │       ├─ 匹配 → 允许加入
   │       └─ 不匹配 → 拒绝加入
   │
   └─> 加入 Lobby


【安全特性】

1. 不传输明文密码
   └─> 只传输 SHA256 哈希

2. 哈希不可逆
   └─> 无法从哈希还原密码

3. 彩虹表攻击防护
   └─> 可选: 添加盐值 (Salt)
       └─> SHA256(password + salt)

4. 暴力破解防护
   └─> 限制尝试次数 (3 次)
   └─> 失败后冷却时间 (30 秒)
```


## 十三、完整示例：玩家射击流程

### 端到端射击流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        完整射击流程示例                                        │
└─────────────────────────────────────────────────────────────────────────────┘

客户端 A (射击者)              主机 (服务器)              客户端 B (旁观者)
      │                          │                          │
      │  1. 玩家按下射击键        │                          │
      │  ─> Input.GetMouseButton  │                          │
      │                          │                          │
      │  2. 本地立即播放特效      │                          │
      │  ─> 枪口火光              │                          │
      │  ─> 射击音效              │                          │
      │  ─> 后坐力动画            │                          │
      │                          │                          │
      │  3. 本地射线检测          │                          │
      │  ─> Physics.Raycast       │                          │
      │  ─> 显示弹孔 (临时)       │                          │
      │                          │                          │
      │  4. 打包射击数据          │                          │
      │  ─> writer.Put(Op.SHOOT)  │                          │
      │  ─> writer.Put(weaponId)  │                          │
      │  ─> writer.PutV3cm(origin)│                          │
      │  ─> writer.PutDir(dir)    │                          │
      │  ─> writer.Put(timestamp) │                          │
      │  ─> writer.Put(latency)   │                          │
      │                          │                          │
      │  5. 发送到服务器          │                          │
      │  ══════════════════════> │                          │
      │  Channel 0 (Critical)    │                          │
      │  ReliableOrdered         │                          │
      │                          │                          │
      │                          │  6. 接收射击数据          │
      │                          │  ─> OnNetworkReceive     │
      │                          │                          │
      │                          │  7. 延迟补偿              │
      │                          │  ─> rewindTime =         │
      │                          │      timestamp - latency │
      │                          │  ─> 回溯玩家位置          │
      │                          │                          │
      │                          │  8. 服务器端验证          │
      │                          │  ─> 检查弹药              │
      │                          │  ─> 检查射击间隔          │
      │                          │  ─> 检查武器有效性        │
      │                          │                          │
      │                          │  9. 服务器端射线检测      │
      │                          │  ─> Physics.Raycast      │
      │                          │  ─> 命中目标: 客户端 C    │
      │                          │                          │
      │                          │  10. 计算伤害             │
      │                          │  ─> baseDamage = 50      │
      │                          │  ─> critRoll = Random    │
      │                          │  ─> isCrit = true        │
      │                          │  ─> finalDamage = 100    │
      │                          │                          │
      │                          │  11. 应用伤害             │
      │                          │  ─> target.TakeDamage()  │
      │                          │  ─> health: 200 → 100    │
      │                          │                          │
      │                          │  12. 打包伤害事件         │
      │                          │  ─> writer.Put(          │
      │                          │      Op.DAMAGE_EVENT)    │
      │                          │  ─> writer.Put(          │
      │                          │      shooterId)          │
      │                          │  ─> writer.Put(          │
      │                          │      targetId)           │
      │                          │  ─> writer.PutDamage     │
      │                          │      Payload(di)         │
      │                          │                          │
      │                          │  13. 广播给所有客户端     │
      │  <══════════════════════ │  ═════════════════════> │
      │  CallRPC("DamageEvent",  │                          │
      │    AllClients)           │                          │
      │                          │                          │
      │  14. 接收伤害事件         │                          │  14. 接收伤害事件
      │  ─> OnDamageEvent        │                          │  ─> OnDamageEvent
      │                          │                          │
      │  15. 播放命中特效         │                          │  15. 播放命中特效
      │  ─> 血液粒子              │                          │  ─> 血液粒子
      │  ─> 伤害数字 (-100)       │                          │  ─> 伤害数字 (-100)
      │  ─> 命中音效              │                          │  ─> 命中音效
      │                          │                          │
      │  16. 更新 UI              │                          │  16. 更新 UI
      │  ─> 击杀提示 (如果击杀)   │                          │  ─> 目标血条更新
      │                          │                          │


【数据包内容详解】

射击数据包 (客户端 → 服务器)
   ├─> byte opCode = Op.SHOOT                    (1 字节)
   ├─> int weaponId = 1001                       (4 字节)
   ├─> Vector3 origin (压缩) = (100, 2, 50)      (12 字节)
   ├─> Vector3 direction (压缩) = (0.7, 0, 0.7)  (4 字节)
   ├─> float timestamp = 123.456                 (4 字节)
   ├─> int latency = 50                          (4 字节)
   └─> 总大小: 29 字节

伤害事件包 (服务器 → 所有客户端)
   ├─> byte opCode = Op.DAMAGE_EVENT             (1 字节)
   ├─> string shooterId = "10.255.0.1:9050"      (16 字节)
   ├─> string targetId = "10.255.0.3:9050"       (16 字节)
   ├─> DamageInfo (压缩)                         (36 字节)
   │   ├─ float damage = 100                     (4 字节)
   │   ├─ float armorPen = 0.3                   (4 字节)
   │   ├─ float critMult = 2.0                   (4 字节)
   │   ├─ float critChance = 0.15                (4 字节)
   │   ├─ byte flags (isCrit, isExplosion, ...)  (1 字节)
   │   ├─ Vector3 hitPoint (压缩)                (12 字节)
   │   ├─ Vector3 hitNormal (压缩)               (4 字节)
   │   ├─ int weaponId = 1001                    (4 字节)
   │   ├─ float bleedChance = 0.2                (4 字节)
   │   └─ float explosionRadius = 0.0            (4 字节)
   └─> 总大小: 69 字节


【时间线】

t=0ms    客户端 A 按下射击键
t=1ms    本地播放特效
t=2ms    发送射击数据包
t=27ms   主机接收 (延迟 25ms)
t=28ms   主机验证和计算
t=29ms   主机广播伤害事件
t=54ms   客户端 A 接收 (延迟 25ms)
t=54ms   客户端 B 接收 (延迟 25ms)
t=55ms   所有客户端播放特效

总延迟: 54ms (从射击到看到伤害)
```


## 十四、总结

### 架构优势

```
┌─────────────────────────────────────────────────────────┐
│                    架构优势总结                          │
└─────────────────────────────────────────────────────────┘

1. 混合传输模式
   ├─> 直连模式: 低延迟，适合局域网
   └─> Steam P2P: 全球联机，自动 NAT 穿透

2. 虚拟 IP 映射
   ├─> 透明的 P2P 通信
   ├─> 无需修改应用层代码
   └─> 易于调试和维护

3. 多通道系统
   ├─> 优先级管理
   ├─> 带宽优化
   └─> 减少关键数据延迟

4. 数据压缩
   ├─> 位置压缩: 节省 50%
   ├─> 方向压缩: 节省 67%
   └─> 总带宽节省: 50-70%

5. RPC 系统
   ├─> 简化网络编程
   ├─> 类型安全
   └─> 易于扩展

6. 客户端预测
   ├─> 降低感知延迟
   ├─> 平滑的游戏体验
   └─> 误差校正

7. 延迟补偿
   ├─> 公平的射击判定
   ├─> 回溯验证
   └─> 防作弊

8. 容错机制
   ├─> 断线重连
   ├─> 主机迁移
   └─> 数据验证
```

### 性能指标

```
┌─────────────────────────────────────────────────────────┐
│                    性能指标参考                          │
└─────────────────────────────────────────────────────────┘

延迟 (Latency)
   ├─> 局域网直连: 1-10ms
   ├─> Steam P2P 直连: 20-50ms
   ├─> Steam P2P 中继: 50-200ms
   └─> 目标: < 100ms

带宽 (Bandwidth)
   ├─> 每个玩家上行: ~50 KB/s
   ├─> 每个玩家下行: ~100 KB/s (2 人房间)
   ├─> 每个玩家下行: ~200 KB/s (4 人房间)
   └─> 峰值: ~500 KB/s

同步频率 (Sync Rate)
   ├─> 玩家位置: 66.7 Hz (15ms)
   ├─> 玩家动画: 30 Hz (33ms)
   ├─> NPC 位置: 10 Hz (100ms)
   └─> 事件驱动: 即时

丢包率 (Packet Loss)
   ├─> 正常: < 1%
   ├─> 可接受: 1-5%
   ├─> 较差: 5-10%
   └─> 不可用: > 10%

CPU 占用
   ├─> 网络线程: ~5-10%
   ├─> 序列化: ~2-5%
   └─> 总计: ~10-15%

内存占用
   ├─> 网络缓冲区: ~10 MB
   ├─> 历史数据: ~5 MB
   └─> 总计: ~20 MB
```

### 技术栈

```
┌─────────────────────────────────────────────────────────┐
│                      技术栈清单                          │
└─────────────────────────────────────────────────────────┘

网络库
   ├─> LiteNetLib: 底层网络传输
   ├─> Steamworks.NET: Steam P2P 封装
   └─> UniTask: 异步操作

序列化
   ├─> NetDataWriter/Reader: 二进制序列化
   ├─> 自定义压缩算法
   └─> 零 GC 分配

游戏引擎
   ├─> Unity 2021.3+
   ├─> C# 9.0+
   └─> .NET Standard 2.1

依赖项
   ├─> LiteNetLib 1.1.0+
   ├─> Steamworks.NET 20.2.0+
   └─> UniTask 2.3.3+
```

### 最佳实践建议

```
1. 网络设计
   ├─> 使用权威服务器模型
   ├─> 客户端只发送输入，不发送结果
   ├─> 服务器验证所有操作
   └─> 使用客户端预测降低延迟感知

2. 数据优化
   ├─> 只同步变化的数据
   ├─> 使用压缩算法
   ├─> 选择合适的同步频率
   └─> 使用对象池避免 GC

3. 错误处理
   ├─> 实现断线重连
   ├─> 提供清晰的错误提示
   ├─> 记录详细的日志
   └─> 优雅降级

4. 安全性
   ├─> 服务器端验证所有数据
   ├─> 使用哈希保护密码
   ├─> 实现防作弊机制
   └─> 限制请求频率

5. 测试
   ├─> 模拟高延迟环境
   ├─> 测试丢包情况
   ├─> 压力测试
   └─> 跨平台测试

6. 监控
   ├─> 实时显示网络统计
   ├─> 记录异常事件
   ├─> 性能分析
   └─> 用户反馈收集
```

---

## 参考资料

- **LiteNetLib 文档**: https://github.com/RevenantX/LiteNetLib
- **Steamworks.NET 文档**: https://steamworks.github.io/
- **Unity 网络最佳实践**: https://docs.unity3d.com/Manual/BestPracticeUnderstandingPerformanceInUnity.html
- **RPC API 文档**: 见项目根目录 `RPC_API_Documentation.md`
- **Net 模块文档**: 见 `codeAnalysis/duckovAPI/Net/`

---

**文档版本**: 1.0  
**最后更新**: 2025-01-08  
**作者**: Kiro AI Assistant
