# 战利品与坟墓同步系统

## 概述

战利品与坟墓同步系统负责处理怪物掉落、玩家死亡坟墓、战利品箱交互的网络同步，确保所有客户端的物品状态一致。系统采用主机权威架构，使用Token机制和ID分配防止重复操作。

## 核心模块

### Main/Item - 物品同步核心

**位置**: `EscapeFromDuckovCoopMod/Main/Item/`

**核心文件**:
- `ItemHandle.cs` - 物品掉落/拾取处理
- `ItemRequest.cs` - 物品请求发送
- `ItemTool.cs` - 物品工具函数

**详细文档**: [Main/Item.md](duckovAPI/Main/Item.md)

### Patch/InventoryAndLootBox - 战利品箱补丁

**位置**: `EscapeFromDuckovCoopMod/Patch/InventoryAndLootBox/`

**核心文件**:
- `InteractableLootboxPatch.cs` - 战利品箱交互补丁（死亡坟墓生成）
- `InventoryPatch.cs` - 背包操作补丁
- `LootBoxLoaderPatch.cs` - 战利品箱加载补丁
- `LootSpawner.cs` - 战利品生成器补丁
- `LootViewPatch.cs` - 战利品视图补丁

**详细文档**: [Patch/PatchInventoryAndLootBox.md](duckovAPI/Patch/PatchInventoryAndLootBox.md)

---

## 关键同步流程

### 1. 怪物掉落战利品流程

```
怪物死亡 (AI OnDead)
    ↓
服务端生成战利品箱 (InteractableLootbox.CreateFromItem)
    ↓
广播 LOOT_SPAWN 消息
    ↓
客户端接收并创建战利品箱
    ↓
战利品箱注册到 LevelManager.LootBoxInventories
```

**关键代码**:
```csharp
// InteractableLootboxPatch.cs - Patch_Lootbox_CreateFromItem_DeferredSpawn
private static IEnumerator DeferredSpawn(InteractableLootbox box, CharacterMainControl who)
{
    yield return null;
    var mod = ModBehaviourF.Instance;
    if (mod && mod.IsServer && box) 
        DeadLootBox.Instance.Server_OnDeadLootboxSpawned(box, who);
}
```

### 2. 玩家死亡坟墓生成流程

```
玩家死亡 (CharacterMainControl.OnDead)
    ↓
设置 DeadLootSpawnContext.InOnDead 标记
    ↓
服务端延迟1帧生成坟墓 (防止同帧冲突)
    ↓
客户端路径被阻断 (Patch_Lootbox_CreateFromItem_BlockClient)
    ↓
广播 DEAD_LOOT_SPAWN 消息
    ↓
客户端接收并创建坟墓战利品箱
```

**关键机制**:
- **延迟生成**: 使用协程延迟1帧，避免同帧多次调用冲突
- **客户端阻断**: 客户端在 OnDead 路径禁止本地创建，防止双生
- **上下文标记**: `DeadLootSpawnContext.InOnDead` 标识当前死亡角色

**关键代码**:
```csharp
// InteractableLootboxPatch.cs - Patch_Lootbox_CreateFromItem_BlockClient
private static bool Prefix()
{
    var mod = ModBehaviourF.Instance;
    if (mod != null && mod.networkStarted && !mod.IsServer && 
        DeadLootSpawnContext.InOnDead != null)
        return false; // 客户端处于OnDead路径→禁止本地创建
    return true;
}
```

### 3. 战利品箱打开流程

```
客户端打开箱子 (InteractableLootbox.StartLoot)
    ↓
设置 Inventory.Loading = true (挂起UI)
    ↓
发送 LOOT_STATE_REQUEST 请求
    ↓
启动1.5秒超时兜底 (KickLootTimeout)
    ↓
服务端返回完整快照 (LOOT_STATE 消息)
    ↓
客户端同步物品列表
    ↓
设置 Loading = false，显示UI
```

**关键代码**:
```csharp
// InteractableLootboxPatch.cs - Patch_Lootbox_StartLoot_RequestState
private static void Postfix(InteractableLootbox __instance, ref bool __result)
{
    if (!__result) return;
    var m = ModBehaviourF.Instance;
    if (m == null || !m.networkStarted || m.IsServer) return;
    
    var inv = __instance ? __instance.Inventory : null;
    if (inv == null) return;
    
    inv.Loading = true; // 先挂起 UI
    COOPManager.LootNet.Client_RequestLootState(inv); // 请求快照
    LootManager.Instance.KickLootTimeout(inv); // 1.5s 兜底
}
```

### 4. 物品掉落流程

```
客户端掉落物品 (Item.Drop)
    ↓
生成本地物品并分配 Token
    ↓
记录到 pendingTokenItems (token → item)
    ↓
发送 ITEM_DROP_REQUEST (包含 token + 物品快照)
    ↓
服务端接收并生成物品
    ↓
分配唯一 ID，记录到 serverDroppedItems
    ↓
广播 ITEM_SPAWN (包含 token + id)
    ↓
客户端接收：
  - 发起者：匹配 token，绑定 id 到本地物品
  - 其他人：创建新物品
```

**关键代码**:
```csharp
// ItemHandle.cs - HandleItemDropRequest
public void HandleItemDropRequest(NetPeer peer, NetPacketReader r)
{
    if (!IsServer) return;
    var token = r.GetUInt();
    var pos = r.GetV3cm();
    var dir = r.GetDir();
    var angle = r.GetFloat();
    var create = r.GetBool();
    var snap = ItemTool.ReadItemSnapshot(r);
    
    // 在主机生成物体（并阻止 Postfix 再广播）
    var item = ItemTool.BuildItemFromSnapshot(snap);
    if (item == null) return;
    _serverSpawnedFromClientItems.Add(item);
    var agent = item.Drop(pos, create, dir, angle);
    
    // 分配唯一 id，入表
    var id = ItemTool.AllocateDropId();
    ItemTool.serverDroppedItems[id] = item;
    
    if (agent && agent.gameObject) 
        ItemTool.AddNetDropTag(agent.gameObject, id);
    
    // 广播 SPAWN（包含 token，发回给所有客户端）
    var w = writer;
    if (w == null) return;
    w.Reset();
    w.Put((byte)Op.ITEM_SPAWN);
    w.Put(token); // 回显客户端 token
    w.Put(id);
    w.PutV3cm(pos);
    w.PutDir(dir);
    w.Put(angle);
    w.Put(create);
    ItemTool.WriteItemSnapshot(w, item);
    CoopTool.BroadcastReliable(w);
}
```

### 5. 物品拾取流程

```
客户端拾取物品 (Item.Pickup)
    ↓
查找物品的 NetDropTag，获取 ID
    ↓
发送 ITEM_PICKUP_REQUEST (包含 id)
    ↓
服务端验证物品存在
    ↓
从 serverDroppedItems 移除
    ↓
销毁场景中的 Agent GameObject
    ↓
广播 ITEM_DESPAWN (包含 id)
    ↓
客户端接收并移除物品
```

**关键代码**:
```csharp
// ItemHandle.cs - HandleItemPickupRequest
public void HandleItemPickupRequest(NetPeer peer, NetPacketReader r)
{
    if (!IsServer) return;
    var id = r.GetUInt();
    if (!serverDroppedItems.TryGetValue(id, out var item) || item == null)
        return; // 可能已经被别人拿走
    
    // 从映射表移除，并销毁场景 agent
    serverDroppedItems.Remove(id);
    try
    {
        var agent = item.ActiveAgent;
        if (agent != null && agent.gameObject != null)
            Object.Destroy(agent.gameObject);
    }
    catch (Exception e)
    {
        Debug.LogWarning($"[ITEM] 服务器销毁 agent 异常: {e.Message}");
    }
    
    // 广播 DESPAWN
    var w = writer;
    if (w == null) return;
    w.Reset();
    w.Put((byte)Op.ITEM_DESPAWN);
    w.Put(id);
    CoopTool.BroadcastReliable(w);
}
```

---

## 关键技术机制

### 1. Token 机制

**用途**: 防止重复掉落和拾取

**实现**:
- 客户端掉落时生成本地 Token (uint)
- 服务端回显 Token，客户端据此识别自己的掉落
- 避免发起者重复创建物品

**相关字段**:
```csharp
public readonly HashSet<uint> pendingLocalDropTokens = new();
public readonly Dictionary<uint, Item> pendingTokenItems = new(); // token → item
```

### 2. ID 分配系统

**用途**: 服务端统一分配唯一物品ID

**实现**:
- 服务端维护全局 ID 计数器
- 每个掉落物品分配唯一 ID
- 客户端通过 ID 识别和操作物品

**相关字段**:
```csharp
public readonly Dictionary<uint, Item> serverDroppedItems = new(); // 主机记录
public readonly Dictionary<uint, Item> clientDroppedItems = new(); // 客户端记录
```

### 3. 标记集合

**用途**: 防止重复广播和请求

**实现**:
```csharp
// 客户端：标记"来自主机的生成"，防止 Prefix 误发请求
public readonly HashSet<Item> _clientSpawnByServerItems = new();

// 主机：标记"来自客户端请求的生成"，防止 Postfix 二次广播
public readonly HashSet<Item> _serverSpawnedFromClientItems = new();
```

### 4. 快照系统

**用途**: 序列化物品完整状态

**实现**:
- `ItemTool.WriteItemSnapshot()` - 序列化物品
- `ItemTool.ReadItemSnapshot()` - 反序列化物品
- `ItemTool.BuildItemFromSnapshot()` - 从快照重建物品

**包含数据**:
- 物品类型、ID、名称
- 耐久度、弹药数量
- 附件、改装件
- 检视状态

### 5. 延迟广播优化

**用途**: 合并同帧多次广播，减少网络开销

**实现**:
- 使用 `DeferedRunner` 延迟到帧结束
- 同一帧内同一容器的多次广播合并为一次
- 显著减少场景加载时的性能尖峰

**关键代码**:
```csharp
// 延迟到帧结束执行
DeferedRunner.Instance.RunAtEndOfFrame(() => {
    // 广播战利品状态
    COOPManager.LootNet.Server_BroadcastLootState(inventory);
});
```

### 6. 缓存优化

**用途**: 减少 FindObjectsOfType 调用

**实现**:
- `InteractableLootbox` 缓存
- 使用 `LevelManager.LootBoxInventories` 字典
- 减少 7 处高频 FindObjectsOfType 调用

**性能提升**: 减少 81% 的 FindObjectsOfType 调用

### 7. 死亡路径保护

**用途**: 避免客户端在死亡路径双生坟墓

**实现**:
- `DeadLootSpawnContext.InOnDead` 标记当前死亡角色
- 客户端在 OnDead 路径禁止本地创建
- 服务端延迟1帧生成，避免同帧冲突

---

## 网络消息协议

### ITEM_DROP_REQUEST (客户端 → 服务端)

**触发**: 客户端掉落物品

**数据结构**:
```
uint token          // 本地 Token
Vector3 position    // 掉落位置（压缩）
Vector3 direction   // 掉落方向（压缩）
float angle         // 旋转角度
bool create         // 是否创建 Agent
ItemSnapshot snap   // 物品快照
```

### ITEM_SPAWN (服务端 → 所有客户端)

**触发**: 服务端确认掉落

**数据结构**:
```
uint token          // 回显客户端 Token
uint id             // 服务端分配的唯一 ID
Vector3 position    // 掉落位置
Vector3 direction   // 掉落方向
float angle         // 旋转角度
bool create         // 是否创建 Agent
ItemSnapshot snap   // 物品快照
```

### ITEM_PICKUP_REQUEST (客户端 → 服务端)

**触发**: 客户端拾取物品

**数据结构**:
```
uint id             // 物品 ID
```

### ITEM_DESPAWN (服务端 → 所有客户端)

**触发**: 服务端确认拾取

**数据结构**:
```
uint id             // 物品 ID
```

### LOOT_STATE_REQUEST (客户端 → 服务端)

**触发**: 客户端打开战利品箱

**数据结构**:
```
int lootKey         // 战利品箱唯一键
```

### LOOT_STATE (服务端 → 请求客户端)

**触发**: 服务端响应战利品状态请求

**数据结构**:
```
int lootKey         // 战利品箱唯一键
int itemCount       // 物品数量
ItemSnapshot[] items // 物品快照数组
bool needInspection // 是否需要检视
```

### DEAD_LOOT_SPAWN (服务端 → 所有客户端)

**触发**: 玩家死亡生成坟墓

**数据结构**:
```
int lootKey         // 坟墓唯一键
Vector3 position    // 坟墓位置
Quaternion rotation // 坟墓旋转
int deadPlayerId    // 死亡玩家 ID
ItemSnapshot[] items // 掉落物品列表
```

---

## 性能优化

### 异步消息队列系统

**问题**: 客户端在复杂地图前1分钟帧率极低（10-20 FPS）

**根因**: 2600+ 个 `LOOT_STATE` 消息在网络接收线程同步处理，阻塞主线程

**解决方案**:
- 新增 `AsyncMessageQueue.cs` - 异步消息处理队列
- 批量模式：100条/帧（原10条/帧）
- 在 `Op.SCENE_BEGIN_LOAD` 时立即启用批量模式

**性能提升**: 客户端帧率从 10-20 FPS 提升至 50-60 FPS

**相关文档**: [PR_性能优化与稳定性提升.md](../PR_性能优化与稳定性提升.md)

### FindObjectsOfType 优化

**问题**: 频繁的 `FindObjectsOfType` 调用导致性能瓶颈

**优化内容**:
- `InteractableLootbox` 缓存（7处优化）
- 使用 `LevelManager.LootBoxInventories` 字典
- 场景加载时预缓存所有战利品箱

**性能提升**: 减少 81% 的 FindObjectsOfType 调用

### 延迟广播合并

**问题**: 场景加载时战利品同步导致性能尖峰

**优化内容**:
- 使用 `DeferedRunner` 将广播延迟到帧结束
- 同一帧内同一容器的多次广播合并为一次
- 场景保护：添加空值检查

**性能提升**: 战利品消息处理速度提升 900%

---

## 故障排查

### 问题1: 客户端打开箱子卡死

**症状**: 客户端打开战利品箱后 UI 一直显示 Loading

**可能原因**:
1. 网络消息丢失，未收到 `LOOT_STATE` 响应
2. 服务端战利品箱状态异常
3. 超时兜底未触发

**排查步骤**:
1. 检查日志中是否有 `LOOT_STATE_REQUEST` 发送记录
2. 检查服务端是否返回 `LOOT_STATE` 消息
3. 确认 `KickLootTimeout` 是否正常工作（1.5秒超时）

**解决方案**:
```csharp
// InteractableLootboxPatch.cs 已实现超时兜底
LootManager.Instance.KickLootTimeout(inv); // 1.5s 后强制解除 Loading
```

### 问题2: 物品重复生成

**症状**: 同一个掉落物品在场景中出现多次

**可能原因**:
1. Token 机制失效，客户端未正确识别自己的掉落
2. 标记集合未正确维护
3. 网络消息重复接收

**排查步骤**:
1. 检查 `pendingLocalDropTokens` 是否正确移除
2. 检查 `_clientSpawnByServerItems` 标记是否生效
3. 查看日志中是否有重复的 `ITEM_SPAWN` 消息

**解决方案**:
```csharp
// ItemHandle.cs - HandleItemSpawn
if (pendingLocalDropTokens.Remove(token))
{
    // 这是自己的掉落，绑定 ID 到本地物品
    if (pendingTokenItems.TryGetValue(token, out var localItem) && localItem != null)
    {
        clientDroppedItems[id] = localItem;
        pendingTokenItems.Remove(token);
        return; // 不重复创建
    }
}
```

### 问题3: 玩家死亡坟墓双生

**症状**: 玩家死亡后出现两个坟墓战利品箱

**可能原因**:
1. 客户端在 OnDead 路径未被阻断
2. `DeadLootSpawnContext.InOnDead` 标记未正确设置
3. 延迟生成协程失效

**排查步骤**:
1. 检查 `Patch_Lootbox_CreateFromItem_BlockClient` 是否生效
2. 确认 `DeadLootSpawnContext.InOnDead` 是否正确设置
3. 查看是否有异常导致协程中断

**解决方案**:
```csharp
// InteractableLootboxPatch.cs - Patch_Lootbox_CreateFromItem_BlockClient
private static bool Prefix()
{
    var mod = ModBehaviourF.Instance;
    if (mod != null && mod.networkStarted && !mod.IsServer && 
        DeadLootSpawnContext.InOnDead != null)
        return false; // 客户端处于OnDead路径→禁止本地创建
    return true;
}
```

### 问题4: 战利品状态不同步

**症状**: 不同客户端看到的战利品箱内容不一致

**可能原因**:
1. 战利品生成种子不同步
2. 客户端未正确请求状态快照
3. 网络消息丢失

**排查步骤**:
1. 检查 `LootSpawner` 补丁是否正确同步种子
2. 确认客户端打开箱子时发送了 `LOOT_STATE_REQUEST`
3. 查看服务端是否返回完整快照

**解决方案**:
- 确保所有客户端使用相同的随机种子
- 客户端打开箱子时强制请求服务端状态
- 使用可靠传输确保消息不丢失

---

## 最佳实践

### 1. 物品掉落

```csharp
// ✅ 正确：使用 Token 机制
var token = ItemTool.GenerateToken();
pendingLocalDropTokens.Add(token);
pendingTokenItems[token] = item;
ItemRequest.SendItemDropRequest(token, item, position, direction);

// ❌ 错误：直接本地生成
item.Drop(position, true, direction, 0f); // 会导致重复生成
```

### 2. 战利品箱打开

```csharp
// ✅ 正确：请求服务端状态
inv.Loading = true;
COOPManager.LootNet.Client_RequestLootState(inv);
LootManager.Instance.KickLootTimeout(inv);

// ❌ 错误：直接使用本地状态
// 可能导致状态不一致
```

### 3. 死亡坟墓生成

```csharp
// ✅ 正确：设置上下文标记并延迟生成
DeadLootSpawnContext.InOnDead = deadCharacter;
yield return null; // 延迟1帧
InteractableLootbox.CreateFromItem(...);
DeadLootSpawnContext.InOnDead = null;

// ❌ 错误：直接同步生成
InteractableLootbox.CreateFromItem(...); // 可能导致双生
```

### 4. 物品拾取

```csharp
// ✅ 正确：通过 ID 请求拾取
var tag = item.GetComponent<NetDropTag>();
if (tag != null)
{
    ItemRequest.SendItemPickupRequest(tag.dropId);
}

// ❌ 错误：直接本地拾取
item.Pickup(); // 其他客户端不会同步
```

---

## 相关文档

- [Main/Item 模块文档](duckovAPI/Main/Item.md)
- [Patch/InventoryAndLootBox 模块文档](duckovAPI/Patch/PatchInventoryAndLootBox.md)
- [Patch/Item 模块文档](duckovAPI/Patch/PatchItem.md)
- [性能优化PR文档](../PR_性能优化与稳定性提升.md)
- [项目总览](README.md)

---

## 更新历史

| 日期 | 版本 | 更新内容 |
|------|------|----------|
| 2024-11-08 | 1.0.0 | 初始版本，完整文档化战利品与坟墓同步系统 |

---

*本文档由 Kiro AI 自动生成和维护*
