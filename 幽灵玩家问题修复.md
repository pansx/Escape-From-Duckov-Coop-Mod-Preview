# 幽灵玩家问题修复

## 问题描述

玩家状态列表中，当玩家断开连接后，UI上仍然显示该玩家（幽灵玩家），导致玩家列表不准确。

## 根本原因分析

### 问题1：主机端断开连接时未清理数据库

在 `NetService.OnPeerDisconnected()` 方法中，虽然清理了 `playerStatuses` 和 `remoteCharacters`，但没有从 `PlayerInfoDatabase` 数据库中删除玩家记录。

而 `MModUI.UpdatePlayerList()` 方法直接从数据库读取所有玩家并显示，导致已断开连接的玩家仍然显示在UI上。

### 问题2：客户端接收投票消息时未完全同步数据库

客户端在接收主机的投票消息（包括 `voteId=0` 的玩家信息更新消息）时，只是**增量添加**玩家到数据库，没有删除不在主机列表中的玩家。

这导致当某个玩家断开连接后，主机的玩家列表中已经没有该玩家，但客户端数据库中仍然保留着该玩家的记录，UI上继续显示幽灵玩家。

## 修复方案

### 修复1：主机端断开连接时清理数据库

在 `NetService.OnPeerDisconnected()` 方法中添加数据库清理逻辑：

```csharp
// 🔥 从数据库中删除断开连接的玩家
var player = Utils.Database.PlayerInfoDatabase.Instance.GetPlayerByEndPoint(_st.EndPoint);
if (player != null)
{
    Debug.Log($"[OnPeerDisconnected] 从数据库删除玩家: {player.PlayerName} (SteamId: {player.SteamId})");
    Utils.Database.PlayerInfoDatabase.Instance.RemovePlayer(player.SteamId);
}
```

### 修复2：客户端完全同步数据库

在 `SceneVoteMessage.Client_HandleVoteState()` 方法中，处理 `voteId=0` 的玩家信息更新消息时，改为**完全同步**数据库：

```csharp
// 🔥 第一步：添加或更新主机发来的所有玩家
var hostPlayerSteamIds = new HashSet<string>();
foreach (var player in data.playerList.items)
{
    if (!string.IsNullOrEmpty(player.steamId))
    {
        hostPlayerSteamIds.Add(player.steamId);
        playerDb.AddOrUpdatePlayer(...);
    }
}

// 🔥 第二步：删除数据库中不在主机列表中的玩家（除了本地玩家）
var allPlayersInDb = playerDb.GetAllPlayers().ToList();
foreach (var dbPlayer in allPlayersInDb)
{
    if (dbPlayer.IsLocalPlayer)
        continue;
    
    if (!hostPlayerSteamIds.Contains(dbPlayer.SteamId))
    {
        playerDb.RemovePlayer(dbPlayer.SteamId);
    }
}
```

## 修改文件

- `EscapeFromDuckovCoopMod/Main/NetService.cs` (第 270-305 行)
- `EscapeFromDuckovCoopMod/Net/SceneVoteMessage.cs` (第 608-680 行)

## 测试建议

### 测试场景1：主机端断开连接

1. 启动游戏作为主机
2. 让客户端加入
3. 按 `P` 键打开玩家状态面板，确认客户端显示在列表中
4. 让客户端断开连接
5. 确认主机的玩家列表中该客户端已被移除

### 测试场景2：客户端同步玩家列表

1. 启动游戏作为主机，客户端A和客户端B加入
2. 在客户端A上按 `P` 键，确认看到主机和客户端B
3. 让客户端B断开连接
4. 主机会自动发送玩家信息更新消息给客户端A
5. 确认客户端A的玩家列表中客户端B已被移除（不再显示幽灵玩家）

## 技术细节

### 数据库同步策略

- **主机端**：玩家断开连接时立即从数据库删除
- **客户端**：接收主机的玩家列表时，完全同步数据库（删除不在列表中的玩家）
- **本地玩家**：永远不会被删除（即使不在主机列表中）

### 消息类型

- `voteId > 0`：真正的投票消息，激活投票UI
- `voteId = 0`：玩家信息更新消息，不激活投票UI，只同步数据库

## 版本信息

- 修复日期: 2025-11-11
- 部署版本: EscapeFromDuckovCoopMod-20251111-2108
