# 踢人功能使用说明

## 功能概述

实现了基于 Steam ID 的踢人功能，主机可以通过玩家列表 UI 踢出指定玩家。

## 功能特点

- ✅ **纯基于 Steam ID**：使用 Steam ID 作为唯一标识，确保踢人准确性
- ✅ **JSON 消息传输**：使用现有的 JSON 消息系统（Op.JSON）
- ✅ **UI 集成**：在玩家列表中显示"踢"按钮（仅主机可见）
- ✅ **自动断开连接**：被踢玩家自动断开所有连接并离开 Lobby
- ✅ **仅 Steam 模式**：只在 Steam P2P 模式下启用

## 实现文件

### 1. KickMessage.cs
位置：`EscapeFromDuckovCoopMod/Net/KickMessage.cs`

**功能**：
- `Server_KickPlayer(ulong targetSteamId, string reason)`：主机踢出指定玩家
- `Client_HandleKickMessage(string json)`：客户端处理踢人消息
- `IsKickMessage(string json)`：检查是否是踢人消息

**消息格式**：
```json
{
    "type": "kick",
    "targetSteamId": 76561198012345678,
    "reason": "被主机踢出"
}
```

### 2. JsonMessageRouter.cs
位置：`EscapeFromDuckovCoopMod/Net/JsonMessageRouter.cs`

**修改**：
- 在 `HandleJsonMessage` 方法中添加了 `case "kick"` 分支
- 路由踢人消息到 `KickMessage.Client_HandleKickMessage`

### 3. MModUI.cs
位置：`EscapeFromDuckovCoopMod/Main/UI/MModUI.cs`

**修改**：
- 在 `CreatePlayerEntry` 方法中添加了踢人按钮
- 按钮仅在以下条件下显示：
  - 当前是主机（`IsServer`）
  - 不是本地玩家（`!isLocal`）
  - Steam 模式（`isSteamMode`）
  - Steam 已初始化（`SteamManager.Initialized`）
  - 成功获取到玩家的 Steam ID

## 使用方法

### 主机端

1. 按 `=` 键打开联机面板
2. 按 `P` 键打开玩家状态窗口
3. 在玩家列表中，每个远程玩家的右侧会显示一个红色的"踢"按钮
4. 点击"踢"按钮即可踢出该玩家

### 客户端

- 被踢出时会自动：
  1. 断开与主机的连接
  2. 停止网络服务
  3. 离开 Steam Lobby
  4. 在状态栏显示"已被踢出：[原因]"

## 工作流程

```
主机点击踢人按钮
    ↓
调用 KickMessage.Server_KickPlayer(steamId, reason)
    ↓
构造 JSON 消息 {"type":"kick", "targetSteamId":xxx, "reason":"..."}
    ↓
通过 JsonMessage.BroadcastToAllClients 广播给所有客户端
    ↓
所有客户端接收到 Op.JSON 消息
    ↓
JsonMessageRouter 路由到 KickMessage.Client_HandleKickMessage
    ↓
客户端检查 targetSteamId 是否是自己
    ↓
如果是自己，执行断开连接操作
```

## 技术细节

### Steam ID 获取

使用 `GetSteamIdFromStatus(PlayerStatus status)` 方法从玩家状态获取 Steam ID：

1. 如果 EndPoint 是 "Steam:xxx" 格式，直接解析
2. 如果 EndPoint 是 "Host:xxx" 格式，获取 Lobby 所有者的 Steam ID
3. 如果是虚拟 IP 格式，通过 `SteamEndPointMapper` 查询

### 消息传输

- 使用 `Op.JSON` 操作码（值为 200）
- 传输方式：`DeliveryMethod.ReliableOrdered`（可靠有序）
- 通过 `JsonMessage.BroadcastToAllClients` 广播给所有客户端

### 断开连接流程

被踢玩家执行以下操作：
1. 更新状态显示：`service.status = "已被踢出: [原因]"`
2. 断开 Peer 连接：`service.connectedPeer.Disconnect()`
3. 停止网络服务：`service.StopNetwork()`
4. 离开 Steam Lobby：`SteamLobbyManager.Instance.LeaveLobby()`

## 注意事项

1. **仅 Steam 模式**：踢人功能只在 Steam P2P 模式下可用，直连模式不支持
2. **主机权限**：只有主机可以看到和使用踢人按钮
3. **广播机制**：踢人消息会广播给所有客户端，但只有目标玩家会执行断开操作
4. **Steam ID 必需**：如果无法获取玩家的 Steam ID，踢人按钮不会显示

## 测试建议

1. **正常踢人**：
   - 主机创建 Steam Lobby
   - 客户端加入
   - 主机点击踢人按钮
   - 验证客户端是否正确断开连接

2. **多人场景**：
   - 3+ 玩家加入同一个 Lobby
   - 主机踢出其中一个玩家
   - 验证其他玩家不受影响

3. **边界情况**：
   - 尝试踢出自己（应该看不到按钮）
   - 客户端尝试踢人（应该看不到按钮）
   - 直连模式下检查按钮是否隐藏

## 未来改进建议

1. **踢人确认对话框**：点击踢人按钮后弹出确认对话框，避免误操作
2. **踢人原因选择**：提供预设的踢人原因（如"AFK"、"作弊"、"违规"等）
3. **踢人日志**：记录踢人操作到日志文件，便于追溯
4. **黑名单功能**：被踢玩家加入黑名单，防止再次加入
5. **投票踢人**：非主机玩家可以发起投票踢人
6. **临时封禁**：踢人后一段时间内禁止该玩家加入

## 相关文档

- [JSON 消息系统使用指南](EscapeFromDuckovCoopMod/Net/JsonMessage_Usage.md)
- [玩家状态同步机制](玩家渲染同步机制分析.md)
- [Steam Lobby 管理](EscapeFromDuckovCoopMod/Net/Steam/SteamLobbyHelper.cs)
