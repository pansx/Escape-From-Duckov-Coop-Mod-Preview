# 场景投票未进图问题分析

## 问题描述
客机检测到两人就绪了，但还是没进图。主机已经开始加载场景，但客机连接断开了。

## 日志分析

### 客户端日志 (logs_client.txt)
时间: 23:27:12

**正常的投票流程**:
1. ✅ 收到SceneVote消息
2. ✅ 识别到2个参与者: Host和Client
3. ✅ 两人都是ready=True
4. ✅ 客户端多次收到JSON投票消息
5. ❌ **但是连接断开了**: `断开连接: 192.168.123.1:9050, 原因: RemoteConnectionClose`
6. ❌ **没有收到SCENE_BEGIN_LOAD消息**

### 主机日志 (logs_host.txt)
时间: 23:27:24-23:27:26

**异常情况**:
1. ❌ **完全没有SceneVote相关的日志**
2. ✅ 主机已经在加载场景: `加载场景：Level_GroundZero_Main`
3. ✅ 主机加载了加载屏幕: `加载场景：LoadingScreen_Getout`
4. ❌ 主机日志中没有任何投票、准备、广播相关的日志

## 核心问题

### 问题1: 主机端SceneVote日志完全缺失

**现象**: 
- 客户端日志中有大量 `[SceneVote]` 日志
- 主机日志中完全没有 `[SceneVote]` 日志
- SceneVoteMessage.cs 中有大量 Debug.Log 语句

**可能原因**:
1. 主机端的日志级别被过滤了（但其他日志正常输出）
2. 主机端根本没有执行SceneVote相关代码
3. 主机端使用了不同的投票机制（旧版本代码？）

### 问题2: 客户端连接断开

**现象**:
```
[23:27:12] [INFO] 断开连接: 192.168.123.1:9050, 原因: RemoteConnectionClose
[23:27:12] [INFO] [Patch_OnPeerDisconnected] LiteNetLib断开: 192.168.123.1:9050, 原因: RemoteConnectionClose
```

**时间线**:
- 23:27:12 - 客户端收到投票消息，两人就绪
- 23:27:12 - 客户端连接断开
- 23:27:24 - 主机开始加载场景（12秒后）

**可能原因**:
1. 主机在检测到全员就绪后，立即开始加载场景
2. 主机在加载场景时，没有发送SCENE_BEGIN_LOAD消息给客户端
3. 主机直接断开了客户端连接（可能是踢出逻辑？）
4. 网络问题导致连接中断

### 问题3: 缺少SteamID导致踢出？

在SceneVoteMessage.cs中有这段代码：

```csharp
// 检查是否有玩家缺少 SteamID（仅在 Steam P2P 模式下）
if (service.TransportMode == NetworkTransportMode.SteamP2P)
{
    var playersToKick = new List<string>();
    foreach (var player in _hostVoteState.playerList.items)
    {
        if (string.IsNullOrEmpty(player.steamId))
        {
            Debug.LogWarning(
                $"[SceneVote] 玩家 {player.playerName}({player.playerId}) 缺少SteamID，准备踢出"
            );
            playersToKick.Add(player.playerId);
        }
    }
    
    if (playersToKick.Count > 0)
    {
        // 踢出玩家
        peer.Disconnect();
    }
}
```

**客户端日志显示**:
```
[SceneVote] 解析玩家: name='Client', id='192.168.137.30:52945', steamId='', ready=True
```

客户端的steamId是空的！如果使用Steam P2P模式，客户端会被踢出。

## 调试建议

### 1. 确认主机端是否执行了SceneVote代码

在 `SceneVoteMessage.cs` 的关键位置添加更明显的日志：

```csharp
public static void Host_StartVote(...)
{
    // 在方法开头添加
    Debug.LogError("[SceneVote] ========== 主机发起投票 ==========");
    
    // ... 现有代码 ...
}

public static void Host_HandleReadyToggle(string playerId, bool ready)
{
    // 在方法开头添加
    Debug.LogError($"[SceneVote] ========== 主机处理准备状态: {playerId} = {ready} ==========");
    
    // ... 现有代码 ...
}

public static void Host_StartSceneLoad()
{
    // 在方法开头添加
    Debug.LogError("[SceneVote] ========== 主机开始加载场景 ==========");
    
    // ... 现有代码 ...
}
```

使用 `Debug.LogError` 而不是 `Debug.Log`，确保日志一定会输出。

### 2. 检查是否使用Steam P2P模式

添加日志输出当前的传输模式：

```csharp
public static void Host_StartVote(...)
{
    var service = NetService.Instance;
    Debug.LogError($"[SceneVote] 传输模式: {service.TransportMode}");
    
    // ... 现有代码 ...
}
```

### 3. 检查SteamID踢出逻辑

在踢出逻辑前添加日志：

```csharp
if (service.TransportMode == NetworkTransportMode.SteamP2P)
{
    Debug.LogError("[SceneVote] 检查SteamID（Steam P2P模式）");
    
    var playersToKick = new List<string>();
    foreach (var player in _hostVoteState.playerList.items)
    {
        if (string.IsNullOrEmpty(player.steamId))
        {
            Debug.LogError(
                $"[SceneVote] ⚠️⚠️⚠️ 玩家缺少SteamID，准备踢出: {player.playerName}({player.playerId})"
            );
            playersToKick.Add(player.playerId);
        }
    }
    
    if (playersToKick.Count > 0)
    {
        Debug.LogError($"[SceneVote] ⚠️⚠️⚠️ 即将踢出 {playersToKick.Count} 个玩家");
        // ... 踢出逻辑 ...
    }
}
else
{
    Debug.LogError("[SceneVote] 直连模式，跳过SteamID检查");
}
```

### 4. 检查SCENE_BEGIN_LOAD是否发送

在 `Host_StartSceneLoad()` 中添加日志：

```csharp
private static void Host_StartSceneLoad()
{
    Debug.LogError("[SceneVote] ========== 开始加载场景 ==========");
    
    // ... 现有代码 ...
    
    // 在发送SCENE_BEGIN_LOAD前
    Debug.LogError($"[SceneVote] 准备发送SCENE_BEGIN_LOAD给 {connectedPeers} 个客户端");
    
    // 发送后
    Debug.LogError("[SceneVote] ✓ 已发送SCENE_BEGIN_LOAD");
}
```

## 临时解决方案

### 方案1: 禁用SteamID检查

如果问题是由SteamID检查导致的，可以临时禁用：

```csharp
// 注释掉SteamID检查逻辑
/*
if (service.TransportMode == NetworkTransportMode.SteamP2P)
{
    // ... SteamID检查和踢出逻辑 ...
}
*/
```

### 方案2: 延迟场景加载

在检测到全员就绪后，延迟几秒再加载场景，给客户端更多时间准备：

```csharp
if (allReady)
{
    Debug.Log("[SceneVote] 全员准备，3秒后开始加载场景");
    // 使用协程延迟3秒
    ModBehaviourF.Instance.StartCoroutine(DelayedSceneLoad(3f));
}

private static IEnumerator DelayedSceneLoad(float delay)
{
    yield return new WaitForSeconds(delay);
    Host_StartSceneLoad();
}
```

### 方案3: 确保发送SCENE_BEGIN_LOAD

检查 `Host_StartSceneLoad()` 是否正确发送了消息给所有客户端。

## 下一步行动

1. **添加Debug.LogError日志**：在所有关键位置使用LogError确保日志输出
2. **重新测试**：运行游戏并收集新的日志
3. **分析新日志**：确认主机端是否执行了SceneVote代码
4. **定位问题**：根据新日志确定是SteamID问题、消息发送问题还是其他问题

## 总结

当前最可疑的问题：
1. **主机端SceneVote日志完全缺失** - 需要确认代码是否执行
2. **客户端缺少SteamID** - 可能导致被踢出
3. **客户端连接断开** - 发生在收到投票消息后
4. **主机没有发送SCENE_BEGIN_LOAD** - 或者发送失败

建议先添加更明显的日志（LogError），然后重新测试。
