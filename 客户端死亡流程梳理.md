# 客户端死亡流程完整梳理

## 📋 概述

本文档详细梳理客户端玩家死亡时的完整代码执行流程，从死亡触发到战利品箱生成的全过程。

---

## 🔄 完整流程图

```
客户端玩家血量归零
    ↓
Health.Hurt() 触发死亡判定
    ↓
Health.OnDeadEvent?.Invoke(damageInfo)
    ↓
CharacterMainControl.OnDead(damageInfo)
    ↓
[Patch_Client_OnDead_ReportCorpseTree] 拦截
    ↓
检查是否已上报 (_cliCorpseTreeReported)
    ↓
标记 DeadLootSpawnContext.InOnDead
    ↓
Net_ReportPlayerDeadTree() 上报装备树
    ↓
设置 _cliCorpseTreeReported = true
    ↓
主机接收 Op.PLAYER_DEAD_TREE (173)
    ↓
Server_HandlePlayerDeadTree() 处理
    ↓
生成战利品箱 + 广播 Op.DEAD_LOOT_SPAWN (248)
    ↓
客户端接收并生成战利品箱
    ↓
✅ 死亡流程完成
```

---

## 📝 详细代码流程

### 1️⃣ 死亡触发阶段

#### **位置：** `CharacterMainControlPatch.cs`

```csharp
[HarmonyPatch(typeof(CharacterMainControl), "OnDead")]
internal static class Patch_Client_OnDead_ReportCorpseTree
{
    private static void Postfix(CharacterMainControl __instance)
    {
        var mod = ModBehaviourF.Instance;
        if (mod == null || !mod.networkStarted) return;

        // ⭐ 仅"客户端 + 本机玩家"分支上报
        if (mod.IsServer) return;
        if (__instance != CharacterMainControl.Main) return;

        // ⭐ 已经上报过（= 主机已经/可以生成过尸体战利品），直接跳过
        if (LocalPlayerManager.Instance._cliCorpseTreeReported) return;

        try
        {
            // 给客户端的 CreateFromItem 拦截补丁一个"正在死亡路径"的标记
            // 避免本地也生成（双生）
            DeadLootSpawnContext.InOnDead = __instance;

            // 🔑 首次上报整棵"尸体装备树"给主机
            SendLocalPlayerStatus.Instance.Net_ReportPlayerDeadTree(__instance);

            // ✅ 标记"本轮生命已经上报过尸体树"
            LocalPlayerManager.Instance._cliCorpseTreeReported = true;
        }
        finally
        {
            DeadLootSpawnContext.InOnDead = null;
        }
    }
}
```

**关键点：**
- 只在**客户端**且是**本机玩家**时触发
- 使用 `_cliCorpseTreeReported` 防止重复上报
- 设置 `DeadLootSpawnContext.InOnDead` 标记防止本地生成物品

---

### 2️⃣ 装备树上报阶段

#### **位置：** `SendLocalPlayerStatus.cs`

```csharp
public void Net_ReportPlayerDeadTree(CharacterMainControl who)
{
    // 仅客户端上报；主机不需要发
    if (!networkStarted || IsServer || connectedPeer == null || who == null) 
        return;

    var item = who.CharacterItem; // 本机一定能拿到
    if (item == null) return;

    // 尸体位置/朝向尽量贴近角色模型
    var pos = who.transform.position;
    var rot = who.characterModel 
        ? who.characterModel.transform.rotation 
        : who.transform.rotation;

    // 组包并发送
    writer.Reset();
    writer.Put((byte)Op.PLAYER_DEAD_TREE);  // 操作码 173
    writer.PutV3cm(pos);                     // 位置（厘米精度）
    writer.PutQuaternion(rot);               // 旋转

    // 🔑 把整棵物品"快照"写进包里
    ItemTool.WriteItemSnapshot(writer, item);

    // 使用智能发送（Critical → ReliableOrdered）
    connectedPeer.SendSmart(writer, Op.PLAYER_DEAD_TREE);
}
```

**数据包内容：**
1. **操作码：** `Op.PLAYER_DEAD_TREE` (173)
2. **位置：** Vector3（厘米精度压缩）
3. **旋转：** Quaternion
4. **装备树快照：** 完整的物品树结构（递归序列化）

**物品快照包含：**
- 物品 TypeID
- 数量、耐久度
- 所有插槽内的嵌套物品（配件、弹药等）
- 递归处理所有子物品

---

### 3️⃣ 主机接收处理阶段

#### **位置：** `HostHandle.cs`

```csharp
public void Server_HandlePlayerDeadTree(Vector3 pos, Quaternion rot, ItemSnapshot snap)
{
    if (!IsServer) return;

    // 🔧 从快照重建物品树
    var tmpRoot = ItemTool.BuildItemFromSnapshot(snap);
    if (!tmpRoot)
    {
        Debug.LogWarning("[LOOT] HostDeath BuildItemFromSnapshot failed.");
        return;
    }

    // 🪦 获取墓碑预制体（玩家专用）
    var deadPfb = LootManager.Instance.ResolveDeadLootPrefabOnServer(); 
    // → LootBoxPrefab_Tomb

    // 🎁 从物品树创建战利品箱
    var box = InteractableLootbox.CreateFromItem(
        tmpRoot, 
        pos + Vector3.up * 0.10f,  // 稍微抬高避免穿地
        rot, 
        true,                       // 是死亡战利品
        deadPfb                     // 墓碑预制体
    );

    // 📡 广播战利品箱生成事件
    if (box) 
        DeadLootBox.Instance.Server_OnDeadLootboxSpawned(
            box, 
            null  // whoDied=null → aiId=0 → 客户端识别为"玩家墓碑"
        );

    // 🗑️ 清理临时物品
    if (tmpRoot && tmpRoot.gameObject) 
        Object.Destroy(tmpRoot.gameObject);
}
```

**关键步骤：**
1. **重建物品树：** 从快照完整还原所有装备和嵌套物品
2. **创建战利品箱：** 使用 `InteractableLootbox.CreateFromItem()`
3. **广播生成事件：** 通知所有客户端生成对应的战利品箱
4. **清理临时对象：** 销毁用于创建的临时物品

---

### 4️⃣ 战利品箱广播阶段

#### **位置：** `DeadLootBox.cs`

```csharp
public void Server_OnDeadLootboxSpawned(InteractableLootbox box, CharacterMainControl whoDied)
{
    if (!IsServer || box == null) return;
    
    try
    {
        // 🆔 生成稳定的全局唯一ID
        var lootUid = LootManager.Instance._nextLootUid++;
        var inv = box.Inventory;
        if (inv) LootManager.Instance._srvLootByUid[lootUid] = inv;

        // 🔍 获取死者的 aiId（玩家死亡时为 0）
        var aiId = 0;
        if (whoDied)
        {
            var tag = whoDied.GetComponent<NetAiTag>();
            if (tag != null) aiId = tag.aiId;
            if (aiId == 0)
                foreach (var kv in AITool.aiById)
                    if (kv.Value == whoDied)
                    {
                        aiId = kv.Key;
                        break;
                    }
        }

        // 🔒 设置箱子为"未鉴定"状态
        if (inv != null)
        {
            inv.NeedInspection = true;
            
            // 清空"已搜索"标记
            try
            {
                Traverse.Create(inv)
                    .Field<bool>("hasBeenInspectedInLootBox")
                    .Value = false;
            }
            catch { }

            // 把所有物品标记为"未鉴定"
            for (var i = 0; i < inv.Content.Count; ++i)
            {
                var it = inv.GetItemAt(i);
                if (it) it.Inspected = false;
            }
        }

        // 📦 组包广播
        writer.Reset();
        writer.Put((byte)Op.DEAD_LOOT_SPAWN);  // 操作码 248
        writer.Put(SceneManager.GetActiveScene().buildIndex);
        writer.Put(aiId);                       // 0 = 玩家墓碑
        writer.Put(lootUid);                    // 稳定ID
        writer.PutV3cm(box.transform.position);
        writer.PutQuaternion(box.transform.rotation);
        
        // 📡 智能广播（Important → ReliableSequenced）
        netManager.SendSmart(writer, Op.DEAD_LOOT_SPAWN);

        // 🔇 抑制填充期间的广播（避免重复同步）
        if (inv) LootManager.Instance.Server_MuteLoot(inv, 2.0f);
    }
    catch (Exception e)
    {
        Debug.LogError("[LOOT] Server_OnDeadLootboxSpawned failed: " + e);
    }
}
```

**广播数据包内容：**
1. **操作码：** `Op.DEAD_LOOT_SPAWN` (248)
2. **场景索引：** buildIndex
3. **死者ID：** aiId（玩家为 0）
4. **战利品箱ID：** lootUid（全局唯一）
5. **位置：** Vector3（厘米精度）
6. **旋转：** Quaternion

---

### 5️⃣ 客户端生成战利品箱阶段

#### **位置：** `DeadLootBox.cs`

```csharp
public void SpawnDeadLootboxAt(int aiId, int lootUid, Vector3 pos, Quaternion rot)
{
    try
    {
        // 🗑️ 移除附近的AI尸体模型（避免视觉重复）
        AITool.TryClientRemoveNearestAICorpse(pos, 3.0f);

        // 🎨 获取正确的战利品箱预制体
        var prefab = GetDeadLootPrefabOnClient(aiId);
        if (!prefab)
        {
            Debug.LogWarning("[LOOT] DeadLoot prefab not found, spawn aborted.");
            return;
        }

        // 🏗️ 实例化战利品箱
        var go = Instantiate(prefab, pos, rot);
        var box = go ? go.GetComponent<InteractableLootbox>() : null;
        if (!box) return;

        var inv = box.Inventory;
        if (!inv)
        {
            Debug.LogWarning("[Client DeadLootBox Spawn] Inventory is null!");
            return;
        }

        // 🔧 初始化客户端战利品箱
        WorldLootPrime.PrimeIfClient(box);

        // 📍 注册位置索引（用于查找）
        var dict = InteractableLootbox.Inventories;
        if (dict != null)
        {
            var correctKey = LootManager.ComputeLootKeyFromPos(pos);
            var wrongKey = -1;
            
            // 清理错误的旧索引
            foreach (var kv in dict)
                if (kv.Value == inv && kv.Key != correctKey)
                {
                    wrongKey = kv.Key;
                    break;
                }

            if (wrongKey != -1) dict.Remove(wrongKey);
            dict[correctKey] = inv;
        }

        // 🆔 注册稳定ID索引
        if (lootUid >= 0) 
            LootManager.Instance._cliLootByUid[lootUid] = inv;

        // 📦 检查是否有缓存的快照数据
        if (lootUid >= 0 && 
            LootManager.Instance._pendingLootStatesByUid.TryGetValue(lootUid, out var pack))
        {
            // 🎯 快照先到达，直接应用缓存
            LootManager.Instance._pendingLootStatesByUid.Remove(lootUid);

            COOPManager.LootNet._applyingLootState = true;
            try
            {
                var cap = Mathf.Clamp(pack.capacity, 1, 128);
                inv.Loading = true; // ★ 进入批量模式
                inv.SetCapacity(cap);

                // 清空现有物品
                for (var i = inv.Content.Count - 1; i >= 0; --i)
                {
                    Item removed;
                    inv.RemoveAt(i, out removed);
                    try
                    {
                        if (removed) Destroy(removed.gameObject);
                    }
                    catch { }
                }

                // 根据快照重建物品
                foreach (var (p, snap) in pack.Item2)
                {
                    var item = ItemTool.BuildItemFromSnapshot(snap);
                    if (item) inv.AddAt(item, p);
                }
            }
            finally
            {
                inv.Loading = false; // ★ 结束批量模式
                COOPManager.LootNet._applyingLootState = false;
            }

            WorldLootPrime.PrimeIfClient(box);
            return; // 吃完缓存就不再发请求
        }

        // 📨 正常路径：请求战利品箱内容
        COOPManager.LootNet.Client_RequestLootState(inv);
        
        // ⏱️ 超时兜底（1.5秒后清除Loading状态）
        StartCoroutine(LootManager.Instance.ClearLootLoadingTimeout(inv, 1.5f));
    }
    catch (Exception e)
    {
        Debug.LogError("[LOOT] SpawnDeadLootboxAt failed: " + e);
    }
}
```

**预制体选择逻辑：**
```csharp
private GameObject GetDeadLootPrefabOnClient(int aiId)
{
    // 1️⃣ 首选：死亡CMC上的 deadLootBoxPrefab
    if (aiId > 0 && AITool.aiById.TryGetValue(aiId, out var cmc) && cmc)
    {
        if (cmc.deadLootBoxPrefab?.gameObject != null)
            return cmc.deadLootBoxPrefab.gameObject;
    }

    // 2️⃣ 兜底：本地玩家的 deadLootBoxPrefab
    var main = CharacterMainControl.Main;
    if (main?.deadLootBoxPrefab?.gameObject != null)
        return main.deadLootBoxPrefab.gameObject;

    // 3️⃣ 最后兜底：场景中任意CMC的 deadLootBoxPrefab
    var any = FindObjectOfType<CharacterMainControl>();
    if (any?.deadLootBoxPrefab?.gameObject != null)
        return any.deadLootBoxPrefab.gameObject;

    return null;
}
```

---

## 🔑 关键机制详解

### 1. 防重复上报机制

```csharp
// LocalPlayerManager.cs
internal bool _cliCorpseTreeReported;  // 本轮生命是否已上报

// 死亡时检查
if (LocalPlayerManager.Instance._cliCorpseTreeReported) return;

// 上报后标记
LocalPlayerManager.Instance._cliCorpseTreeReported = true;

// 复活时清空
if (cur > 1e-3f)  // 血量 > 0
{
    _cliSelfDeathFired = false;
    _cliCorpseTreeReported = false;  // ✅ 下一条命允许重新上报
    _cliInEnsureSelfDeathEmit = false;
    return;
}
```

---

### 2. 防本地生成机制

```csharp
// 死亡期间打标记
DeadLootSpawnContext.InOnDead = __instance;

// 在物品生成补丁中检查
if (DeadLootSpawnContext.InOnDead != null)
{
    // 阻止本地生成，避免双生
    return false;
}
```

---

### 3. 稳定ID系统

```csharp
// 主机生成全局唯一ID
var lootUid = LootManager.Instance._nextLootUid++;

// 主机注册
LootManager.Instance._srvLootByUid[lootUid] = inv;

// 客户端注册
LootManager.Instance._cliLootByUid[lootUid] = inv;
```

**作用：**
- 解决位置浮点误差导致的匹配失败
- 支持快照缓存机制
- 精确定位战利品箱

---

### 4. 快照缓存机制

```csharp
// 快照先到达时缓存
if (lootUid >= 0 && 
    LootManager.Instance._pendingLootStatesByUid.TryGetValue(lootUid, out var pack))
{
    // 直接应用缓存，不再发请求
    // ...
}
else
{
    // 正常请求
    COOPManager.LootNet.Client_RequestLootState(inv);
}
```

**处理场景：**
- 网络包乱序（快照比生成消息先到）
- 减少不必要的请求
- 提高同步效率

---

### 5. 补发死亡事件机制

```csharp
// LocalPlayerManager.cs
public void Client_EnsureSelfDeathEvent(Health h, CharacterMainControl cmc)
{
    if (!h || !cmc) return;

    var cur = 1f;
    try { cur = h.CurrentHealth; } catch { }

    // 血量 > 0：清空所有死亡标记
    if (cur > 1e-3f)
    {
        _cliSelfDeathFired = false;
        _cliCorpseTreeReported = false;
        _cliInEnsureSelfDeathEmit = false;
        return;
    }

    // 防重入：本轮只补发一次
    if (_cliSelfDeathFired) return;

    try
    {
        var di = new DamageInfo
        {
            isFromBuffOrEffect = false,
            damageValue = 0f,
            finalDamage = 0f,
            damagePoint = cmc.transform.position,
            damageNormal = Vector3.up,
            fromCharacter = null
        };

        // 标记：这次OnDead来源于"补发"
        _cliInEnsureSelfDeathEmit = true;

        // 🔑 补发死亡事件
        h.OnDeadEvent?.Invoke(di);

        _cliSelfDeathFired = true;
    }
    finally
    {
        _cliInEnsureSelfDeathEmit = false;
    }
}
```

**作用：**
- 处理血量归零但未触发 `OnDead` 的边界情况
- 确保死亡流程一定会执行
- 防止漏报导致的不同步

---

## 🛡️ 容错机制

### 1. 预制体缺失兜底
```csharp
// 三级查找确保总能找到可用的箱子模型
1. 死亡AI的 deadLootBoxPrefab
2. 本地玩家的 deadLootBoxPrefab
3. 场景中任意CMC的 deadLootBoxPrefab
```

### 2. 快照缓存处理
```csharp
// 处理网络包乱序
_pendingLootStatesByUid[lootUid] = pack;
```

### 3. 超时清理
```csharp
// 防止永久Loading
StartCoroutine(ClearLootLoadingTimeout(inv, 1.5f));
```

### 4. 异常吞噬
```csharp
try
{
    // 关键逻辑
}
catch (Exception e)
{
    Debug.LogError("[LOOT] Operation failed: " + e);
}
```

### 5. 幂等注册
```csharp
// 重复调用不会出错
if (dict.ContainsKey(key))
    dict[key] = value;
else
    dict.Add(key, value);
```

---

## 📊 数据流时序图

```
时间轴 | 客户端                    | 网络                | 主机
-------|--------------------------|---------------------|------------------
T0     | 玩家血量归零              |                     |
T1     | OnDead() 触发            |                     |
T2     | 检查 _cliCorpseTreeReported |                  |
T3     | 标记 DeadLootSpawnContext |                   |
T4     | 序列化装备树快照          |                     |
T5     | 发送 PLAYER_DEAD_TREE    | ───────────────>   |
T6     |                          |                     | 接收消息
T7     |                          |                     | 解析快照
T8     |                          |                     | 重建物品树
T9     |                          |                     | 创建战利品箱
T10    |                          |                     | 设置未鉴定状态
T11    |                          |                     | 生成 lootUid
T12    |                          |                     | 广播 DEAD_LOOT_SPAWN
T13    |                          | <───────────────   |
T14    | 接收生成消息              |                     |
T15    | 移除附近尸体              |                     |
T16    | 实例化战利品箱            |                     |
T17    | 注册索引                  |                     |
T18    | 检查快照缓存              |                     |
T19    | 请求/应用内容             |                     |
T20    | ✅ 战利品箱可见           |                     |
```

---

## 🎯 关键变量状态追踪

### 客户端状态变量

| 变量名 | 类型 | 作用 | 重置时机 |
|--------|------|------|----------|
| `_cliCorpseTreeReported` | bool | 本轮生命是否已上报尸体树 | 血量恢复时 |
| `_cliSelfDeathFired` | bool | 本轮是否已补发死亡事件 | 血量恢复时 |
| `_cliInEnsureSelfDeathEmit` | bool | 正在执行补发死亡 | 补发完成后 |
| `DeadLootSpawnContext.InOnDead` | CMC | 正在执行OnDead的角色 | OnDead结束后 |

### 主机状态变量

| 变量名 | 类型 | 作用 |
|--------|------|------|
| `_nextLootUid` | int | 下一个战利品箱的全局ID |
| `_srvLootByUid` | Dict | 主机侧战利品箱索引 |
| `_pendingLootStatesByUid` | Dict | 待应用的快照缓存 |

---

## 🔍 调试要点

### 1. 检查是否触发上报
```csharp
Debug.Log($"[Death] _cliCorpseTreeReported = {_cliCorpseTreeReported}");
```

### 2. 检查装备树是否完整
```csharp
Debug.Log($"[Death] CharacterItem = {who.CharacterItem?.name}");
Debug.Log($"[Death] Snapshot size = {writer.Length} bytes");
```

### 3. 检查主机是否接收
```csharp
Debug.Log($"[Host] Received PLAYER_DEAD_TREE from {peer.EndPoint}");
```

### 4. 检查战利品箱生成
```csharp
Debug.Log($"[Loot] Spawned box at {pos}, lootUid={lootUid}");
```

### 5. 检查客户端是否生成
```csharp
Debug.Log($"[Client] Spawning lootbox, aiId={aiId}, lootUid={lootUid}");
```

---

## ⚠️ 常见问题

### 问题1：客户端死亡后没有生成战利品箱

**可能原因：**
1. `_cliCorpseTreeReported` 已经为 true（重复死亡）
2. 网络连接断开
3. 主机未接收到消息
4. 预制体缺失

**排查方法：**
```csharp
// 在 Patch_Client_OnDead_ReportCorpseTree 中添加日志
Debug.Log($"[Death] IsServer={mod.IsServer}, " +
          $"IsMain={__instance == CharacterMainControl.Main}, " +
          $"Reported={LocalPlayerManager.Instance._cliCorpseTreeReported}");
```

---

### 问题2：战利品箱生成但内容为空

**可能原因：**
1. 快照序列化失败
2. 主机重建物品失败
3. 客户端未请求/接收内容

**排查方法：**
```csharp
// 检查快照大小
Debug.Log($"[Death] Snapshot bytes = {writer.Length}");

// 检查重建结果
Debug.Log($"[Host] Rebuilt item = {tmpRoot?.name}, " +
          $"slots = {tmpRoot?.Slots?.Count}");
```

---

### 问题3：战利品箱位置不正确

**可能原因：**
1. 位置序列化精度问题
2. 客户端地形差异
3. 坐标系转换错误

**排查方法：**
```csharp
// 对比发送和接收的位置
Debug.Log($"[Client] Sent pos = {pos}");
Debug.Log($"[Host] Received pos = {receivedPos}");
Debug.Log($"[Client] Spawned at = {box.transform.position}");
```

---

## 📌 总结

客户端死亡流程的核心设计思想：

1. **单次上报：** 使用 `_cliCorpseTreeReported` 确保每条命只上报一次
2. **完整快照：** 序列化整棵装备树，包括所有嵌套物品
3. **主机权威：** 由主机创建战利品箱并广播
4. **稳定ID：** 使用全局唯一ID解决匹配问题
5. **防重复生成：** 通过 `DeadLootSpawnContext` 标记阻止本地生成
6. **容错设计：** 多重兜底机制确保流程健壮

这套机制确保了客户端玩家死亡后，装备能正确转化为战利品箱并在所有客户端同步显示。
