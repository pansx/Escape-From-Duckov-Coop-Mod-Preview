# 撤离时主机卡死问题分析

## 问题描述

在撤离时，主机端卡死，只能强制退出游戏。

## 日志分析

### 主机端日志 (logs_host.txt)

主机端日志显示：
- 最后的时间戳：`13:14:59`
- 最后的操作：物品安装检测失败
- **没有任何撤离相关的日志**
- **没有场景切换的日志**
- **没有网络断开的日志**

这说明主机在撤离时**完全卡住**，连日志都没有输出。

### 客户端日志 (logs_client.txt)

客户端日志显示：
- 最后的时间戳：`13:18:07` （比主机晚约3分钟）
- 客户端成功进入了新场景（Level Initialization）
- 在场景切换时出现了**大量物品Slot错误**：

```
[13:17:02] [ERROR] 物品 一级防弹衣 被通知从Slot移除，但当前Slot 空 与通知Slot 躯壳/Armor 不匹配。
[13:17:02] [ERROR] 物品 装饰包 被通知从Slot移除，但当前Slot 空 与通知Slot 躯壳/Backpack 不匹配。
```

这些错误发生在 `UnloadGameScene` 阶段，说明场景卸载时物品系统出现了问题。

## 根本原因分析 🎯

### 撤离时的死锁机制

**问题根源**：撤离点触发的场景切换与投票系统的冲突

#### 场景1：主机先触发撤离点

1. **主机触发撤离点** → `SceneLoaderProxy.LoadScene` 被Patch拦截
2. **Patch调用** `Host_BeginSceneVote_Simple` → 发送投票消息给客户端
3. **主机的场景加载被阻止** (`return false`)
4. **主机等待客户端准备** → 检查 `sceneReady` 字典，等待全员 `ready = true`
5. **客户端收到投票消息** → 但客户端可能也在撤离点附近
6. **客户端也触发撤离点** → 客户端的 `SceneLoaderProxy.LoadScene` 也被拦截
7. **客户端发送投票请求** → `Client_RequestBeginSceneVote`
8. **死锁形成**：
   - 主机等待客户端点击"准备"按钮
   - 客户端等待主机响应投票请求
   - 双方都在等待对方，永远卡住

#### 场景2：客户端先触发撤离点

1. **客户端触发撤离点** → 发送 `SCENE_VOTE_REQ` 给主机
2. **主机收到请求** → 调用 `Host_BeginSceneVote_Simple`
3. **主机发送投票消息** → 客户端收到
4. **客户端显示投票UI** → 等待玩家点击"准备"
5. **主机也触发撤离点** → 再次调用 `Host_BeginSceneVote_Simple`
6. **投票状态被重置** → `sceneReady.Clear()`
7. **之前的投票进度丢失** → 客户端的"准备"状态被清空
8. **死锁形成**：主机等待客户端准备，但客户端已经准备过了

### 关键代码缺陷

#### 1. 没有撤离点的特殊处理

撤离点应该**直接切换场景**，而不是发起投票：

```csharp
// 当前代码（有问题）
if (mod.IsServer)
{
    SceneNet.Instance.Host_BeginSceneVote_Simple(...); // 发起投票
    return false; // 阻止原始加载
}
```

**应该改为**：
```csharp
if (mod.IsServer)
{
    // 检查是否是撤离场景
    if (notifyEvac) // 撤离标志
    {
        // 撤离不需要投票，直接切换
        SceneNet.Instance.Server_BroadcastBeginSceneLoad();
        return false;
    }
    else
    {
        // 普通场景切换才需要投票
        SceneNet.Instance.Host_BeginSceneVote_Simple(...);
        return false;
    }
}
```

#### 2. 投票系统没有防重入保护

```csharp
public void Host_BeginSceneVote_Simple(...)
{
    // ❌ 没有检查是否已经在投票中
    sceneVoteActive = true;
    sceneReady.Clear(); // 直接清空，丢失之前的进度
}
```

**应该改为**：
```csharp
public void Host_BeginSceneVote_Simple(...)
{
    // ✅ 如果已经在投票同一个场景，忽略重复请求
    if (sceneVoteActive && sceneTargetId == targetSceneId)
    {
        Debug.LogWarning($"[SCENE] 已经在投票 {targetSceneId}，忽略重复请求");
        return;
    }
    
    sceneVoteActive = true;
    sceneReady.Clear();
}
```

#### 3. 没有超时机制

主机等待客户端准备时，**没有超时机制**：

```csharp
public void Server_OnSceneReadySet(NetPeer fromPeer, bool ready)
{
    // 检查是否全员准备
    foreach (var id in sceneParticipantIds)
        if (!sceneReady.TryGetValue(id, out var r) || !r)
            return; // ❌ 如果有人不准备，永远等待
    
    // 全员就绪 → 开始加载
    Server_BroadcastBeginSceneLoad();
}
```

**应该添加超时**：
```csharp
// 在 Host_BeginSceneVote_Simple 中启动超时计时器
private float _voteStartTime;

public void Host_BeginSceneVote_Simple(...)
{
    _voteStartTime = Time.realtimeSinceStartup;
    // ...
}

// 在 Update 中检查超时
void Update()
{
    if (sceneVoteActive && Time.realtimeSinceStartup - _voteStartTime > 30f)
    {
        Debug.LogWarning("[SCENE] 投票超时（30秒），强制开始加载");
        Server_BroadcastBeginSceneLoad();
    }
}
```

## 代码分析结果

### 1. Host_BeginSceneVote_Simple 的实现 ✅

已查看完整实现（`SceneNet.cs:732-832`），关键发现：

```csharp
public void Host_BeginSceneVote_Simple(...)
{
    // 1. 重置场景门控状态
    _srvSceneGateOpen = false;
    _srvGateReadyPids.Clear();
    
    // 2. 使用 JSON 投票系统发送投票消息
    SceneVoteMessage.Host_StartVote(...);
    
    // 3. 初始化投票状态
    sceneVoteActive = true;
    localReady = false;
    sceneReady.Clear();
    
    // ❌ 问题：没有自动开始加载！
    // 主机需要等待所有玩家准备，但如果客户端已经开始切换场景，
    // 主机还在等待，就会形成死锁
}
```

**关键问题**：
- 主机调用此方法后，**不会自动开始场景加载**
- 需要等待 `Server_OnSceneReadySet` 检测到全员准备后，才调用 `Server_BroadcastBeginSceneLoad`
- 如果客户端已经开始切换场景（触发了撤离点），但主机还在等待，就会卡死

### 2. 场景切换的完整流程 ✅

**正常流程**：
1. 主机触发撤离 → `Host_BeginSceneVote_Simple` → 发送 JSON 投票消息
2. 客户端收到投票 → 显示投票UI → 玩家点击"准备"
3. 客户端发送 `SCENE_READY_SET` → 主机收到
4. 主机检查全员准备 → `Server_BroadcastBeginSceneLoad` → 双方开始加载

**撤离时的异常流程**：
1. 主机触发撤离点 → `SceneLoaderProxy.LoadScene` 被Patch拦截
2. Patch调用 `Host_BeginSceneVote_Simple` → 发送投票消息
3. **主机的场景加载被 `return false` 阻止**
4. 客户端可能也触发了撤离点 → 开始切换场景
5. 主机还在等待客户端"准备"，但客户端已经在新场景了
6. **死锁**：主机等客户端准备，客户端等主机放行

### 3. 场景门控机制 ✅

查看了 `Server_SceneGateAsync` 和 `Client_SceneGateAsync`：

**主机端**：
```csharp
public async UniTask Server_SceneGateAsync()
{
    _srvSceneGateOpen = true; // 立即开门
    
    // 放行所有已举手的客户端
    foreach (var kv in playerStatuses)
    {
        if (_srvGateReadyPids.Contains(st.EndPoint))
        {
            Server_SendGateRelease(peer, _srvGateSid);
        }
    }
}
```

**客户端**：
```csharp
public async UniTask Client_SceneGateAsync()
{
    // 发送举手消息
    writer.Put((byte)Op.SCENE_GATE_READY);
    connectedPeer.SendSmart(writer, Op.SCENE_GATE_READY);
    
    // 等待主机放行（60秒超时）
    _cliGateDeadline = Time.realtimeSinceStartup + 60f;
    while (!_cliSceneGateReleased && Time.realtimeSinceStartup < _cliGateDeadline)
    {
        await UniTask.Delay(100);
    }
}
```

**问题**：
- 客户端有60秒超时，但主机**没有超时机制**
- 如果主机在等待投票时卡住，客户端会超时继续，但主机永远卡在投票等待中

### 4. 物品系统的清理顺序

客户端日志显示的错误：
```
[13:17:02] [ERROR] 物品 一级防弹衣 被通知从Slot移除，但当前Slot 空 与通知Slot 躯壳/Armor 不匹配。
```

这发生在 `UnloadGameScene` 阶段，说明：
- 场景卸载时，物品系统的清理顺序有问题
- 可能是 `SlotPatch` 影响了清理逻辑
- 但这不是主机卡死的直接原因，只是副作用

## 修复方案

### 方案1：撤离场景跳过投票系统 ⭐⭐⭐⭐⭐（推荐）

**原理**：撤离是单向操作，不需要投票，应该直接切换场景。

**修改位置**：`EscapeFromDuckovCoopMod/Patch/Scene/ScenePatch.cs`

```csharp
[HarmonyPatch(typeof(SceneLoaderProxy), "LoadScene")]
public static class Patch_SceneLoaderProxy_Authority
{
    private static bool Prefix(SceneLoaderProxy __instance)
    {
        var mod = ModBehaviourF.Instance;
        if (mod == null || !mod.networkStarted) return true;
        if (SceneNet.Instance.allowLocalSceneLoad) return true;

        // 获取参数
        var proxySceneId = Traverse.Create(__instance).Field<string>("sceneID").Value;
        var notifyEvac = Traverse.Create(__instance).Field<bool>("notifyEvacuation").Value;
        // ... 其他参数

        if (mod.IsServer)
        {
            // ✅ 撤离场景直接切换，不发起投票
            if (notifyEvac)
            {
                Debug.Log($"[SCENE] 撤离场景，跳过投票，直接切换: {targetId}");
                SceneNet.Instance.sceneTargetId = targetId;
                SceneNet.Instance.sceneCurtainGuid = curtainGuid;
                SceneNet.Instance.sceneNotifyEvac = notifyEvac;
                SceneNet.Instance.sceneSaveToFile = save;
                SceneNet.Instance.sceneUseLocation = useLoc;
                SceneNet.Instance.sceneLocationName = locationName;
                
                // 直接广播开始加载
                SceneNet.Instance.Server_BroadcastBeginSceneLoad();
                return false;
            }
            
            // 普通场景切换才需要投票
            SceneNet.Instance.Host_BeginSceneVote_Simple(targetId, curtainGuid, notifyEvac, save, useLoc, locationName);
            return false;
        }

        // 客户端逻辑保持不变
        SceneNet.Instance.Client_RequestBeginSceneVote(targetId, curtainGuid, notifyEvac, save, useLoc, locationName);
        return false;
    }
}
```

**优点**：
- ✅ 彻底解决撤离卡死问题
- ✅ 符合撤离的语义（单向操作）
- ✅ 不影响普通场景切换的投票系统

**缺点**：
- ⚠️ 客户端可能还在战斗中就被强制撤离

### 方案2：添加投票超时机制 ⭐⭐⭐⭐

**原理**：如果投票长时间没有全员准备，自动开始加载。

**修改位置**：`EscapeFromDuckovCoopMod/Main/SceneService/SceneNet.cs`

```csharp
public class SceneNet : MonoBehaviour
{
    private float _voteStartTime;
    private const float VOTE_TIMEOUT_SECONDS = 30f;
    
    public void Host_BeginSceneVote_Simple(...)
    {
        // 记录投票开始时间
        _voteStartTime = Time.realtimeSinceStartup;
        
        // 原有逻辑...
    }
    
    void Update()
    {
        // 检查投票超时
        if (IsServer && sceneVoteActive)
        {
            if (Time.realtimeSinceStartup - _voteStartTime > VOTE_TIMEOUT_SECONDS)
            {
                Debug.LogWarning($"[SCENE] 投票超时（{VOTE_TIMEOUT_SECONDS}秒），强制开始加载");
                Server_BroadcastBeginSceneLoad();
            }
        }
    }
}
```

**优点**：
- ✅ 防止永久卡死
- ✅ 保留投票系统的功能

**缺点**：
- ⚠️ 30秒等待时间较长
- ⚠️ 没有解决根本问题（重复投票）

### 方案3：投票防重入保护 ⭐⭐⭐

**原理**：如果已经在投票同一个场景，忽略重复请求。

**修改位置**：`EscapeFromDuckovCoopMod/Main/SceneService/SceneNet.cs`

```csharp
public void Host_BeginSceneVote_Simple(...)
{
    // ✅ 防重入：如果已经在投票同一个场景，忽略
    if (sceneVoteActive && sceneTargetId == targetSceneId)
    {
        Debug.LogWarning($"[SCENE] 已经在投票 {targetSceneId}，忽略重复请求");
        return;
    }
    
    // 原有逻辑...
}
```

**优点**：
- ✅ 防止投票状态被重置
- ✅ 简单易实现

**缺点**：
- ⚠️ 没有解决撤离点的特殊性
- ⚠️ 如果第一次投票失败，无法重新发起

### 综合方案（推荐）⭐⭐⭐⭐⭐

结合方案1、2、3：

1. **撤离场景跳过投票**（方案1）
2. **投票添加超时机制**（方案2）
3. **投票添加防重入保护**（方案3）

这样可以：
- 撤离场景直接切换，不会卡死
- 普通场景投票有超时保护
- 防止重复投票导致状态混乱

## 下一步行动

1. **实现方案1**：修改 `ScenePatch.cs`，让撤离场景跳过投票
2. **编译测试**：验证撤离是否正常工作
3. **实现方案2和3**：添加超时和防重入保护
4. **全面测试**：测试各种场景切换情况

## 临时解决方案

在修复之前，建议：
1. **撤离前断开连接**：主机点击"关闭主机"，客户端点击"离开房间"
2. **各自单机撤离**：断开后各自触发撤离点
3. **避免同时撤离**：一个人先撤离，另一个人等待
