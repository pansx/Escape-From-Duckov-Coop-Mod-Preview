# 场景投票问题 - 已修复

## 问题描述
客机检测到两人就绪后，连接被断开，无法进入场景。

## 问题原因
通过日志分析发现，主机在检测到全员准备后，会检查所有玩家的SteamID。如果玩家缺少SteamID，主机会将其踢出。

**主机日志显示**:
```
[23:32:35] [WARNING] [SceneVote] 玩家 Client(192.168.137.30:64393) 缺少SteamID，准备踢出
[23:32:35] [WARNING] [SceneVote] 发现 1 个玩家缺少SteamID，开始踢出
[23:32:35] [WARNING] [SceneVote] 踢出玩家: Client(192.168.137.30:64393)
[23:32:35] [INFO] [SceneVote] 全员准备，开始加载场景
```

**问题代码**:
在 `SceneVoteMessage.cs` 的 `Host_StartSceneLoad()` 方法中，SteamID检查逻辑在**所有模式下**都会执行，而不是只在Steam P2P模式下执行。

```csharp
// 原代码 - 错误
if (
    service != null
    && service.IsServer
    && SteamManager.Initialized  // ⚠️ 只检查Steam是否初始化，没有检查传输模式
    && _hostVoteState.playerList != null
    && _hostVoteState.playerList.items != null
)
{
    // 检查并踢出没有SteamID的玩家
}
```

## 修复方案

### 修改内容
在SteamID检查条件中添加传输模式判断，只在Steam P2P模式下才检查SteamID。

**修改文件**: `EscapeFromDuckovCoopMod/Net/SceneVoteMessage.cs`

**修改位置**: `Host_StartSceneLoad()` 方法

**修改代码**:
```csharp
// 修复后的代码
if (
    service != null
    && service.IsServer
    && service.TransportMode == NetworkTransportMode.SteamP2P  // ✅ 添加传输模式检查
    && SteamManager.Initialized
    && _hostVoteState.playerList != null
    && _hostVoteState.playerList.items != null
)
{
    // 检查并踢出没有SteamID的玩家
}
else if (service != null && service.IsServer)
{
    Debug.Log($"[SceneVote] 传输模式: {service.TransportMode}, 跳过SteamID检查");
}
```

### 修复效果
- ✅ 直连模式下不再检查SteamID
- ✅ 直连模式下玩家不会被踢出
- ✅ Steam P2P模式下仍然会检查SteamID（保持原有安全性）
- ✅ 添加日志输出当前传输模式

## 测试建议

### 测试场景1: 直连模式
1. 主机启动游戏（直连模式）
2. 客机连接到主机
3. 主机发起场景投票
4. 双方按J键准备
5. **预期结果**: 双方都能正常进入场景

### 测试场景2: Steam P2P模式
1. 主机启动游戏（Steam P2P模式）
2. 客机通过Steam连接
3. 主机发起场景投票
4. 双方按J键准备
5. **预期结果**: 
   - 如果双方都有SteamID，正常进入场景
   - 如果客机缺少SteamID，会被踢出（这是预期行为）

## 相关日志

### 修复前的日志
**主机端**:
```
[SceneVote] 玩家 Client(192.168.137.30:64393) 缺少SteamID，准备踢出
[SceneVote] 发现 1 个玩家缺少SteamID，开始踢出
[SceneVote] 踢出玩家: Client(192.168.137.30:64393)
[SceneVote] 全员准备，开始加载场景
```

**客户端**:
```
[SceneVote] 收到 2 个玩家信息: Host(Host:9050), Client(192.168.137.30:64393)
[SceneVote] 解析玩家: name='Client', id='192.168.137.30:64393', steamId='', ready=True
[SceneVote] 解析玩家: name='Host', id='Host:9050', steamId='76561198101474180', ready=True
断开连接: 192.168.123.1:9050, 原因: RemoteConnectionClose
```

### 修复后的预期日志
**主机端（直连模式）**:
```
[SceneVote] 传输模式: Direct, 跳过SteamID检查
[SceneVote] 全员准备，开始加载场景
```

**主机端（Steam P2P模式）**:
```
[SceneVote] 玩家 Client(xxx) 缺少SteamID，准备踢出
[SceneVote] 发现 1 个玩家缺少SteamID，开始踢出
[SceneVote] 踢出玩家: Client(xxx)
```

## 其他改进

### HP调试日志
同时添加了HP同步的调试日志，用于追踪血量同步问题：
- `[HP_REPORT_INIT]` - 客户端初始血量上报
- `[HP_REPORT]` - 客户端血量变化上报
- `[HP_RECEIVE]` - 主机接收血量上报
- `[HP_AUTH_SELF]` - 客户端接收权威血量
- `[HP_BOUNCEBACK]` - 防回弹机制触发

详见：
- `客户端HP回报机制分析.md`
- `HP血量调试日志_已添加.md`
- `HP血量调试_完成总结.md`

## 提交信息
```
commit 8e83cc9
修复场景投票问题和添加HP调试日志

问题修复:
- 修复直连模式下SteamID检查导致玩家被踢出的问题
- 现在只在Steam P2P模式下才检查SteamID
- 添加传输模式日志输出

HP调试功能:
- 在关键位置添加JSON格式的HP同步日志
- 添加血量有效性检查，防止上报无效数据
```

## 总结
这个问题是由于SteamID检查逻辑在所有模式下都执行导致的。修复后，直连模式下不再检查SteamID，玩家可以正常进入场景。Steam P2P模式下仍然保持原有的安全检查。
