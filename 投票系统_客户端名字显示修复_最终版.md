# 投票系统 - 客户端名字显示修复（最终版）

## 问题描述

**客户端日志显示：**
- 连接时（22:42:36）：客户端显示 "Player_0" 和 "Host"（默认名字）
- 投票开始后（22:42:40）：能正确显示 "CLIENT_ransx" 和 "HOST_pansx"

**根本原因：**
客户端在非投票状态下，无法获取主机的 Steam 信息，因为主机没有向客户端发送自己的 Steam 信息。

## 解决方案

### 方案概述
主机在收到客户端状态消息时，发送一个 `active=false`、`voteId=0` 的投票消息来更新客户端的玩家名字显示。

### 实现细节

#### 1. 主机端（ClientStatusMessage.cs）

**已有功能：**
- `Host_HandleClientStatus()` 方法在收到客户端状态后，会调用 `SendPlayerInfoUpdateToClients()`
- `SendPlayerInfoUpdateToClients()` 方法会构建一个特殊的投票消息：
  - `voteId = 0`（特殊ID，表示这不是真正的投票）
  - `active = false`（不激活投票UI）
  - 包含所有玩家的 Steam 信息（主机 + 所有客户端）

**代码位置：**
```csharp
// EscapeFromDuckovCoopMod/Net/ClientStatusMessage.cs
// 第 334-403 行

private static void SendPlayerInfoUpdateToClients()
{
    // 构建玩家列表（包含主机和所有客户端的 Steam 信息）
    var playerList = new System.Collections.Generic.List<SceneVoteMessage.VotePlayerInfo>();
    
    // 添加主机自己
    var (hostSteamId, hostSteamName) = GetLocalSteamInfo();
    playerList.Add(new SceneVoteMessage.VotePlayerInfo
    {
        playerId = $"Host:{service.port}",
        playerName = "Host",
        steamId = hostSteamId,
        steamName = hostSteamName,
        ready = false
    });
    
    // 添加所有客户端...
    
    // 构建投票数据（active=false，voteId=0）
    var voteData = new SceneVoteMessage.VoteStateData
    {
        type = "sceneVote",
        voteId = 0,  // 特殊ID
        active = false,  // 不激活投票UI
        playerList = new SceneVoteMessage.VotePlayerList { items = playerList },
        // ...
    };
    
    // 发送给所有客户端
    JsonMessage.BroadcastToClients(voteData, LiteNetLib.DeliveryMethod.ReliableOrdered);
}
```

#### 2. 客户端端（SceneVoteMessage.cs）

**修改内容：**
在 `Client_HandleVoteState()` 方法中，添加对 `voteId=0` 且 `active=false` 消息的特殊处理。

**修改位置：**
```csharp
// EscapeFromDuckovCoopMod/Net/SceneVoteMessage.cs
// 第 578-607 行

// 如果投票已取消
if (!data.active)
{
    // 🆕 特殊处理：voteId=0 表示这是玩家信息更新消息（不是真正的投票）
    if (data.voteId == 0 && data.playerList != null && data.playerList.items != null)
    {
        LoggerHelper.Log($"[SceneVote] 收到玩家信息更新消息 (voteId=0)，更新缓存但不激活投票UI");
        
        // 🔧 更新缓存的投票数据（供 UI 使用），但不激活投票
        sceneNet.cachedVoteData = data;
        
        LoggerHelper.Log($"[SceneVote] ✓ 已更新玩家信息缓存，共 {data.playerList.items.Length} 名玩家");
        return;
    }
    
    // 原有的投票取消逻辑...
}
```

## 工作流程

1. **客户端连接**
   - 客户端发送 `ClientStatusMessage`（包含 SteamID 和 SteamName）

2. **主机处理**
   - 主机收到客户端状态消息
   - 缓存客户端的 Steam 信息
   - 调用 `SendPlayerInfoUpdateToClients()`

3. **主机广播**
   - 构建 `voteId=0`、`active=false` 的投票消息
   - 包含所有玩家的 Steam 信息（主机 + 客户端）
   - 广播给所有客户端

4. **客户端接收**
   - 客户端收到投票消息
   - 检测到 `voteId=0` 且 `active=false`
   - 更新 `cachedVoteData`（供 UI 使用）
   - **不激活投票UI**

5. **UI 显示**
   - MModUI 从 `cachedVoteData` 读取玩家的 Steam 名字
   - 正确显示 "HOST_pansx" 和 "CLIENT_ransx"

## 优势

1. **复用现有机制**：利用投票系统的 JSON 消息机制，无需新增消息类型
2. **不影响投票**：`voteId=0` 确保不会与真实投票冲突
3. **不干扰UI**：`active=false` 确保不会激活投票UI
4. **实时更新**：每次客户端状态变化时都会更新

## 测试验证

**预期结果：**
- 客户端连接后，立即能看到正确的 Steam 名字（无需等待投票）
- 主机和客户端都能正确显示对方的 Steam 名字
- 不会出现投票UI的闪烁或误触发

**测试步骤：**
1. 主机启动游戏并创建房间
2. 客户端加入房间
3. 检查客户端的玩家列表UI
4. 验证是否显示正确的 Steam 名字（如 "HOST_pansx" 和 "CLIENT_ransx"）

## 相关文件

- `EscapeFromDuckovCoopMod/Net/ClientStatusMessage.cs`（主机端处理）
- `EscapeFromDuckovCoopMod/Net/SceneVoteMessage.cs`（客户端端处理）
- `EscapeFromDuckovCoopMod/Main/UI/MModUI.cs`（UI 显示）

## 注意事项

1. **voteId=0 是保留ID**：不要在真实投票中使用 voteId=0
2. **依赖 cachedVoteData**：UI 必须从 `SceneNet.cachedVoteData` 读取玩家信息
3. **Steam 初始化**：需要确保 Steam 已正确初始化，否则无法获取 Steam 名字
