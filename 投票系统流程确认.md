# 投票系统流程确认

## 当前实现流程

### 1. 客户端投票（切换准备状态）

**文件**: `SceneVoteMessage.cs` -> `Client_ToggleReady(bool ready)`

```csharp
// 1. 构建准备状态切换消息
var data = new ReadyToggleData {
    playerId = myId,
    ready = ready,
    timestamp = ...
};

// 2. 发送给主机
JsonMessage.SendToHost(data, DeliveryMethod.ReliableOrdered);

// 3. 本地乐观更新
sceneNet.localReady = ready;
sceneNet.sceneReady[myId] = ready;
```

### 2. 主机接收并处理

**文件**: `SceneVoteMessage.cs` -> `Host_HandleReadyToggle(string json)`

```csharp
// 1. 解析JSON消息
var data = JsonUtility.FromJson<ReadyToggleData>(json);

// 2. 调用处理方法
Host_HandleReadyToggle(data.playerId, data.ready);
```

**文件**: `SceneVoteMessage.cs` -> `Host_HandleReadyToggle(string playerId, bool ready)`

```csharp
// 1. 解析当前所有玩家的准备状态
var states = new Dictionary<string, bool>();
foreach (var pair in _hostVoteState.readyStates.Split(',')) {
    var lastColonIndex = pair.LastIndexOf(':');
    var pid = pair.Substring(0, lastColonIndex);
    var readyStr = pair.Substring(lastColonIndex + 1);
    states[pid] = readyStr == "true";
}

// 2. 更新该玩家的状态
states[playerId] = ready;

// 3. 重新构建 readyStates 字符串
_hostVoteState.readyStates = string.Join(",", 
    states.Select(kv => $"{kv.Key}:{(kv.Value ? "true" : "false")}"));

// 4. 立即广播更新
Host_BroadcastVoteState();

// 5. 检查是否全员准备
bool allReady = states.Count > 0 && states.Values.All(r => r);
if (allReady) {
    Host_StartSceneLoad();
}
```

### 3. 主机广播更新

**文件**: `SceneVoteMessage.cs` -> `Host_BroadcastVoteState()`

```csharp
// 更新时间戳
_hostVoteState.timestamp = System.DateTime.Now.ToString("yyyy-MM-dd HH:mm:ss.fff");

// 发送给所有客户端（包括发起投票的客户端）
JsonMessage.BroadcastToAllClients(_hostVoteState, DeliveryMethod.ReliableOrdered);
```

### 4. 所有客户端接收更新

**文件**: `SceneVoteMessage.cs` -> `Client_HandleVoteState(string json)`

```csharp
// 1. 解析JSON
var data = JsonUtility.FromJson<VoteStateData>(json);

// 2. 清空并重建参与者列表
sceneNet.sceneParticipantIds.Clear();
sceneNet.sceneReady.Clear();

// 3. 从 readyStates 解析所有玩家的状态
foreach (var pair in data.readyStates.Split(',')) {
    var lastColonIndex = pair.LastIndexOf(':');
    var pid = pair.Substring(0, lastColonIndex);
    var readyStr = pair.Substring(lastColonIndex + 1);
    var isReady = readyStr == "true";
    
    // 添加到列表
    sceneNet.sceneParticipantIds.Add(pid);
    sceneNet.sceneReady[pid] = isReady;
    
    // 如果是自己，更新本地状态
    if (service.IsSelfId(pid)) {
        sceneNet.localReady = isReady;
    }
}
```

## 确认清单

### ✅ 已实现的功能

1. **客户端投票后发送消息给主机** ✅
   - `Client_ToggleReady()` 发送 JSON 消息

2. **主机更新 readyStates 字符串** ✅
   - `Host_HandleReadyToggle()` 更新 `_hostVoteState.readyStates`
   - 格式: `"id1:true,id2:false"`

3. **主机广播更新给所有客户端** ✅
   - `Host_BroadcastVoteState()` 调用 `JsonMessage.BroadcastToAllClients()`

4. **所有客户端接收并更新本地状态** ✅
   - `Client_HandleVoteState()` 解析 `readyStates` 并更新 `sceneNet.sceneReady`

### ❓ 需要确认的问题

**UI 是否会自动刷新？**

投票系统更新了 `SceneNet.Instance.sceneReady` 字典，但 UI 需要：

1. **定期读取** `sceneNet.sceneReady` 来显示每个玩家的准备状态
2. **或者监听** `sceneReady` 的变化事件来触发 UI 更新

**可能的问题：**

- 如果 UI 只在投票开始时读取一次 `sceneReady`，后续的更新不会显示
- 如果 UI 没有在 Update() 中持续读取 `sceneReady`，UI 不会刷新

## 建议的解决方案

### 方案1：UI 在 Update() 中持续读取

UI 代码应该在每帧检查 `sceneNet.sceneReady` 的值并更新显示：

```csharp
void Update() {
    if (sceneNet.sceneVoteActive) {
        foreach (var kv in sceneNet.sceneReady) {
            var playerId = kv.Key;
            var isReady = kv.Value;
            // 更新UI显示该玩家的准备状态
            UpdatePlayerReadyUI(playerId, isReady);
        }
    }
}
```

### 方案2：添加事件通知

在 `Client_HandleVoteState()` 中，解析完状态后触发事件：

```csharp
// 在 SceneVoteMessage.cs 中添加事件
public static event Action OnVoteStateUpdated;

// 在 Client_HandleVoteState() 末尾触发
OnVoteStateUpdated?.Invoke();

// UI 订阅事件
SceneVoteMessage.OnVoteStateUpdated += RefreshVoteUI;
```

## 测试方法

1. **启动2人联机**
2. **主机发起投票**
3. **客户端点击准备**
4. **查看日志**：
   - 客户端：`[SceneVote] 客户端发送准备状态切换`
   - 主机：`[SceneVote] 主机收到准备状态切换消息`
   - 主机：`[SceneVote] 更新后的 readyStates: ...`
   - 主机：`[SceneVote] 已广播更新的投票状态`
   - 所有客户端：`[SceneVote] 收到 readyStates: ...`
   - 所有客户端：`[SceneVote] 解析玩家: pid='...', ready=true`
5. **检查 UI**：
   - 主机的投票面板是否显示客户端已准备？
   - 客户端的投票面板是否显示自己已准备？

## 下一步

需要查看投票 UI 的实现代码，确认它是如何读取和显示 `sceneReady` 状态的。

可能的文件位置：
- `MModUI.cs`
- `ModUI.cs`
- `SceneNet.cs` 的 OnGUI 方法
- 或者其他 UI 相关文件

