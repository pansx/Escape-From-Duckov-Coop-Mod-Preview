# 场景投票客户端无法显示问题分析

## 问题描述

主机发起场景投票后，只有主机能看到投票界面，客户端看不到。

## 日志分析

### 主机端日志（logs_host.txt）

主机端正常工作，每秒广播投票消息：

```
[19:30:24] [INFO] [JSON] 暴力发包 2 个客户端
{"type":"sceneVote","active":true,"targetSceneId":"Level_GroundZero_Main",...}
[19:30:24] [INFO] [SceneVote] 主机广播 JSON:
```

**关键信息：**
- 主机正在向2个客户端发送消息
- 玩家列表包含3个玩家：
  - Host:9050 (主机)
  - 10.255.0.17:27015 (客户端1)
  - 10.255.0.3:27015 (客户端2)
- 消息格式正确，包含完整的playerList

### 客户端日志（logs_client.txt）

**客户端日志完全为空！**

这说明：
1. 客户端的日志系统可能没有正常工作
2. 或者客户端根本没有收到任何消息

## 可能的原因

### 1. 日志系统问题

客户端的日志可能没有正确配置或初始化。需要检查：
- `get-logs.bat` 脚本是否正确获取客户端日志
- 客户端的 `clientId=2` 是否正确

### 2. 消息接收问题

虽然主机显示"暴力发包 2 个客户端"，但客户端可能没有收到消息。可能的原因：
- 网络连接问题
- P2P会话未正确建立
- 消息被过滤或丢弃

### 3. 消息处理问题

即使客户端收到了消息，也可能在处理时出错：
- `JsonMessageRouter.HandleJsonMessage` 可能抛出异常
- `SceneVoteMessage.Client_HandleVoteState` 可能没有正确执行

## 代码检查

### 消息路由（JsonMessageRouter.cs）

```csharp
case "sceneVote":
    // 场景投票状态广播
    SceneVoteMessage.Client_HandleVoteState(json);
    break;
```

✅ 路由逻辑正确

### 消息处理（SceneVoteMessage.cs）

```csharp
public static void Client_HandleVoteState(string json)
{
    var service = NetService.Instance;
    if (service == null || service.IsServer)
        return;  // ⚠️ 如果是主机，直接返回
    
    Debug.Log($"[SceneVote] 客户端收到 JSON:\n{json}");
    // ...
}
```

✅ 处理逻辑正确，但需要确认客户端是否真的执行到这里

## 调试建议

### 1. 验证日志系统

在客户端添加更多日志输出，确认：
- 客户端是否收到任何网络消息
- `OnNetworkReceive` 是否被调用
- `Op.JSON` 分支是否被执行

### 2. 验证网络连接

检查：
- 客户端的 `NetService.Instance.IsServer` 值
- 客户端的 `connectedPeer` 是否为空
- P2P会话状态

### 3. 添加调试日志

在关键位置添加日志：

```csharp
// Mod.cs - OnNetworkReceive
case Op.JSON:
    Debug.Log($"[DEBUG] 收到JSON消息，长度: {reader.AvailableBytes}");
    JsonMessageRouter.HandleJsonMessage(reader);
    break;

// JsonMessageRouter.cs - HandleJsonMessage
public static void HandleJsonMessage(NetPacketReader reader)
{
    Debug.Log("[DEBUG] HandleJsonMessage 被调用");
    var json = reader.GetString();
    Debug.Log($"[DEBUG] JSON内容: {json}");
    // ...
}
```

## 下一步行动

1. **重新获取日志**：确保客户端日志正确获取
2. **添加调试日志**：在关键位置添加更多日志输出
3. **测试网络连接**：验证客户端是否真的连接到主机
4. **检查UI显示**：即使收到消息，UI可能也没有正确显示

## 临时解决方案

如果是日志系统问题，可以：
1. 在游戏中添加屏幕调试信息显示
2. 使用Unity的Debug.Log直接输出到Unity日志文件
3. 在UI上显示当前投票状态（用于调试）
